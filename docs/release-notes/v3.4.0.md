# v3.4.0 — Claude Opus 4.8 (1M context, Max thinking)

**Date:** 2026-05-29

## AI Model — Opus 4.8 (5)

- **Default model bumped `claude-opus-4-7` → `claude-opus-4-8`** across `config.py`, `constants_env.py`, `dashboard_config.py`, `api_failover.py` health check, `database.py` analytics default + column defaults, and `validate_runtime.py`
- **1M-token context window** — native on both the CLI subscription and direct-API backends (no beta header, no long-context premium)
- **Max thinking by default** — `CLAUDE_EFFORT` now defaults to `max` (Opus-tier reasoning) with adaptive thinking; effort is now forwarded on the Discord API path too, not just the dashboard
- **Adaptive-thinking detection hardened** — extracted `_uses_adaptive_thinking()`; Opus 4.7+/4.8 (adaptive-only) no longer fall through to the removed `budget_tokens` path, which would 400 on 4.8
- **Token pricing** — added Opus 4.8 ($5 / $25 per MTok); corrected the Opus 4.7 entry to $5 / $25 (was wrongly $15 / $75)

## Database (1)

- **Migration 016** — bumps the default model to `claude-opus-4-8` on `token_usage` + `ai_analytics`. Historical rows keep their real model attribution via `COALESCE(NULLIF(model,''), …)`; only blank/NULL models are backfilled.

## Fixes (4)

- **Dashboard offline conversation-switch race** — `loadConversationFallback` now drops late/out-of-order loads via the `pendingConversationLoadId` guard (mirrors the WebSocket path), so switching conversations while the bot is offline can no longer display the wrong one
- **Tauri CSP** — `connect-src` now allows `ws://localhost:8765` + `wss://localhost:8765`; the WS client dials `localhost` as a fallback and the shipped CSP was silently blocking it
- **install_all.ps1** — Go fallback installer bumped 1.23.5 → 1.26.3 to satisfy `go.mod` (`go 1.25.0`)
- **document_extractor.py** — corrected a stale diagnostic log ("defusedxml missing" → "python-docx not installed")

## Documentation (3)

- **Backend routing corrected** — `cli` is the default (was mislabeled `api (default)` in ARCHITECTURE.md); Discord AI replies + dashboard chat work in **both** modes; `api` is required only for the memory consolidator + history summarizer (both disabled under `cli`)
- **1M-context wording** — corrected to "native on both backends" (was "CLI backend only")
- **Test-count accuracy** — refreshed to 3,371 collected (3,363 pass / 8 skip) + 10 vitest files / 189 tests across README, CONTRIBUTING, Makefile, and the docs guides

## Verification

- Python: **3,363 passed / 8 skipped**, `ruff check .` clean
- Dashboard: `tsc --noEmit` clean, **189 vitest** pass, `cargo check` clean, `ui/*.js` in sync with `src-ts/`
- Migration 016 applied on a temp DB (defaults bumped, historical rows preserved, indexes recreated)
- Opus 4.8 request shape (`effort: max` + adaptive thinking + 1M) validated against the live Models API

## Stats

- 33 files changed (1 new migration + 1 new release note)
- Default model: `claude-opus-4-8` · Default effort: `max` · Context: 1,000,000 tokens
