# v3.3.13 — Python 3.14 Migration + Full Dependency Upgrade

**Date:** 2026-03-02

## Python 3.14.3 Migration
- Migrated runtime from Python 3.10 → **3.14.3**
- Updated `pyproject.toml` requires-python, ruff target `py314`, mypy `3.14`
- Updated `Dockerfile` + `Dockerfile.prod` base images → `python:3.14.3-slim`
- Updated CI matrix → Python 3.14 only
- Fixed `asyncio.iscoroutinefunction` deprecation → `inspect.iscoroutinefunction` (4 files)
- Removed Python 3.7 fallback comment in `bot.py`

## Dependency Updates (30/30 → latest)

### Python
- `google-genai` 1.64.0 → **1.65.0**
- `numpy` 2.2.6 → **2.4.2**
- Added `pytest-timeout==2.4.0` (safety net for hung tests)

### Rust Extensions (`rust_extensions/`)
- `pyo3` 0.21 → **0.28** (breaking: `allow_threads` → `detach`, `PyBytes::new_bound` → `new`, `#[pyclass(from_py_object)]`)
- `thiserror` 1.0 → **2.0**

### Native Dashboard (`native_dashboard/`)
- `rusqlite` 0.32 → **0.38**
- `sysinfo` 0.31 → **0.38** (breaking: `refresh_processes()` now takes 2 args)

### Config
- CI Go version 1.22 → **1.23** (matches `go.mod`)
- Dockerfile.prod Go builder 1.22 → **1.23**

## Audit Fixes
- **CSP**: Added `'unsafe-inline'` to `style-src` for KaTeX in `tauri.conf.json`
- **Database**: Added `close_pool()` + `close_pool_sync()` public API for clean shutdown

## Test Infrastructure
- Fixed tests hanging on Windows (root cause: DB pool keeping event loop alive)
- Added `pytest-timeout=30s` default for all tests
- Refactored `conftest.py`: use `close_pool_sync()` instead of internal `_conn_pool` hack
- Fixed `mock_bot` fixture: replaced real `asyncio.new_event_loop()` with `MagicMock`
- Moved `EventLoopPolicy` deprecation suppression to `pyproject.toml` filterwarnings
- **2926 tests pass, 0 failures, ~12s**

## Build Verification
- ✅ Python 3.14.3 — all imports OK
- ✅ Rust extensions — `cargo build --release`
- ✅ Native dashboard — `cargo tauri build` (NSIS installer)
- ✅ TypeScript — `tsc --noEmit`
- ✅ Full test suite — 2926 passed
