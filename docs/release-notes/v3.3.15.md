# v3.3.15 — Code Quality & Dead Code Cleanup

**Date:** 2026-04-02

## Dead Code Removal (3)

- **Deleted `content_processor.py`** — Deprecated re-export shim to `media_processor.py`, all usages migrated
- **Removed `detect_refusal()` + `RefusalResult`** — Dead code in `guardrails.py`, `api_handler.py`, `imports.py`; only `is_silent_block()` remains
- **Removed `ESCALATION_FRAMINGS` import** — Unused 4-tier refusal bypass escalation from `api_handler.py`

## Database Cleanup (1)

- **Migration 006** — `DROP TABLE IF EXISTS dashboard_character_profiles` (incomplete feature, no backend/frontend)

## Security Hardening (2)

- **CSP `unsafe-inline` removed** — Refactored all 28 inline `style.display` assignments to `classList.add/remove('hidden')` in dashboard TypeScript; petal `cssText` converted to individual property assignments; modal uses `.active` class pattern
- **Narrowed `except Exception`** — `entity_memory.py` → `aiosqlite.Error`, `ai_cache.py` → `sqlite3.DatabaseError`, `storage.py` → `aiosqlite.Error` (13 catches total narrowed)

## Observability (1)

- **Structured logging `extra={}`** — Added structured fields to 7+ key log statements in `api_handler.py`, `ai_cog.py`, `storage.py` (thinking mode, timeout, rate limit, bg_task_failed, cog_loaded, cache_cleanup, state_cleanup)

## Testing (1)

- **`test_dashboard_handlers.py`** — 42 new tests covering all 13 WebSocket dashboard handler functions

## CI/CD (1)

- **Smoke tests in CI** — Added "import core modules" and "DB schema init" steps to `python-test` job in `.github/workflows/ci.yml`

## Performance (1)

- **Rust criterion benchmarks** — Added `rag_bench` (cosine similarity at dim 128/512/1024) and `media_bench` (base64 encode/decode, GIF detection) benchmarks

## Code Quality (1)

- **Deduplicated `_KNOWN_TABLES`** — Shared `KNOWN_TABLES: frozenset[str]` in `utils/database/__init__.py`, used by both `database.py` and `view_db.py`

## Stats

- **3,012 tests passed**, 1 skipped, 0 failures
- **214 Python files**, 90 test files
- 20+ source files modified, 4 new files created, 1 file deleted
- All 4 test suites green: Python ✅ Go ✅ Rust (clippy) ✅ Dashboard ✅

## Files Changed

### Deleted

- `cogs/ai_core/content_processor.py`

### Created

- `scripts/maintenance/migrations/006_drop_character_profiles.sql`
- `tests/test_dashboard_handlers.py`
- `rust_extensions/rag_engine/benches/rag_bench.rs`
- `rust_extensions/media_processor/benches/media_bench.rs`

### Modified (Source)

- `cogs/ai_core/processing/guardrails.py` — Removed `detect_refusal`, `RefusalResult`, `detect_refusal_advanced`
- `cogs/ai_core/api/api_handler.py` — Removed dead code (GUARDRAILS_AVAILABLE, detect_refusal, ESCALATION_FRAMINGS, refusal_count), added structured logging
- `cogs/ai_core/imports.py` — Removed `detect_refusal` import
- `cogs/ai_core/ai_cog.py` — Added structured logging extra fields
- `cogs/ai_core/storage.py` — Narrowed exceptions, added structured logging
- `cogs/ai_core/memory/entity_memory.py` — Narrowed exceptions to `aiosqlite.Error`
- `cogs/ai_core/cache/ai_cache.py` — Narrowed exceptions to `sqlite3.DatabaseError`
- `native_dashboard/src-ts/app.ts` — Replaced all inline styles with CSS classes
- `native_dashboard/ui/index.html` — Replaced `style="display:none"` with `.hidden` class
- `native_dashboard/ui/styles.css` — Added `.hidden` utility class
- `native_dashboard/ui/app.js` — Regenerated from TypeScript
- `native_dashboard/tauri.conf.json` — Removed `'unsafe-inline'` from CSP `style-src`
- `utils/database/__init__.py` — Added shared `KNOWN_TABLES` frozenset
- `utils/database/database.py` — Uses shared `KNOWN_TABLES`
- `scripts/maintenance/view_db.py` — Uses shared `KNOWN_TABLES`
- `rust_extensions/Cargo.toml` — Added criterion workspace dependency
- `rust_extensions/rag_engine/Cargo.toml` — Added benchmark config
- `rust_extensions/media_processor/Cargo.toml` — Added benchmark config
- `rust_extensions/media_processor/src/lib.rs` — Added `get_gif_frame_count` re-export
- `.github/workflows/ci.yml` — Added smoke test steps

### Modified (Tests)

- `tests/test_media_processor.py` — Migrated from content_processor imports
- `tests/test_api_handler.py` — Removed dead code test methods
- `tests/test_api_handler_retry.py` — Removed GUARDRAILS_AVAILABLE mock
- `tests/test_guardrails.py` — Removed detect_refusal test classes
