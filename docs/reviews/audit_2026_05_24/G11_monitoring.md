# G11: Monitoring Subsystem — Re-Audit Report

Audit date: 2026-05-24
Auditor scope: MONITORING subsystem of the production Discord bot (Python 3.13).
Method: every line of every assigned file read in full (full working-tree content, not diff). Cross-referenced against the rest of the repo (bot.py, utils/database, cogs/ai_core/*, tests/*, requirements.txt) and against the prior audit `docs/reviews/audit_2026_05/` (G9_monitoring.md, MASTER_TABLE.md). Dependency currency verified via web search (cutoff Jan 2026; today 2026-05-24).

Files audited (13):
`utils/monitoring/health_api.py` (1268), `structured_logger.py` (668), `metrics.py` (427), `performance_tracker.py` (362), `token_tracker.py` (342), `alerting.py` (313), `sentry_integration.py` (306), `feedback.py` (298), `audit_log.py` (258), `health_client.py` (383), `logger.py` (382), `tracing.py` (50), `__init__.py` (17). Cross-ref read: `cogs/ai_core/cache/token_tracker.py`, `cogs/ai_core/imports.py`, `utils/database/database.py` (audit_log schema).

## Severity-count summary (this re-audit)

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 2 |
| MEDIUM | 9 |
| LOW | 31 |
| INFO | 6 |
| **Total** | **48** |

### Prior-finding disposition (G9 → 2026-05-24)

| Prior ID | Title | Disposition (current lines) |
|---|---|---|
| H30 | audit_log silent loss when DB missing (returned True) | **FIXED-VERIFIED** — JSONL fallback `_write_fallback_entry` now persists on both `DB_AVAILABLE=False` (audit_log.py:101-127) and runtime DB write failure (154-175). |
| H31 | audit_log no append-only / hash chain / tamper resistance | **STILL-PRESENT** — schema at database.py:594-603 has no hash chain, no UPDATE/DELETE trigger; fallback JSONL also has no chaining. See G11-AUDIT-1. |
| H32 | Sentry attach_stacktrace + locals leak secrets | **FIXED-VERIFIED** — `_scrub_event` before_send strips `vars`/`pre_context`/`context_line`/`post_context` (sentry_integration.py:130-159) + `include_local_variables=False` (174). |
| H33 | Two TokenTracker impls used simultaneously → double count + drift | **PARTIALLY-FIXED / changed shape** — monitoring impl now marked DEPRECATED (token_tracker.py:5-12). But it is now effectively DEAD: `record_token_usage` is imported in imports.py:147-153 yet **never called** anywhere; cache impl's `record_usage` is also not called from the live AI path. No double-count today, but the divergence/dead-wiring is real. See G11-TT-1. |

---

## Findings table

| File | Line(s) | Severity | Category | Issue | Prior | Suggested fix |
|---|---|---|---|---|---|---|
| utils/monitoring/audit_log.py | 76-149, 29; database.py:594-603 | HIGH | tamper-resistance / integrity | Audit log has **no tamper resistance**: SQLite `audit_log` table has no `BEFORE UPDATE/DELETE` trigger and no `prev_hash` chain; `created_at DATETIME DEFAULT CURRENT_TIMESTAMP` is freely updatable and stored in server-local time (not UTC). The JSONL fallback (`audit_fallback.jsonl`) is plain append with no chaining either — a shell user can edit/delete rows in either store undetected. For a security/accountability log this defeats the purpose. | H31 STILL-PRESENT | Add a `BEFORE UPDATE/DELETE` trigger that raises; add a `prev_hash` HMAC chain column (and a chained `hash` field in the JSONL fallback). Switch `created_at` to UTC (`STRFTIME('%Y-%m-%dT%H:%M:%fZ','now')`) or epoch INTEGER. |
| utils/monitoring/token_tracker.py | 5-12, 121-195, 337-342; imports.py:147-153; ai_cog.py:199-205 | HIGH | dead/unwired code / metric integrity | The monitoring `TokenTracker.record()` and `record_token_usage()` are **dead in production**: `record_token_usage` is imported (imports.py:147) but **never invoked** anywhere in the codebase; the cache `token_tracker.record_usage()` is likewise never called from the live AI request path (only `init_from_db()` at ai_cog.py:205 and `get_global_stats()` at debug_commands.py:377 are used). Net effect: per-user/channel/global token analytics + quotas from BOTH trackers are populated only by DB replay, and the in-memory monitoring tracker records nothing — any metric pulled from it is empty/misleading. | H33 PARTIALLY-FIXED | Confirm where token usage is actually recorded in the streaming/response path; wire exactly one tracker into it. Then delete the monitoring `TokenTracker` (or make it a thin re-export) and remove the dead `record_token_usage` import. |
| utils/monitoring/health_api.py | 396-419, 691-700 | MEDIUM | CORS / info-disclosure | `Access-Control-Allow-Origin: http://localhost` is hardcoded on all 2xx JSON/HTML responses. When `HEALTH_API_TOKEN` is unset, an ephemeral token is auto-generated (76-99) so endpoints return 401 to any caller lacking the token — but the dashboards (`/`, `/health`, `/stats`, `/ai/stats`) require the Bearer header which a cross-origin browser tab cannot attach, so this is mitigated. Residual: the static CORS origin still advertises `http://localhost` to any local page; with a known/leaked token, a localhost tab could read internal state. | G9 CORS (partial) | Drop the CORS header entirely unless a same-origin dashboard genuinely needs it, or gate it behind an explicit `HEALTH_API_ALLOW_CORS` env. |
| utils/monitoring/health_api.py | 720-737 | MEDIUM | unauthenticated-info-leak | `/health/ready` + `/readyz` are intentionally NOT in `_PROTECTED_ENDPOINTS` (k8s probes), but the ready branch returns `latency_ms` and `guilds` count to unauthenticated callers (730-732). Discord latency and guild count are internal state that should not be exposed on an unauth probe. | G9 (k8s-probe) STILL-PRESENT | Return only `{"status": "ready"}` on the unauth path; move detailed fields behind the token. |
| utils/monitoring/health_api.py | 1156, 1194, 314 | MEDIUM | concurrency / data-race | `health_data.feature_flags = _ff.get_all()` is assigned from the update loop (1156, 1194) while `to_dict()` reads `self.feature_flags` (314) and `service_health` (313) — the dict-reference swap for feature_flags is atomic, but `service_health` is mutated key-by-key under `_data_lock` in `check_service` (1167, 1179) while `to_dict()` reads `self.service_health` at line 313 **without** taking `_data_lock`. A Prometheus/HTML scrape can observe a half-updated `service_health` row or trip a dict-resize race. | new (related to G9 concurrency theme) | Read `service_health`/`feature_flags` under `_data_lock` inside `to_dict()` (snapshot-copy), matching the pattern already used for counters. |
| utils/monitoring/health_api.py | 730-731, 739-744, 906-909 | MEDIUM | concurrency / data-race | `/health/ready` reads `health_data.latency_ms`/`guild_count`, `/health/status` calls `is_healthy()` then `_perform_deep_health_check` reads `health_data.latency_ms`/`guild_count`/`is_ready` (906-909) all **outside** `_data_lock`. `is_healthy()` itself locks (324-336) but the subsequent direct attribute reads in the handlers/deep-check do not, so they can tear against `update_from_bot`. | new | Snapshot the needed fields under `_data_lock` once per handler invocation. |
| utils/monitoring/structured_logger.py | 166-199 | MEDIUM | secret-redaction (incomplete) | `StructuredFormatter.format` now runs the serialized JSON through `_redact_sensitive` (193-198) — good. But `_redact_sensitive` is regex-based and only catches token-shaped strings; an `extra={"api_key": "<short>"}` or a structured field whose value is a non-string (e.g. a dict containing secrets, serialized via `default=str` at 188) can still leak. Redaction runs on the final string so nested dict values ARE covered, but `default=str` will stringify arbitrary objects (e.g. a `discord.User`) into PII before redaction sees them. | G9 extra-leak (improved) | Keep the string-level redaction; additionally use a strict `default` that drops/whitelists object types instead of `str`, and document that `extra` values must not carry raw secrets. |
| utils/monitoring/alerting.py | 24, 56, 214-221 | MEDIUM | secret-handling / SSRF | `ALERT_WEBHOOK_URL` is read from env with **no host allowlist**. A misconfigured/hostile `.env` can ship every alert (titles, descriptions, memory MB, service names, error context) to an attacker-controlled URL. No validation that it targets `discord.com/api/webhooks`. | G9 secret-handling STILL-PRESENT | Validate the webhook host against `discord.com`/`discordapp.com` at init; reject + log otherwise. |
| utils/monitoring/alerting.py | 194-217 | MEDIUM | log-injection / mention-abuse | The Discord embed `description`/`fields`/`title` flow into the webhook payload with no `allowed_mentions`. `service`/`breaker_name` (used in `alert_health_check_failed`, `alert_circuit_breaker_open`) come from config/code today, but if any caller-influenced string reaches them, `@everyone`/`@here`/role mentions would fire. | G9 log-injection STILL-PRESENT | Add `payload["allowed_mentions"] = {"parse": []}`. |
| utils/monitoring/logger.py | 187-251 | MEDIUM | secret-redaction (coverage gap) | `_redact_sensitive` is comprehensive (Discord/JWT/sk-/AIza/AWS/GitHub) but does NOT cover **Discord webhook URLs** (`discord(app)?.com/api/webhooks/<id>/<token>`). `alerting.py:236` logs `title[:100]` via `logger.exception`, and aiohttp tracebacks can include the request URL → webhook token can land in logs. | G9 secret-pattern-coverage STILL-PRESENT | Add `discord(?:app)?\.com/api/webhooks/\d+/[A-Za-z0-9_\-]+` to `_SECRET_PATTERNS_CI`. |
| utils/monitoring/logger.py | 69-117 | MEDIUM | import-time side-effect | The Windows branch calls `SetConsoleOutputCP(65001)`, `SetConsoleCP`, `sys.stdout.reconfigure(...)`, and `SetConsoleMode` at **module import time**. Importing `utils.monitoring` (via `__init__.py` → `logger`) mutates global console state and is inherited by subprocesses; hurts test isolation. | G9 windows-only-side-effect STILL-PRESENT | Move console setup into an explicit `setup_console()` called once from bot.py. |
| utils/monitoring/__init__.py | 3-17 | LOW | api-surface | Package re-exports only health_api + logger + structured_logger symbols; `alert_manager`, `audit`, `feedback_collector`, `metrics`, `perf_tracker`, `token_tracker`, `tracing`, `sentry_integration` require deep imports. Inconsistent public surface. | G9 LOW STILL-PRESENT | Standardize on deep paths (document) or expand `__init__`. |
| utils/monitoring/health_api.py | 71-99 | INFO | security-default (improved) | Missing `HEALTH_API_TOKEN` now auto-generates an ephemeral urlsafe token and logs only an 8-char prefix, so protected endpoints fail closed (401) instead of serving unauthenticated. Good improvement over prior "silently exposed". Note: ephemeral token changes each restart → sidecars/Grafana must set a stable token. | improvement over G9 | None (document operator requirement). |
| utils/monitoring/health_api.py | 32-68, 114-115 | INFO | env-validation (fixed) | `_env_int`/`_env_float` now guard against bad env input that previously crashed import. | G9 env-validation FIXED | None. |
| utils/monitoring/health_api.py | 101-111 | LOW | security-default | Remote bind requires `HEALTH_API_ALLOW_REMOTE`; otherwise forced to 127.0.0.1. Good. But if an operator sets `HEALTH_API_ALLOW_REMOTE=1` with a non-localhost host, the API (incl. dashboards) is exposed on a public interface protected only by the Bearer token — no rate limiting, no TLS. | partial G9 | Document that remote binding needs a reverse proxy with TLS; consider refusing remote bind unless a token is explicitly set. |
| utils/monitoring/health_api.py | 119-141 | LOW | SSRF-defense | `_validate_service_url` blocks userinfo + non-localhost hostnames but does not block hostnames that resolve to public IPs via DNS rebinding, nor `0.0.0.0`. Low risk (localhost-only sidecar). | G9 LOW STILL-PRESENT | Validate via `socket.gethostbyname` or restrict to numeric loopback literals. |
| utils/monitoring/health_api.py | 204-207, 825, 1000 | LOW | concurrency | `_get_process()` lazy-inits `self._process` with no lock; under ThreadingHTTPServer two concurrent first-requests can race two `psutil.Process()` constructions (one orphaned → its cpu_percent baseline lost). Benign but real. | G9 MEDIUM (downgraded; impact small) | Guard `_get_process` with a lock or `threading.local`. |
| utils/monitoring/health_api.py | 269, 305, 844, 883 | LOW | metric-correctness | `cpu_percent(interval=None)` returns 0.0 on the first call after start; health/metrics scrapes during the first interval report CPU=0. Documented in code. | G9 test-impact noted | None (documented); optionally seed one warm-up call at startup. |
| utils/monitoring/health_api.py | 877-879 | LOW | metric-format | `discord_bot_uptime_seconds` declared `# TYPE ... counter` but resets to 0 on restart; Prometheus counters must be monotonic. Misleads `rate()`/`increase()`. | G9 LOW STILL-PRESENT | Declare as `gauge`, or expose `process_start_time_seconds` (epoch) and let Prom derive uptime. |
| utils/monitoring/health_api.py | 982-990 | LOW | filesystem-side-effect | `/health/deep` writes/reads/unlinks `temp/.health_check` on every call; on slow disks this blocks the request thread. | G9 LOW STILL-PRESENT | Memoize result ~30s. |
| utils/monitoring/health_api.py | 1019-1034 | LOW | dependency-coupling | Deep check inspects only `gemini_circuit`; multi-breaker deployments report just one. | G9 LOW STILL-PRESENT | Iterate registered breakers. |
| utils/monitoring/health_api.py | 1216-1220 | LOW | alert-spam | `_service_failures.get(svc,0) >= 3` keeps firing every loop iteration once ≥3; relies solely on the alert cooldown to throttle. Failure counter is never reset on successful alert. | G9 LOW STILL-PRESENT | Reset/clamp counter after an alert fires, or alert only on the 3→… edge. |
| utils/monitoring/health_api.py | 1235-1237 | LOW | error-swallowing | `update_health_loop`'s outer `except Exception` logs + continues for ALL errors (incl. MemoryError-class). `logger.exception` is called (good) but the loop can mask sustained faults. | G9 LOW STILL-PRESENT | Narrow the catch or add a consecutive-failure breaker. |
| utils/monitoring/health_api.py | 338-354 | LOW | type-hint / coupling | `get_ai_performance_stats(self) -> dict` (not `dict[str,Any]`); couples monitoring to cog name `"AI"` + `chat_manager` attr path — silently returns the error dict if the cog is renamed. | G9 LOW STILL-PRESENT | Tighten return type; log which branch failed (cog vs attr). |
| utils/monitoring/health_api.py | 421-521, 523-569 | LOW | privacy / XSS-template | Dashboard CSS `@import`s `fonts.googleapis.com` → every health-page hit leaks IP/timing to Google. `_get_sakura_js` interpolates `json_endpoint` into JS via f-string (safe today: internal constants only). | G9 LOW STILL-PRESENT | Self-host font; `json.dumps()` the JS-embedded values. |
| utils/monitoring/structured_logger.py | 38 | LOW | contextvar-default | `_log_context` declared with no default; every read passes `.get({})` — correct, but a future caller forgetting the default raises `LookupError`. | G9 LOW STILL-PRESENT | Declare `ContextVar("log_context", default=None)` and normalize. |
| utils/monitoring/structured_logger.py | 188 | LOW | privacy | `json.dumps(..., default=str)` stringifies arbitrary objects → a `discord.User`/`Member` in `extra` becomes a username (PII) in the JSON log. | G9 LOW STILL-PRESENT | Strict `default` (whitelist primitives) or drop unknowns. |
| utils/monitoring/structured_logger.py | 419-462 | LOW | concurrency | `PerformanceTimer._timings` is a plain dict mutated in `measure()` with no lock; `global_timer` (667) is a shared singleton. Concurrent `measure()` from threads can race the `if step_name not in` insert. | G9 MEDIUM (impact bounded) | Guard with a lock or use `setdefault`. |
| utils/monitoring/structured_logger.py | 219, 242 | LOW | timezone | `HumanReadableFormatter` uses naive `datetime.now()` (local) while the JSON formatter uses UTC — inconsistent across the package. | G9 LOW STILL-PRESENT | Acceptable for console; document. |
| utils/monitoring/structured_logger.py | 318 | LOW | id-collision | `request_id = uuid4()[:8]` (32 bits) — birthday collisions ~65k concurrent. Fine for single-bot tracing. | G9 LOW STILL-PRESENT | Use full uuid for cross-service correlation. |
| utils/monitoring/structured_logger.py | 609-663 | LOW | privacy | `log_ai_request` logs `message_preview` (first 100 chars of user message) with no redaction — RP users may paste tokens/PII. | G9 LOW STILL-PRESENT | Run preview through `_redact_sensitive` or drop it. |
| utils/monitoring/metrics.py | 180-217 | INFO | server-binding (fixed) | `start_http_server(port, addr="127.0.0.1")` with explicit refusal (TypeError branch) on old prometheus_client that defaults to 0.0.0.0. Good. | G9 server-binding FIXED | None. |
| utils/monitoring/metrics.py | 32-42, 269-296 | INFO | concurrency (fixed) | `_guild_queue_sizes` moved to per-instance + `_queue_lock` (was ClassVar shared state). | G9 concurrency FIXED | None. |
| utils/monitoring/metrics.py | 219-229 | LOW | shutdown-guard | `shutdown_server` assumes `self._server` truthy implies a real server object; on older prometheus_client the tuple-unpack stored `None`, so `_server_started=True` but `_server=None` → shutdown is a silent no-op (server thread keeps running). | new (edge) | Track started state independent of `_server`; log if started-but-no-handle. |
| utils/monitoring/metrics.py | 142-146, 418-422 | LOW | label-cardinality | `rate_limit_blocked` now has an allowlist (good). Verify allowlists (`_COMMAND_LABEL_ALLOWLIST`, `_CIRCUIT_NAME_ALLOWLIST`, `_RATE_LIMIT_CONFIG_ALLOWLIST`) stay in sync with real command/breaker/config names; new names silently roll up to "other". | G9 LOW (mostly addressed) | Add a test asserting allowlists cover registered names. |
| utils/monitoring/performance_tracker.py | 123-130, 136-149 | MEDIUM | concurrency | `_stats`/`_hourly_stats` are `defaultdict(PerformanceStats)`; `record()` accesses `self._stats[operation]` and does `self._record_count += 1` with no lock. `record()` is reachable from `_TimerContext.__exit__` in any thread (sync `with measure()`), so the default-factory insert and the counter increment can race (lost recording / redundant cleanup). `PerformanceStats.record` itself is locked, but the dict insertion is not. | G9 MEDIUM STILL-PRESENT | Guard dict access + `_record_count` with a `threading.Lock` (or `setdefault`). |
| utils/monitoring/performance_tracker.py | 48-74 | LOW | percentile-correctness | `p50` returns `sorted[len//2]` (upper element for even N, not interpolated median); `p95`/`p99` use `int(len*q)` nearest-rank. Acceptable approximation but `p50` is biased high for small/even samples and all are computed over only the last 1000 samples (`recent_times` maxlen). | new (nuance) | Document the nearest-rank method; consider `statistics.quantiles` if exactness matters. |
| utils/monitoring/performance_tracker.py | 142-143, 195-196, 214-215 | LOW | timezone | Hourly bucket keys use naive `datetime.now()` (local); cross-DST/multi-region buckets misalign. | G9 LOW STILL-PRESENT | Use UTC. |
| utils/monitoring/performance_tracker.py | 286-338 | LOW | dead-end (Prometheus bridge) | `record_to_prometheus` writes histograms into a **private** `_prom_registry`; `get_prometheus_metrics()` (the only reader) is **never called** anywhere in the repo, and the `metrics.py` HTTP server exposes the *default* registry, not this one. These histograms are recorded but never scraped. | G9 dual-registry STILL-PRESENT | Register on the default registry, or remove the bridge, or wire `get_prometheus_metrics()` into the `/metrics` endpoint. |
| utils/monitoring/performance_tracker.py | 311 | LOW | metric-naming | `safe_name = operation.replace(...).lower()` doesn't strip non-`[a-z0-9_]`; a Thai/Unicode operation name yields an invalid Prometheus metric name → `Histogram()` raises (caught only by the histogram lock, not by name validation). | G9 LOW STILL-PRESENT | `re.sub(r'[^a-z0-9_]','_', safe_name)`. |
| utils/monitoring/sentry_integration.py | 95-159, 161-180 | INFO | stack-var-scrub (fixed) | `_scrub_event` strips `vars`/`pre_context`/`context_line`/`post_context` on every exception+thread frame and re-redacts the message; `include_local_variables=False` set. Solid fix for H32. | H32 FIXED | None. |
| utils/monitoring/sentry_integration.py | 266-298 | INFO | scope-isolation (fixed) | `set_user_context` now writes to `get_isolation_scope()` (per-task) with graceful fallback; `set_user_global` provided separately. Fixes prior global-scope user-leak. | G9 scope-pollution FIXED | None. |
| utils/monitoring/sentry_integration.py | 104-108 | LOW | breadcrumb-scrub depth | `_scrub_breadcrumb` redacts `crumb["data"]` only one level deep (top-level string values); nested dicts/lists inside `data` are not walked. | G9 (partially) | Recursively scrub, or rely on the before_send message re-redaction (note current limitation). |
| utils/monitoring/sentry_integration.py | 161-167 | LOW | integration-control | `integrations=[logging_integration]` does not set `auto_enabling_integrations=False`, so Sentry still auto-enables StdLib/Threading/AsyncIO/etc. Not a bug; reduces explicit control. | G9 LOW STILL-PRESENT | Pass `auto_enabling_integrations=False` if explicit control desired. |
| utils/monitoring/sentry_integration.py | 27-33, 161-180 | LOW | sampling-cost | `traces_sample_rate=0.1` default = 10% transaction tracing; for a high-volume bot this is still notable spend. | G9 LOW STILL-PRESENT | Document; consider lowering in prod. |
| utils/monitoring/feedback.py | 53-59, 265-285 | LOW | privacy | `FeedbackEntry` stores `user_id` + 100-char response `context`; `export_stats`/`get_recent_negative` expose them. Internal tool, but PII. | G9 LOW STILL-PRESENT | Document trust boundary; consider hashing user_id in exports. |
| utils/monitoring/feedback.py | 238-263 | LOW | logic-inconsistency | `get_channel_stats` has no `hours` window while `get_stats(hours=...)` does — channel stats are all-time. | G9 LOW STILL-PRESENT | Add `hours` param for parity. |
| utils/monitoring/feedback.py | 186-187 | LOW | error-handling | Callback failure logged via `.error("...: %s", e)` not `.exception(...)` → no stack trace. | G9 LOW STILL-PRESENT | Use `self.logger.exception`. |
| utils/monitoring/feedback.py | 117-119 | LOW | minor | `from collections import deque` inside `__init__`; top-of-file already imports `OrderedDict`. | G9 LOW STILL-PRESENT | Merge imports. |
| utils/monitoring/audit_log.py | 129-152 | LOW | data-corruption | `d["target_type"] = target_type` overwrites a caller-supplied `target_type` already inside `details` JSON with no warning. | G9 MEDIUM (downgraded; narrow) | Log a warning when overwriting an existing key. |
| utils/monitoring/audit_log.py | 151 | LOW | observability | Successful DB audit write logged at DEBUG (`logger.debug`); failures at exception level. For audits, success should be INFO so ops can confirm commits. | G9 LOW STILL-PRESENT | Raise success log to INFO. |
| utils/monitoring/audit_log.py | 177-208 | LOW | fallback-read-gap | `get_recent_actions` returns `[]` when `DB_AVAILABLE` is False and **never reads** the JSONL fallback — entries written during a DB outage are invisible to readers. | new (paired with H30 fix) | Read + merge `audit_fallback.jsonl` when DB is unavailable. |
| utils/monitoring/health_client.py | 19-37 | LOW | env-collision / port | `_normalize_service_host` collapses `::1`/`[::1]` to `localhost` (breaks on Windows hosts files lacking IPv6 loopback); the `GO_HEALTH_API_PORT or HEALTH_API_PORT (if !=8080)` fallback chain is confusing and shares an env var with the Python health server. | G9 LOW/MEDIUM STILL-PRESENT | Canonicalize loopback to `127.0.0.1`; rename Python's var or document. |
| utils/monitoring/health_client.py | 257-287 | LOW | data-loss | On flush failure only `BUFFER_CAP - len(buffer)` of the failed batch is re-queued; the rest are dropped silently. Acceptable for metrics. | G9 MEDIUM (metrics, acceptable) | Document drop policy. |
| utils/monitoring/health_client.py | 199-209 | LOW | semantics | `is_ready()` returns `True` ("assume ready") when the Go service is unavailable — a readiness check that returns ready when it can't check could mask outages for callers that trust it. | new (minor) | Return a tri-state or `False`/`unknown` when unchecked. |
| utils/monitoring/tracing.py | 44-49 | LOW | standards-interop | Uses non-standard `X-Trace-ID` header instead of W3C `traceparent`; OTel/Jaeger/Tempo-instrumented Go/Rust services won't correlate automatically. | G9 LOW STILL-PRESENT | Emit `traceparent` (or both). |
| utils/monitoring/tracing.py | 1-49 | LOW | misnomer | Module named "tracing" but only propagates a trace ID — no spans/parent-child/timing. | G9 MEDIUM (downgraded; design) | Rename to correlation-id, or adopt `opentelemetry-api`. |
| utils/monitoring/logger.py | 363-381 | LOW | scope-creep | `cleanup_cache()` deletes audio files from `temp/` — unrelated to logging. | G9 LOW STILL-PRESENT | Relocate to `utils/media/`. |
| utils/monitoring/logger.py | 324-333 | LOW | rotation-sizing | `RotatingFileHandler(5MB × backupCount=5)` not env-configurable; a busy bot rotates within hours. | G9 LOW STILL-PRESENT | Make size/backup env-driven. |
| (cross-cutting) | — | INFO | datetime consistency | Mixed UTC/local across the package (health_api now UTC for ISO timestamps — improved; logger.JSONLogFormatter now UTC — improved; structured_logger human formatter, performance_tracker buckets, token_tracker buckets still local). | G9 theme (partially improved) | Standardize on UTC for all machine-read timestamps. |

---

## Cross-cutting notes

### Dependency currency (verified 2026-05-24)
- **aiohttp 3.13.5** (requirements.txt:8) — current latest; includes the fixes for the 2026 CVE batch (CVE-2026-22815, -34516, -34519, -34520, -34525). No action.
- **sentry-sdk 2.59.0** (requirements.txt:43) — 2.x; one minor behind 2.60.0. All APIs used (`new_scope`, `before_send`, `before_breadcrumb`, `include_local_variables`, `get_isolation_scope`) are valid 2.x. No security action; optional bump to 2.60.x.
- **prometheus-client 0.25.0** (requirements.txt:44) — recent; supports `start_http_server(addr=...)`. No action.

### Token tracker (H33) — current reality
Two `TokenTracker` classes still coexist, but the *double-counting* of the prior audit no longer occurs because **neither in-memory `record` path is wired into the live AI request flow** (`record_token_usage` imported but never called; cache `record_usage` not called from the response path). The cache tracker is populated only by `init_from_db()` replay and read by debug commands. The monitoring tracker (`utils/monitoring/token_tracker.py`) is dead. Recommend: identify the actual recording site (or confirm it's DB-trigger/SQL-insert based at cache/token_tracker.py:460), wire one tracker, delete the other.

### Audit log (H30 fixed / H31 open)
H30's data-loss is genuinely fixed via the JSONL fallback. H31 (tamper resistance) remains the top monitoring-subsystem risk: no hash chain, no immutability trigger, local-time `created_at`, and the new fallback file is itself unprotected. The read path also ignores the fallback file (new LOW).

### Confirmation
All 13 assigned files read in full (line counts: 1268, 668, 427, 362, 342, 313, 306, 298, 258, 383, 382, 50, 17). Cross-refs read: cogs/ai_core/cache/token_tracker.py, cogs/ai_core/imports.py, cogs/ai_core/ai_cog.py, cogs/ai_core/commands/debug_commands.py, utils/database/database.py (audit_log schema 594-615), requirements.txt. Prior audit G9_monitoring.md + MASTER_TABLE.md (H30–H33) cross-checked and dispositioned above.
