# G2 RE-AUDIT — AI Cog & Logic (2026-05-24)

## Scope
Read in full (entire current working-tree content, not diff):

| File | Lines read |
|---|---|
| `cogs/ai_core/ai_cog.py` | 1791 / 1791 |
| `cogs/ai_core/logic.py` | 1634 / 1634 |
| **Total** | **3425** |

Cross-referenced (read selectively to verify symbols/behaviour, not audited in full): `core/message_queue.py`, `core/performance.py`, `session_mixin.py`, `api/api_failover.py`, `data/constants.py`, `data/constants_env.py`, `api/discord_chat_claude_cli.py` (cancel/partial-text path), `bot.py` (process_commands wiring).

Verified facts:
- `CLAUDE_MODEL` defaults to `claude-opus-4-7` (constants_env.py:39). **Confirmed valid current model ID** (Anthropic Claude Opus 4.7, released 2026-04-15, 1M context, model id `claude-opus-4-7`). Not stale.
- `LOCK_TIMEOUT = 180.0` s (constants.py:54) — the prior audit assumed 30 s; comments in logic.py that mention this are accurate to 180 s.
- Default `CLAUDE_BACKEND` is `"cli"` (constants_env / api_failover / setup_ai all default to `cli`), so the bot's primary runtime path is the CLI subprocess, not the SDK.
- `bot.py:727` calls `process_commands`; the cog's `on_message` is an additive listener (no double command execution).

## Severity summary
| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 2 |
| MEDIUM | 11 |
| LOW | 30 |
| INFO | 9 |
| **Total** | **52** |

Prior-audit cross-check: the 2026-05 G2 report used a flat table (no C#/H# IDs were assigned to these two files; MASTER_TABLE records G2 as 0 CRIT / 0 HIGH / 17 MED / ~100 LOW-INFO). All prior line numbers predate the current file sizes (ai_cog.py 1640→1791, logic.py 1379→1634) and were re-mapped. Prior column cites the prior report's line anchor where applicable.

---

## Findings

| File | Line(s) | Severity | Category | Issue | Prior | Suggested fix |
|---|---|---|---|---|---|---|
| logic.py | 270, 794-797 vs message_queue.py 41 | HIGH | Concurrency | **Two independent `processing_locks` dicts exist for the same channels.** `ChatManager.processing_locks` (logic.py:270) is the dict actually used for mutual exclusion in `process_chat` (794-797) and `get_chat_session`. `MessageQueue.processing_locks` (message_queue.py:41) is a *separate* dict mutated by `queue_message` eviction (162-166), `clear_channel` (461), `cleanup_unused_locks` (437), `release_lock` (343). The MessageQueue's lock-management API (`get_lock`, `acquire_lock_with_timeout`, `release_lock`, `is_locked`) is **never called by the live chat path** (only README references it). Net effect: the queue's `MAX_CHANNELS`-driven eviction of a "locked" channel inspects the *wrong* lock dict, so it can evict a channel that `process_chat` is actively serializing on (its real lock lives in ChatManager.processing_locks). The guard comment in `queue_message` (131-152) that claims it avoids "orphaning the lock held by the in-flight processor" is therefore ineffective — the in-flight processor's lock is in the other dict. | prior point 1 (partial) | Make ChatManager use `_message_queue.get_lock()/acquire_lock_with_timeout()/release_lock()` as the single source of truth for per-channel locks, or delete the unused MessageQueue lock dict + API entirely and document that ChatManager.processing_locks is canonical. |
| logic.py | 1276-1311 | HIGH | Correctness / data integrity | **Partial streamed response is persisted to history on cancellation.** When a new message arrives mid-stream, `is_cancelled` becomes true (1275); the handler then saves `model_text` to history (1298-1305) and raises `_NewMessageInterrupt`. In streaming/CLI mode `model_text` is whatever `accumulated_text` had streamed so far (discord_chat_claude_cli.py:336-344 returns partial text on cancel). A truncated/mid-sentence assistant turn is written to `chat_data["history"]` and persisted via `save_history`, poisoning all future turns (the model "sees" a reply it never finished). The non-cancelled save path (1334-1341) has the same shape but is correct there. | logic.py:1060-1098 (MED) STILL-PRESENT | Do not persist the model turn when `was_cancelled` and the response was produced by a streaming path; or tag it `{"partial": true}` and exclude partial turns from the context builder. Only the user message needs saving on cancel. |
| ai_cog.py | 431-471 | MEDIUM | Correctness | **`reset_ai` does not apply the RP_COMMAND→RP_OUTPUT redirect** that `toggle_thinking_cmd` (915-917), `toggle_streaming_cmd` (959-961) and `resend` (1150-1153) all apply. In the RP guild, all chat state is keyed by `CHANNEL_ID_RP_OUTPUT`, but `reset_ai` wipes `ctx.channel.id` (= RP_COMMAND when run there). Result: in-memory + on-disk history for the real session (RP_OUTPUT) is untouched; only no-op pops happen on RP_COMMAND. The owner believes memory was cleared but the next RP reply still uses stale memory. | prior ai_cog.py:385-410 (MED) STILL-PRESENT | Compute `target_channel_id` with the same RP redirect block used by the sibling commands and operate on it (memory pops, `delete_history`, `clear_channel`, CLI `reset_channel_session`). |
| logic.py | 1135-1177 | MEDIUM | Correctness / data loss | **History replay silently drops image/inline-data parts and can collapse role alternation.** The per-item loop only materializes `str` and `{"text": ...}` parts; `{"inline_data"|"image_url"|"source"}` parts set `had_image_only` and emit a `[image]` placeholder *only if* there were no text parts (1170-1174). A turn that had BOTH text and images replays fine, but a turn that was image-only between two same-role turns is fine, while images attached to a text turn are dropped from context after restart. More importantly, the current-turn build path (1079-1087) appends video parts using the **Gemini-era `{"inline_data": {...}}`** shape, not Anthropic's `{"type":"image","source":{...}}`. If `api_handler`/CLI doesn't translate `inline_data`, video frames are silently dropped from the API call. | prior point 6 + logic.py:884-893 (LOW) STILL-PRESENT — needs verification of api_handler translation | Verify `api_handler.py`/CLI converts `inline_data` to Anthropic `source` blocks; if not, emit the correct block shape here. Persist image parts in a replayable form. |
| logic.py | 1496-1504, 1246-1249 | MEDIUM | Correctness | **Multi-character webhook sends are not chunked to Discord's 2000-char limit**, unlike the `resend` command which *does* chunk (ai_cog.py:1246-1249). `char_msg` is passed whole to `send_as_webhook` (1497). A character block >2000 chars triggers a Discord 400 and that character's line is lost (the broad `except Exception` at 1585 then shows a generic error, dropping the rest of the multi-character response). | prior logic.py:1281-1283 (MED) STILL-PRESENT | Chunk `char_msg` into ≤2000-char slices in a loop (mirror ai_cog.py:1246-1249) before calling `send_as_webhook`. |
| ai_cog.py | 740-772 | MEDIUM | Logic / authz-ordering | **DM voice commands execute before any rate-limit / validation gate.** In `_handle_dm_message`, `parse_voice_command` runs on every owner DM and `join_voice_channel` / disconnect-all execute (752-772) *before* the `check_rate_limit` calls at 774-781. Owner-only limits blast radius, but a misparsed sentence can still trigger a real voice connect/disconnect with no rate limiting, and the join path is reachable on every message. | new | Move the rate-limit gate above the voice-command dispatch, or rate-limit the voice actions separately. |
| logic.py | 1100-1126 | MEDIUM | Concurrency / latency | **Synchronous 60-second blocking compression on the channel lock.** When `len(history) > MAX_HISTORY_ITEMS+500` (2500), `await asyncio.wait_for(summarizer.compress_history(...), timeout=60)` runs inside the held channel lock on the user-facing turn. A slow summarizer monopolizes the channel for up to 60 s, during which new messages queue and the user sees nothing. Also the window 2001–2500 items is trimmed (1120-1121) without ever compressing, so the compression branch's intent (compress-then-trim) is partially defeated. | prior logic.py:905-914 + 908-914 (MED) STILL-PRESENT | Run compression as a between-turns background task that updates `chat_data["history"]`; or reduce the inline timeout drastically. Reconcile the compress (2500) vs trim (2000) thresholds. |
| logic.py | 1358-1381 | MEDIUM | Concurrency | **`history_manager.smart_trim` operates on the live list, not a snapshot.** `chat_data["history"]` is passed by reference to the async `smart_trim` (1361-1363); a concurrent `_process_pending_messages`→`process_chat` append during the await would be lost when `chat_data["history"] = trimmed` overwrites it (1365). Same reference-aliasing risk applies to the consolidation task (1404-1407) and `state_tracker.update_from_response` (1389-1391). | prior logic.py:1175-1188 + 1209-1230 (MED) STILL-PRESENT | Snapshot with `list(chat_data["history"])` before passing to async trim/consolidation. |
| logic.py | 288-292 | MEDIUM | Concurrency / encapsulation | **Legacy alias dicts share identity with MessageQueue internals and bypass its lock.** `self.pending_messages = self._message_queue.pending_messages`, `self.cancel_flags = self._message_queue.cancel_flags`, `self._lock_times = self._message_queue._lock_times` (288-291) are the *same* dict objects. `session_mixin.cleanup_inactive_sessions` (259-262) pops these directly without holding `MessageQueue._queue_lock` (threading.Lock), racing the queue's own mutations from `queue_message`/`pop_pending_messages`. While asyncio is single-threaded, `_queue_lock` is a `threading.Lock` implying cross-thread use is intended somewhere; the direct pops defeat it. | prior point 1 (MED) STILL-PRESENT | Route all access through MessageQueue public methods (`clear_channel`, etc.); drop the aliasing or make them read-only properties. |
| ai_cog.py | 549-660 | MEDIUM | Concurrency / DoS | **Webhook verification has no in-flight de-dup; concurrent misses all hit `webhook_channel.webhooks()`.** Between the cache `get` (576) and the write (654), N concurrent proxy messages for the same `webhook_id` each call the rate-limited `webhooks()` API (582). Under a burst this can exhaust Discord's webhook-list rate limit for the channel. The eviction block (642-657) also reads `len()`/sorts/pops without a lock — two coroutines can both evict, transiently shrinking the cache below the ceiling. | prior ai_cog.py:486-548 + 519 (MED) STILL-PRESENT | Add an in-flight `dict[int, asyncio.Future]` so concurrent verifies for the same webhook await one API call; guard the cache mutation with an `asyncio.Lock`. |
| logic.py | 1577-1591 | MEDIUM | Error handling | **Generic error message hides Anthropic/CLI failures, and the broad `except Exception` returns to the pending-drain finally without distinguishing retryable vs fatal.** The narrow tuple `(discord.HTTPException, ValueError, TypeError)` (1577) logs `"Gemini Error"` (stale label) and the broad `except Exception` (1585) catches everything else (now including `anthropic.APIError` family — so the prior "listener crash" concern is FIXED). But both paths emit the same opaque Thai string with no trace id, so an operator cannot tell a 529 overload from a code bug, and the user cannot tell transient from permanent. | prior logic.py:1340-1347 (MED) **PARTIALLY-FIXED** (no longer crashes; still opaque + mislabeled) | Branch on `anthropic.APIStatusError`/`RateLimitError`/`APITimeoutError` for a "try again" message; relabel "Gemini Error" → "Claude API error"; include a short correlation id. |
| ai_cog.py | 86, 742, 1096-1099, 1346-1349 | LOW | Authz | Owner checks are split between `@commands.is_owner()` (most commands) and manual `if author.id != self.OWNER_ID` (`_handle_dm_message`:742, `link_memory`/`move_memory` confirm closures:1096,1346). `OWNER_ID = CREATOR_ID` (env) can disagree with discord.py's `is_owner()` (application owner / `owner_ids`). Multi-owner setups behave inconsistently. | prior ai_cog.py:81 + point 3 (INFO) STILL-PRESENT | Standardize on one mechanism; if env-driven, document that `owner_ids` is ignored. |
| ai_cog.py | 388-402 | LOW | Correctness | In `chat_command`, the reply-fetch `await ctx.channel.fetch_message(...)` (392) runs *before* the `_as_chat_channel` narrowing (410). `ctx.channel` may be a non-text channel type where `fetch_message` behaves unexpectedly; narrow first. | prior ai_cog.py:341-360 (LOW) STILL-PRESENT | Narrow the channel type before fetching the referenced message. |
| ai_cog.py | 424-429, 474-479 | LOW | Error handling | `chat_command_error` / `reset_ai_error` re-`raise error` for the non-handled branch. Re-raising inside a command error handler double-dispatches to discord.py's global handler. | prior ai_cog.py:376-381,413-418 (LOW) STILL-PRESENT | Log + swallow, or convert to `commands.CommandError`; don't bare re-raise. |
| ai_cog.py | 505-527 | LOW | Error handling | `_resolve_prefix_tuple` swallows all callable-prefix exceptions and silently returns `("!",)` (522-523). If the deployment prefix is not `!`, a transient failure in the prefix callable makes both the command detector (887) and the DM command-skip (747) use the wrong prefix, so a real-prefixed message can be routed to AI as chat. | prior ai_cog.py:442-464 + 615-676 (MED, downgraded) STILL-PRESENT | Log the exception at warning; cache last-known-good prefix instead of hardcoding `!`. |
| ai_cog.py | 603-610 | LOW | Security | Webhook trust accepts `creator_id` (`wh.user.id` when `wh.user.bot`) OR `application_id` in the allowlist. A server admin who manually creates a webhook whose creator happens to be an allow-listed bot account is unusual, but the OR means `application_id` (the trustworthy field) is not *required*. Hardcoded IDs (94-99) have no test guarding against the proxy bots being re-issued. | prior ai_cog.py:89-93 (INFO) STILL-PRESENT | Prefer `application_id` as the sole primary signal; add a test asserting the two IDs. |
| ai_cog.py | 713-716 | LOW | Quality | Rate-limit bucket names `gemini_api` / `gemini_global` (also 774-781, 815-839, 892-895) are stale Gemini-era labels for a Claude bot. Comment acknowledges intent but ops/log readers are misled. | prior point 9 (INFO) STILL-PRESENT | Document in a single constant, or migrate bucket keys with a compatibility shim. |
| ai_cog.py | 533-535 | LOW | Correctness | `if message.webhook_id:` treats `0` as falsy. Discord never issues snowflake 0, so pedantic, but `is not None` is the correct intent. | prior ai_cog.py:470-471 (LOW) STILL-PRESENT | Use `if message.webhook_id is not None:`. |
| ai_cog.py | 864-879 | LOW | Correctness | Reply detection in `_handle_guild_message` uses `_as_fetchable_channel` (867) which returns `None` for `DMChannel` — but this method is only reached for guild messages (`on_message` routes DMs to `_handle_dm_message`), so DM reply-loss is not triggered here. The prior audit's "DMs lose reply detection" claim does not apply on this path; DMs never run reply detection at all (no equivalent in `_handle_dm_message`). | prior ai_cog.py:736-751 (MED) **PARTIALLY-FIXED / re-scoped** — guild path correct; DM has no reply detection by design | If DM reply-as-input is desired, add it to `_handle_dm_message`; otherwise document. |
| ai_cog.py | 878 | LOW | Error handling | `except (discord.NotFound, discord.HTTPException, TypeError): pass` catches `TypeError` on `fetch_message` — unusual; undocumented why. | prior ai_cog.py:750 (LOW) STILL-PRESENT | Remove `TypeError` or comment the reason. |
| ai_cog.py | 1067-1072, 1314-1320 | LOW | Correctness | `int(source_channel.strip("<>#"))` strips the chars from both ends, so `"##123>><<"` also parses to `123`. The `<@` guard (1061/1309) blocks user/role mentions; channel-mention shape is reliable, so low impact. | prior ai_cog.py:936 (LOW) STILL-PRESENT | Parse with a `<#(\d+)>`-or-`\d+` regex instead of `strip`. |
| ai_cog.py | 1102-1109, 1352-1359 | LOW | Concurrency | `wait_for("message", check=...)` confirm prompts only honor singular `OWNER_ID`; if the user double-sends `yes`, extra confirmations are dropped silently (no harm) but a second pending `move`/`link` cannot be confirmed by a co-owner. | prior ai_cog.py:962-968,1209-1213 (LOW) STILL-PRESENT | Accept `bot.is_owner()` in the check. |
| ai_cog.py | 1246-1250 | LOW | Performance | Fixed `await asyncio.sleep(0.5)` between every webhook chunk in `resend`. For long multi-character resends this serializes slowly; the sleep is inside the chunk loop so even a single long message sleeps between chunks. | prior ai_cog.py:1109-1112,1112 (LOW) STILL-PRESENT | Use a token bucket tuned to Discord webhook limits (5 / 2 s). |
| ai_cog.py | 1576-1578 | LOW | Quality | `auto_summarize_cmd` reads `chat_data = self.chat_manager.chats.get(channel_id)` directly (not `get_chat_session`), so it only works if the session is already resident in RAM; a channel with on-disk history but evicted from memory reports "No active session". | prior ai_cog.py:1430-1493 (MED, downgraded) STILL-PRESENT | Use `await get_chat_session(channel_id)` to lazily load history. |
| ai_cog.py | 1666 | LOW | Validation | `channel_ratelimit_cmd` passes `limit` to `set_channel_limit` with no lower-bound check; a negative value is forwarded unchecked. | prior ai_cog.py:1495-1520 (LOW) STILL-PRESENT | Validate `limit >= 0` (and an upper bound). |
| ai_cog.py | 1746-1777 | LOW | Quality | `unrestricted_mode_cmd` ignores the return value of `set_unrestricted` and always reports success (the early `GUARDRAILS_AVAILABLE` guard mitigates the stub-returns-False case). | prior ai_cog.py:1522-1627 (LOW) STILL-PRESENT | Check the return and surface failure. |
| ai_cog.py | 1780-1791 | LOW | Resource | `setup` adds three cogs sequentially with no rollback; if `AIDebug`/`MemoryCommands` fails, `AI` stays half-attached. | prior ai_cog.py:1630-1640 (LOW) STILL-PRESENT | Wrap in try/except and `remove_cog` on failure. |
| ai_cog.py | 157-166, 238-243 | LOW | Concurrency | `cog_load` cancels the 3 named tasks on reload but does **not** cancel/clear `_bg_tasks` (token_tracker init) before spawning a new one — a reload mid-init can double-fire `init_from_db`. `cog_unload` does cancel `_bg_tasks`, but reload calls `cog_load` of the *new* instance without the old instance's unload necessarily completing first. | prior ai_cog.py:140 (LOW) STILL-PRESENT | Cancel `_bg_tasks` at the top of `cog_load` too, or guard `init_from_db` against concurrent runs. |
| ai_cog.py | 318-348 | LOW | Error handling | `_cleanup_pending_requests_loop` re-imports `state_tracker`/`consolidator`/`message_queue` on every 30-min iteration and catches all errors as non-critical debug; a persistent failure floods nothing (debug) but also hides a broken cleanup indefinitely. | prior ai_cog.py:255-300,305 (LOW) STILL-PRESENT | Hoist imports to module top; escalate repeated failures. |
| ai_cog.py | 784-792 | LOW | Quality | `_handle_dm_message` creates a `trace_id`, logs it once, and never propagates it into `process_chat`; the trace is a dead single line. | prior ai_cog.py:660-664 (LOW) STILL-PRESENT | Thread via contextvar or drop. |
| ai_cog.py | 1119, 1369-1372 | LOW | Concurrency | `link_memory`/`move_memory` now use `self.chat_manager.chats.pop(target_id, None)` (FIXED — prior used `del`). Remaining nit: source/target reload relies on next-access lazy load; no explicit re-warm. | prior ai_cog.py:985-1005,1214-1217 + point 5 (LOW) **FIXED-VERIFIED** (pop instead of del) | None required. |
| logic.py | 58-66 | LOW | Quality | `_utc_now_iso` now correctly returns UTC (docstring matches), resolving the prior "misnamed, returns Bangkok" finding. | prior logic.py:43-51 + 26 (LOW/INFO) **FIXED-VERIFIED** | None. |
| logic.py | 792-797 | LOW | Concurrency | Lock creation now uses check-then-set (not `setdefault(.., asyncio.Lock())`), eliminating the per-call throwaway-Lock allocation the prior audit flagged. Residual: the `if lock.locked()` check (800) → `await wait_for(lock.acquire())` (829) window can still see a release+reacquire by another coroutine (benign under cooperative scheduling). | prior logic.py:610 + 613-629 (MED) **FIXED-VERIFIED** (setdefault churn gone); residual race LOW | None critical. |
| logic.py | 686-740 | LOW | Concurrency | `_process_pending_messages` is now a bounded `while iteration < 16` loop (no longer recursive via `process_chat`), fixing the prior unbounded-recursion concern. Residual: the 16-iteration cap silently bails with a warning if a burst keeps repopulating the queue, stranding messages until the next turn. | prior logic.py:548-568,1378-1379 + point at 1378 (MED) **FIXED-VERIFIED** (recursion→loop); residual LOW | Consider re-queue-and-reschedule instead of bail. |
| logic.py | 1611-1617 | LOW | Concurrency | Outer `finally` releases the lock with `if lock_acquired and lock.locked(): lock.release()`. `asyncio.Lock` is not owner-aware; if another coroutine acquired it after release elsewhere, this would mis-release. In practice `lock_acquired` + single-threaded scheduling makes this safe, but the `lock.locked()` check is redundant with the `try/except RuntimeError`. | prior logic.py:1367-1373,1370 (LOW) STILL-PRESENT | Drop the `lock.locked()` check; rely on try/except, or track ownership. |
| logic.py | 774 | LOW | Correctness | Dedup key (`generate_key(channel_id, user.id, message)`) excludes attachments. "hello"+imgA then "hello"+imgB in quick succession dedups the second, dropping imgB. | prior logic.py:596 (LOW) STILL-PRESENT | Fold an attachment fingerprint (count + filenames/sizes) into the key. |
| logic.py | 780-785 | LOW | Correctness | Circuit-breaker check (780) now runs *before* the lock but *after* dedup (775). A user retrying during an OPEN circuit may get "duplicate request blocked" instead of the "AI resting" message because the dedup key from the first attempt is still within TTL. | prior logic.py:602-606 (MED, downgraded) STILL-PRESENT | Move the circuit-breaker check above the dedup add, or remove the dedup key when the circuit is open before returning. |
| logic.py | 1220 + constants.py 129-160 | LOW | Correctness | `force_search = any(kw in msg_lower for kw in GAME_SEARCH_KEYWORDS)` does naive substring matching. Keywords include `"00"`, `"000"`, `"ego"`, `"skill"`, `"stats"`, `"identity"` — `"00"` matches "1000", "00:00", any price/timestamp; `"ego"` matches "category"/"ego" inside words. Forces an unnecessary web search and metrics mislabel on innocuous messages. | prior logic.py:1011 (LOW) STILL-PRESENT | Match on word boundaries (`\b…\b`) and drop ambiguous tokens like `00`/`000`/`☆`. |
| logic.py | 936 | LOW | Correctness | Comment says "Search global memories + channel specific" but `rag_system.search_memory(display_message, limit=3)` omits `channel_id`. Either the comment is stale or channel scoping is missing. | prior logic.py:750 (LOW) STILL-PRESENT | Pass `channel_id` or fix the comment. |
| logic.py | 942-943 | LOW | Error handling | RAG search wrapped in `except OSError` only; `rag_system.search_memory` can raise `ValueError`/`KeyError`/FAISS `RuntimeError`/`TimeoutError`. A non-OSError escapes into the outer broad handler and shows a user error for what should degrade silently. | prior logic.py:745-757 (LOW) STILL-PRESENT | Broaden to the realistic exception set or `except Exception` with debug log. |
| logic.py | 949 vs 199 | LOW | Quality | `re.findall(r"\{\{([^}]+)\}\}", display_message)` uses an inline pattern instead of the precompiled `PATTERN_CHARACTER_TAG` (199), and the two differ (`[^}]+` vs `(.+?)`). | prior logic.py:763 (LOW) STILL-PRESENT | Reuse one precompiled pattern. |
| logic.py | 888-889 | LOW | Type | `has_attachments = attachments and len(attachments) > 0` — `attachments` may be `None`; relies on short-circuit. Type checkers flag `len(None)`. | prior logic.py:702 (LOW) STILL-PRESENT | `has_attachments = bool(attachments)`. |
| logic.py | 1059-1066 | LOW | Resource | PIL images appended to `content_parts` are closed inline in the build loop (1064-1066, 1071-1072) and again in the `finally` (1596-1607). Double-close is safe on PIL but wasteful; the inline `finally: part.close()` also closes the image *before* `_pil_to_inline_data` failure handling could retry. | prior logic.py:864-878,1349-1363 (LOW) STILL-PRESENT | Close once (in the outer finally) or track closed state. |
| logic.py | 1133 | LOW | Performance | `from .api.dashboard_common import normalize_timestamp_to_bangkok as _norm_ts` is a per-call import inside `process_chat` on the hot path. | prior logic.py:939 (LOW) STILL-PRESENT | Hoist to module top. |
| logic.py | 1135-1176 | LOW | Performance | History serialization rebuilds every item each turn (regex `PATTERN_ID.sub` + `_norm_ts` per part) — O(history) per request, up to 2000 items. | prior logic.py:941-967,1191-1200 (LOW) STILL-PRESENT | Cache converted parts on the history item; invalidate on append. |
| logic.py | 1144-1145 | LOW | Robustness | `if ts_raw: ts_prefix = f"[{_norm_ts(ts_raw)}] "` with no try; relies on `normalize_timestamp_to_bangkok` being total on malformed input. | prior logic.py:950-951 (LOW) needs verification | Confirm `_norm_ts` returns a safe value (or "") for bad input. |
| logic.py | 1438 | LOW | Correctness | `_is_valid, sanitized, warnings = validate_response_for_channel(...)` ignores `_is_valid`; if the validator can hard-block, the block signal is dropped and sanitized text is sent anyway. | prior logic.py:1246-1250 (LOW) STILL-PRESENT | Honor `_is_valid` (suppress send or substitute message) when False. |
| logic.py | 1507-1513, 1560-1568 | LOW | Correctness | When a response is split across multiple messages/webhooks, only the **last** sent message id is recorded to the history item's `message_id`. Edit/track operations then only target the final chunk. | prior logic.py:1292-1298,1318-1319 (LOW) STILL-PRESENT | Store a list of message ids, or record the first chunk for reply anchoring. |
| logic.py | 1525-1556 | LOW | Correctness | Smart-split fallback `split_at = 2000` can split inside a markdown code fence / bold span, breaking formatting across the boundary (the Thai-combining handling is correct). | prior logic.py:1303-1321 (LOW) STILL-PRESENT | Prefer splitting at fence/paragraph boundaries; balance code fences across chunks. |
| logic.py | 155-170, 1343-1349 | INFO | Dead code / design | Tool-use roundtrip is intentionally unwired; `_function_calls` is always `[]`. Well-documented. Confirms the prior "sequential tool execution blocks" finding is moot (no tool loop exists now). | prior logic.py:1131-1166 (MED) **FIXED-VERIFIED / N-A** (block removed) | None; re-evaluate if tool use is re-added. |
| logic.py | 5, 247, 571, 608, 1582 | INFO | Quality | Pervasive "Gemini" naming on a Claude codebase: module docstring "Gemini API integration" (5), class docstring "interactions with the Gemini API" (247), method names `_call_gemini_api`/`_call_gemini_api_streaming` (571,608), log "Gemini Error" (1582). | prior point 9 (INFO) STILL-PRESENT | Rename for clarity in a dedicated pass. |
| logic.py | 296-381 | INFO | Concurrency | `_enforce_channel_limit` computes `evict_count` before the locked-channel skip (314-319), so under heavy lock contention fewer than `evict_count` channels evict and `len(self.chats)` can stay above `MAX_CHANNELS`. Mitigated because it's retried on every `get_chat_session`. | prior logic.py:229-307,247-250 (MED, downgraded) STILL-PRESENT | Iterate until `evict_count` unlocked channels are collected, or recompute against the unlocked subset. |
| logic.py | 460-462 | INFO | Error handling | Legacy SDK init catches `(ValueError, OSError)`; real auth failures surface as `anthropic.APIError` on first call, not at construction — the catch is largely cosmetic. Low impact since default backend is CLI. | prior logic.py:354 (LOW, downgraded) STILL-PRESENT | Catch `anthropic.AnthropicError` or document. |
| logic.py | 419-432 | INFO | Concurrency | `setup_ai`'s failover branch accesses `api_failover._initialized` (private) and `active_endpoint.value` after only checking `active_config`. Verified `initialize()` is env-read-only (no blocking I/O), so the prior "blocks event loop in `__init__`" concern is **not** a real defect. `active_endpoint` is always a valid enum (defaults to DIRECT), so the None-deref concern doesn't apply. | prior logic.py:227,309-356,118 (MED, downgraded) **PARTIALLY-FIXED / re-scoped** | Optionally expose a public `is_initialized`. |
| ai_cog.py | 1530-1574 | INFO | Resource | `audit_export_cmd` now builds the JSON in an in-memory `BytesIO` (no temp file), resolving the prior relative-`temp/` path and unlink-race findings. `days` is bounded 1–365. | prior ai_cog.py:1379-1428,1404-1422 (MED/LOW) **FIXED-VERIFIED** | None. |

---

## Cross-cutting notes

1. **Lock-state duplication (the HIGH at logic.py:270)** is the most consequential structural issue: two `processing_locks` dicts, only one of which the live path uses, while the MessageQueue eviction logic reasons about the other. Consolidate to one lock source.

2. **Default backend is CLI.** Many SDK-path concerns (legacy init exception catch, failover private-flag access) are low-impact in the default `CLAUDE_BACKEND=cli` deployment but become live if an operator switches to the SDK backend.

3. **Prior MEDIUMs now resolved (verified):** recursion→loop in `_process_pending_messages`; `setdefault` lock churn → check-then-set; `del`→`pop` in link/move memory; `_utc_now_iso` naming; audit-export temp-file → BytesIO; tool-execution block removed; broad `except Exception` in `process_chat` now prevents listener crash on Anthropic errors (downgraded to "opaque message" LOW/MED).

4. **Prior MEDIUMs still present (re-mapped):** `reset_ai` RP redirect; partial-response persistence on cancel; multi-character webhook chunking; inline-data/history image replay; webhook-verify concurrency; live-list trim/consolidation snapshots; legacy-alias dict coupling; inline blocking compression.

5. **Model/version currency:** `claude-opus-4-7` is current (verified via Anthropic docs, released 2026-04-15). No deprecated discord.py 2.7.x or anthropic SDK API usage observed in these two files. No SQL/OS-command injection, no subprocess use, no secret leakage in logs in the two assigned files (CLI subprocess env-leak risk lives in `discord_chat_claude_cli.py`, out of scope here — see prior H1/H2).

## Confirmation
Both assigned files read in full, every line: **yes** (ai_cog.py 1791/1791, logic.py 1634/1634).

Sources for currency claims:
- [Claude API — Models overview](https://platform.claude.com/docs/en/about-claude/models/overview)
- [Claude API — Model IDs and versioning](https://platform.claude.com/docs/en/about-claude/models/model-ids-and-versions)
- [What's new in Claude Opus 4.7](https://platform.claude.com/docs/en/about-claude/models/whats-new-claude-4-7)
