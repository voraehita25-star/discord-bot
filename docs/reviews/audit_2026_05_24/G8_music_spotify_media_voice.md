# G8 Re-Audit — Music / Spotify / Media / Voice

Audit date: 2026-05-24
Auditor: G8 (re-audit; cross-checks prior `docs/reviews/audit_2026_05/` — G7_music_spotify.md, G5, G1, MASTER_TABLE C2/H13/H19-H24)
Method: READ-ONLY. Every line of every assigned file read in full (working-tree content, not diff). Cross-referenced `utils/database/database.py` (music_queue methods 1888-1937), `utils/media/ytdl_source.py`, `cogs/music/url_safety.py`.

## Scope (files read in full)

| File | Lines | Purpose |
|---|---|---|
| `cogs/music/cog.py` | 2604 | Music god-cog: playback, queue cmds, voice mgmt, persistence, owner help |
| `cogs/music/queue.py` | 378 | `QueueManager` (alternate, unused by cog) — DB/JSON persistence |
| `cogs/music/views.py` | 178 | `MusicControlView` discord.ui.View (4 buttons) |
| `cogs/music/utils.py` | 94 | `Colors`, `Emojis`, `format_duration`, `create_progress_bar` |
| `cogs/music/__init__.py` | 23 | Package exports + `setup()` |
| `cogs/spotify_handler.py` | 620 | Spotify track/playlist/album extraction, retry, circuit breaker |
| `cogs/ai_core/media_processor.py` | 717 | PIL image / GIF→MP4 / avatar / attachment handling |
| `cogs/ai_core/voice.py` | 206 | AI-driven voice join/leave + status |

## Severity-count summary

| Severity | Count |
|---|---|
| CRITICAL | 1 |
| HIGH | 4 |
| MEDIUM | 12 |
| LOW | 17 |
| INFO | 4 |
| **Total** | **38** |

Prior-audit verification headline: C2 (views on_timeout) FIXED-VERIFIED. H13 (MAX_IMAGE_PIXELS) FIXED-VERIFIED. H19 (play guild_only) FIXED-VERIFIED. H21 (yt-dlp in lock) FIXED-VERIFIED. H22 (periodic save scope) FIXED-VERIFIED. H23 (load_queue delete-before-write) FIXED-VERIFIED. H24 (FFmpeg leaks) FIXED-VERIFIED (all 4 play sites). **H20 (lock leak on cancel) STILL-PRESENT — re-raised to CRITICAL** (see C-1). One NEW high-sev persistence data-loss bug found (H-1, the `type` field is dropped on DB round-trip).

---

## Findings

| File | Line(s) | Severity | Category | Issue | Prior | Suggested fix |
|---|---|---|---|---|---|---|
| `cogs/music/cog.py` | 793, 817, 829-837 | CRITICAL | Concurrency / lock leak | **play_lock leaks permanently on outer-task cancellation.** `_acquire_task = create_task(_acquire_lock())` then `await asyncio.wait_for(asyncio.shield(_acquire_task), timeout=2.0)`. If `_play_next_once` is cancelled while awaiting (cog reload, bot shutdown, task GC, `cleanup_guild_data` racing), `wait_for` raises `CancelledError` but `shield` keeps `_acquire_task` running; it then acquires the lock. The done-callback `_release_if_timed_out` only releases when `_timed_out_flag[0]` is True (set ONLY in the `except TimeoutError` branch, line 834) or when the helper raised with lock held. On pure cancellation neither holds → lock acquired, no owner, **never released → permanent per-guild deadlock**: that guild can never play again until process restart. | G7 line 47 / MASTER H20 (marked fixed but NOT) — STILL-PRESENT | Add `except asyncio.CancelledError: _acquire_task.cancel(); with contextlib.suppress(...): await _acquire_task; raise`. Better: drop the shield/callback machinery and use `async with asyncio.timeout(2.0): async with lock:` (Py3.11+) which releases correctly on cancel. |
| `cogs/music/cog.py` (DB at `utils/database/database.py:1888-1923`) | 372, 420, 957-998; queue.py 205, 280 | HIGH | Queue persistence / data loss | **`type` field is dropped on DB save/load round-trip.** `db.save_music_queue` persists only `url, title, added_by` (database.py:1900); `load_music_queue` returns `{"url","title","added_by"}` (1923) — no `type`. Spotify enqueues store `{"url":"Artist - Track audio","title":...,"type":"search"}` (spotify_handler 363-365, 494-500, 587-593). After a restart these reload WITHOUT `type`, so `_play_next_once` line 975 `item.get("type")=="search"` is False and the raw search string is passed to `YTDLSource.from_url`, which rejects it (`urlparse` → empty scheme → `DownloadError`, ytdl_source 228-231). Caught at cog.py 1160, the track is silently skipped ("ข้ามไปเพลงถัดไป"). Net: **every Spotify-derived track in a persisted queue fails to play after restart.** | NEW (prior G7 H23 only covered JSON-delete ordering, not the column gap) | Add a `track_type` column to `music_queue` + migration; persist & restore `type`. Until then, store the resolved YouTube `webpage_url` for Spotify items at enqueue time (resolve via `search_source`) so the persisted `url` is always a real http(s) URL. |
| `cogs/music/cog.py` | 354-373 (`save_queue`) | HIGH | Race / persistence | **`Music.save_queue` has no per-guild lock** (unlike `QueueManager.save_queue` queue.py:179 which snapshots under `_get_lock`). It does `list(queue)` (line 372) with no lock while `_periodic_queue_save` (202), `cog_unload` (286), `cleanup_guild_data` (328), and command mutators all call it. A concurrent `popleft` in `_play_next_once` (957) or `append` in `play` (1706) during the `list()` materialisation can yield a torn/short snapshot that overwrites the DB. The cog deliberately abandoned `QueueManager`, losing its locking. | G7 line 34 / MASTER H22-adjacent — STILL-PRESENT | Snapshot `list(queue)` synchronously (no await before it — currently OK) AND guard the whole save with a per-guild save lock, or route persistence through `QueueManager` which already locks. |
| `cogs/ai_core/voice.py` | 174, 181 | HIGH | None / AttributeError (DM/crash) | **`get_voice_status` reads `music_cog.current_track` which no longer exists.** The Music cog was refactored to per-guild `MusicGuildState` (cog.py 44-62); `current_track` is now `gs.current_track`, NOT a `Music.current_track` dict. `music_cog.current_track.get(guild_id, {})` raises `AttributeError` whenever the bot is playing/paused and this status path runs (AI "voice status" query). The broad nothing catches it here — it propagates to the caller. | G1 line 165 (flagged MEDIUM, "if refactored") — REGRESSED: the refactor happened, now live. | Use `gs = music_cog._gs(guild_id); track_info = gs.current_track or {}` guarded by `getattr(music_cog, "_guild_states", None)`, or expose a public `get_current_track(guild_id)` on the cog. |
| `cogs/music/cog.py` | 740-765, 767-1201 | HIGH | Concurrency / starvation | **`play_next` retry budget is shared mutable state mutated outside the lock.** `play_retries` is read/written in the wrapper (758, 760, 765) WITHOUT holding `play_lock`, while `_play_next_once` also writes it (1064). Two concurrent `play_next(ctx)` invocations for the same guild (e.g. after-callback + a racing `!play`) interleave their counters; one can reset (1064) while the other is mid-count, defeating the cap and allowing >10 rapid retries or premature give-up. | NEW (prior G7 noted the retry redundancy at line 53, not the race) | Move retry accounting inside the lock-held region, or use a local counter passed through `_play_next_once`. |
| `cogs/music/cog.py` | 882-907, 1032-1058, 1424-1441, 2237-2262 | MEDIUM | Concurrency / thread-safety | **`after_playing*` callbacks run on the FFmpeg player thread but read voice-client state and `_gs()` directly.** `live_vc.is_playing()/is_paused()` and `self._gs(guild_id).fixing/.loop` are inspected from the non-loop thread; discord.py voice state is not documented thread-safe to read off-loop, and `_gs` mutation happens on the loop concurrently (TOCTOU). Works in practice but is a latent race. | G7 line 52 — STILL-PRESENT | Marshal the whole callback body onto the loop via `_safe_run_coroutine` and do all state reads there. |
| `cogs/music/cog.py` | 311-347 (`cleanup_guild_data`) | MEDIUM | Race | `cleanup_guild_data` mutates/deletes `self._guild_states[guild_id]` without holding that guild's `play_lock`. If `_play_next_once` holds the lock and is mid-playback, the `del` (347) / replace (345) drops the state out from under it; the lock object the in-flight task holds is then orphaned and its `finally: lock.release()` (1197) releases a lock no longer reachable. The `fixing`/`cleanup_pending` deferral (319-325) covers the fix path only, not the play path. | G7 line 36 — PARTIALLY-FIXED (setdefault race closed; play-lock-held race remains) | Acquire `gs.play_lock` (with timeout) before mutating `_guild_states`, or check `gs.play_lock.locked()` and defer like the fixing path. |
| `cogs/music/cog.py` | 702-729 (`safe_delete`) | MEDIUM | I/O / async latency | Up to 8 retries × min(2^n,4)s = ~22s of `await asyncio.sleep` per locked file, each iteration spawning 2 fresh `asyncio.to_thread` calls. Many concurrent track-ends serialise file deletion and compete for the default executor pool (shared with yt-dlp downloads). | G7 line 45 — STILL-PRESENT (retry count unchanged) | Cap at 3 retries; run the whole retry loop inside ONE `asyncio.to_thread` call. |
| `cogs/music/cog.py` | 104-171 (`_periodic_temp_cleanup`), 2460-2531 (`cleanup_cache`) | MEDIUM | Resource / cross-cog | Temp cleanup deletes ANY stale file in `temp/` not in the per-guild `current_track` in-use set. `temp/` is shared with other cogs (dashboard/CLI working files); files older than the staleness window (3600s / 120s grace) belonging to other subsystems are deleted. No filename-prefix gate (yt-dlp writes `temp/youtube-*` per outtmpl). | G7 line 78 + line 80 — STILL-PRESENT | Restrict deletion to the music download naming pattern (e.g. only files matching the configured `outtmpl` prefix). |
| `cogs/music/cog.py` | 1136-1141, 1882-1885, 1929-1933, 2588-2596 | MEDIUM | UX / multi-guild | Presence updates gated on `len(self.bot.voice_clients) <= 1`. With ≥2 active guilds, none of the now-playing/idle presence transitions fire (guard is False), so presence freezes on whatever was last set; and where it does fire it can clobber another guild's now-playing. The `<=1` guard prevents clobber but also disables legitimate single-guild-style updates in multi-guild deployments. | G7 line 44/55/157 — PARTIALLY-FIXED (clobber reduced, flicker/freeze trade-off remains) | Introduce a global presence manager that aggregates across all guild states instead of a count gate. |
| `cogs/music/cog.py` | 1137-1141, 2588-2596 | MEDIUM | Discord API | `change_presence(..., name=player.title)` — yt-dlp titles can exceed Discord's 128-char activity-name limit; no `[:128]` truncation. Long titles (livestream descriptions) raise/҂get rejected. | G7 line 55 — STILL-PRESENT | `name=player.title[:128]`. |
| `cogs/music/cog.py` | 1305-1493 (`fix`) | MEDIUM | Voice / async | `fix` sets `fixing=True` (1340) and on the happy path resets it only in `finally` (1484). Between disconnect (1352) and reconnect (1373), if `channel.connect` hangs up to its 30s timeout, `cleanup_guild_data` is blocked (early-returns on `fixing`) for that whole window — a guild-remove during it leaks state until the fix finally runs the deferred cleanup. `disconnect()` at 1352 is not `force=True` (can hang ~3s on a laggy voice WS). | G7 line 57/58 — PARTIALLY-FIXED (cleanup_pending deferral added; connect-hang window + non-force disconnect remain) | Use `disconnect(force=True)`; wrap reconnect in its own timeout already done (30s) — also cancel `auto_disconnect_task` at fix start. |
| `cogs/music/cog.py` | 1909-1947 (`leave`) | MEDIUM | Resource / data loss | `leave` clears `gs.queue` (1916) then schedules a save (1919) — saving the now-EMPTY queue, so the user's built queue is destroyed on `!leave` (cannot rejoin and resume). `disconnect()` (1928) is not `force=True`. | G7 line 69 — STILL-PRESENT | Save BEFORE clearing (or don't clear queue on leave if 24/7-style resume is desired); use `force=True`. |
| `cogs/music/cog.py` | 1861-1890 (stop), 1892-1907 (clear), 2033-2069 (shuffle), 2071-2110 (remove) | MEDIUM | Concurrency | Queue-mutating commands (`clear()`, `extend()`, `del queue[i]`) run without holding `play_lock`, racing `_play_next_once`'s `queue[0]`/`popleft` (945-957). `clear` mid-iteration is mostly benign (IndexError caught) but `del`/`extend` during a concurrent popleft can corrupt ordering or drop the wrong track. | G7 lines 67/68/72/73 + cross-cut "Per-guild lock granularity" — STILL-PRESENT | Add a lightweight `state_lock` per guild for queue mutations, or acquire `play_lock` briefly. |
| `cogs/ai_core/media_processor.py` | 49-61, 339-371 | MEDIUM | Resource / stale comment | GIF→MP4 now uses a shared module-level `_GIF_ENCODE_EXECUTOR` (good, fixes prior per-call leak), but the inline comment block at 332-344 still describes the OLD `shutdown(wait=False)` abandon-executor behaviour, and the timeout path (363-371) leaves the worker thread running ffmpeg in the background with no hard kill — a flood of pathological GIFs can saturate both pool workers indefinitely (DoS of the GIF feature). `MemoryError` is still not in the except at 386 (np.stack of 300×1.5MP frames can OOM). | G5 line 119/120 — PARTIALLY-FIXED (executor leak fixed, MemoryError still uncaught; comment stale) | Add `MemoryError` to the except at 386; correct the stale comment; consider a global semaphore capping concurrent encodes. |
| `cogs/spotify_handler.py` | 57-66, 73-138 | MEDIUM | Async / event-loop binding | `_setup_lock` is created lazily (68-71) but `_setup_client()` is called from `__init__` (66) which may run before an event loop exists (the cog constructs `SpotifyHandler` in `Music.__init__`). `_setup_client` itself is sync (fine), but the `_get_setup_lock()` Lock — once created inside a retry coroutine — binds to whatever loop is running then; if the bot's loop differs from a test loop, `async with self._get_setup_lock()` raises "bound to a different loop". | NEW (prior G7 line 96/99 covered timeouts/session leak, not loop binding) | Create the lock in an async `setup`/`cog_load` hook, or accept the documented single-loop assumption explicitly. |
| `cogs/spotify_handler.py` | 408-424 (`get_all_playlist_tracks`) | MEDIUM | Threading / closure | `get_all_playlist_tracks` calls `self.sp.playlist_tracks` / `self.sp.next` directly inside a `run_in_executor` worker. If `_api_call_with_retry` recreates `self.sp` on a retry (235), the in-flight executor closure keeps using the NEW `self.sp` (good per comment) — BUT it also calls `self.sp.next(results)` where `results` was produced by the OLD client; spotipy paging tokens are server-side URLs so this works, yet there is no None-guard if `self.sp` became None mid-recreate-failure (236 sets it possibly None inside the lock on another path). Low likelihood. | G7 line 110 — STILL-PRESENT (acknowledged) | Snapshot `client = self.sp` once at executor entry and None-check it. |
| `cogs/spotify_handler.py` | 282-287 | MEDIUM | Validation / substring | URL routing uses substring `"/track/" in query` etc. — `https://evil.com/track/x` is treated as a Spotify track and handed to `self.sp.track(query)`. The cog's `play` already validates the Spotify HOST (cog.py 1666-1677) before calling `process_spotify_url`, so the exploit surface is gated upstream — but `process_spotify_url` is `public` and unsafe if called directly. | G7 line 106 — STILL-PRESENT | Parse hostname and require `*.spotify.com` (or `spotify:` URI) inside the handler too. |
| `cogs/music/cog.py` | 234-257 (`_safe_run_coroutine`) | MEDIUM | Error handling | The done-callback `_on_done` calls `fut.result()` to surface exceptions, but `safe_delete`/`play_next` coroutines scheduled here that raise `CancelledError` during shutdown log a warning ("safe_run_coroutine error") for every in-flight deletion at teardown — noisy, and a genuine bug could hide in the noise. | NEW | Suppress `CancelledError` specifically in `_on_done`. |
| `cogs/music/queue.py` | 1-378 (entire `QueueManager`) | LOW | Dead code | `QueueManager` is fully implemented (correct locking, validation, verify-then-delete migration at 344-356) but `cog.py` never uses it — it manages `_guild_states[gid].queue` itself with the buggier `save_queue`/`load_queue`. Two diverging persistence implementations; the unused one is the correct one. | G7 line 81/128 — STILL-PRESENT (module-level singleton removed, class still orphaned) | Delete `QueueManager` or migrate the cog onto it (would also fix H-1 save_queue lock gap). |
| `cogs/music/cog.py` | 412-492 (`load_queue`) | LOW | Validation | Unlike `queue.py:load_queue` (279, 328-330) which filters items lacking a non-empty `url`, the cog's `load_queue` (446-448) only checks `data["queue"]` is a list, not that each item has a valid `url`. A malformed DB/JSON row with no url reaches `_play_next_once` and is dropped there (948-956) — handled, but late. | NEW (queue.py fixed it, cog.py didn't) | Mirror queue.py's per-item `isinstance(item, dict) and item.get("url")` filter. |
| `cogs/music/cog.py` | 1949-1985 (volume) | LOW | UX | `int(self._gs(...).volume * 100)` for display can show 99% for a stored 0.99-ish float after `volume/100.0` roundtrips. | G7 line 70 — STILL-PRESENT | `int(round(volume*100))`. |
| `cogs/music/cog.py` | 1495-1550 (join) | LOW | UX | `join` does not detect "already in your channel" before `move_to`; a no-op move still reports "ย้ายไป <channel>". | G7 line 60 — STILL-PRESENT | Compare `ctx.author.voice.channel == ctx.voice_client.channel` first. |
| `cogs/music/cog.py` | 1808-1859 (queue cmd) | LOW | UX | Only first 10 of up to 500 queued tracks are shown; no pagination. | G7 line 66 — STILL-PRESENT | Add page navigation. |
| `cogs/music/cog.py` | 2296-2366 (nowplaying) | LOW | UX | For livestreams (`duration==0`) elapsed grows unbounded and the progress bar stays empty; no "LIVE" badge. | G7 line 75 — STILL-PRESENT | Show LIVE when `duration==0`. |
| `cogs/music/cog.py` | 1987-2031 (`mode_247`) | LOW | Permission | `manage_channels` is a heavy gate for a music toggle (subjective). | G7 line 71 — STILL-PRESENT | Consider `manage_guild` or DJ role. |
| `cogs/music/cog.py` | 1555-1565 | LOW | Validation | `query[:500]` truncates rather than rejecting an unreasonably long search query. | G7 line 62 — STILL-PRESENT | Reject >~200 chars with a clear message. |
| `cogs/music/cog.py` | 1380-1396, 2195-2212 | LOW | Race | `fix`/`seek` check `Path(filename).exists()` on the loop thread (sync `os.stat`) — minor blocking; also a TOCTOU with the after-callback's `safe_delete`. Benign (handled by later guards). | NEW | Use `await asyncio.to_thread(...)` for the exists check; accept TOCTOU. |
| `cogs/music/views.py` | 25, 57-166 | LOW | Persistence / lifecycle | Views use `timeout=180.0` and are not persistent (`bot.add_view` not used). Each new track sends a fresh `MusicControlView` (cog.py 1133-1134) without `stop()`-ing the previous one, so old messages' buttons stay live until their own 180s timeout; after a bot restart all buttons are dead. `custom_id`s are static, so a persistent-view migration is feasible. | G7 line 88 + cross-cut "Timeout/cancellation" — STILL-PRESENT | Track the active view per guild and `view.stop()` the previous; or register persistent views with `timeout=None` + `bot.add_view`. |
| `cogs/music/views.py` | 167-177 (`on_timeout`) | LOW | UX | FIXED-VERIFIED: now `if self.message:` (173) guards before `.edit`, and `AttributeError` was added to the except (176). The prior CRITICAL/C2 AttributeError is resolved. (Listed as LOW only to record the verification.) | MASTER C2 / G7 line 94 — FIXED-VERIFIED | None. |
| `cogs/ai_core/media_processor.py` | 404-430 (`load_character_image`) | LOW | Logic / false positive | Character-name match is substring (`char_name.lower() in message_lower`, 411) — "Eli" matches "eligible"/"delivery", triggering spurious character-image loads. | G5 line 122/143 — STILL-PRESENT | Word-boundary regex or token-set match; sort longest-first. |
| `cogs/ai_core/media_processor.py` | 626-651 | LOW | Edge case | Text chunking at 15000 chars splits on character boundary; safe for `str` but does not preserve token/line boundaries (cosmetic for the model). | G5 line 124 — STILL-PRESENT | Optional: split on line boundaries. |
| `cogs/ai_core/voice.py` | 46-48 | LOW | Bug | `bot.get_channel(channel_id)` is cache-only; an uncached channel yields "ไม่พบช่องเสียงที่ระบุ" even when it exists. | G1 line 148 — STILL-PRESENT | Fall back to `await bot.fetch_channel`. |
| `cogs/ai_core/voice.py` | 32, 142-145 | LOW | Bug | `PATTERN_CHANNEL_ID` grabs the FIRST 17-20 digit run; a user mention `<@123…>` in the message is matched as the channel ID, joining the wrong/no channel. | G1 line 161 — STILL-PRESENT | Strip mentions first, or require a `<#…>` channel-mention form. |
| `cogs/ai_core/voice.py` | 10 | LOW | Quality | `TypeGuard` is superseded by `TypeIs` in 3.13. | G1 line 145 — STILL-PRESENT | Switch to `TypeIs` (semantics differ — verify). |
| `cogs/music/utils.py` | 66 (`format_duration`) | LOW | Edge case | `if not seconds` returns "00:00" for 0 AND negative; yt-dlp livestreams have returned -1. Pinned by tests intentionally. | G7 line 86 — STILL-PRESENT (documented) | `if not seconds or seconds < 0`. |
| `cogs/spotify_handler.py` | 67-138 (`_setup_client`) | LOW | Secrets | `client_secret` passed to `SpotifyClientCredentials`; on init failure the `except (SpotifyException, ValueError)` logs via `logger.exception` (135) which could include arg values in some spotipy/Py tracebacks. Low risk (spotipy doesn't echo secret in `__str__`), needs verification. | G7 line 97 — STILL-PRESENT (needs verification) | Log `type(e).__name__` only on init failure. |
| `cogs/spotify_handler.py` | 169-178, 304-326 | LOW | UX | Circuit-breaker-open raises `ConnectionError` (176) which `process_spotify_url` (302) catches into a generic "ไม่สามารถเชื่อมต่อ Spotify" — user can't tell breaker-open from a real API error. | G7 line 101 — STILL-PRESENT | Distinct message for breaker-open. |
| `cogs/spotify_handler.py` | 359, 491, 584 | LOW | UX | Only first artist (`track["artists"][0]`) used in the search query/title; multi-artist tracks lose collaborators. | G7 line 107 — STILL-PRESENT | `", ".join(a["name"] for a in artists[:3])`. |
| `cogs/spotify_handler.py` | 534-557 (`_handle_album`) | LOW | Consistency | Album pagination loops `await self._api_call_with_retry(self.sp.next, results)` per page on the event loop (each its own retry/breaker cycle), while playlist paginates inside ONE executor task — inconsistent patterns for the same job. | G7 line 113 — STILL-PRESENT | Consolidate album paging into a single executor helper like playlist. |
| `cogs/music/cog.py` | 1203, 1225, 1267, 1305 etc. (hybrid_command type: ignore) | INFO | Quality | Blanket `# type: ignore[arg-type]` on every `@commands.hybrid_command`; masks any real signature drift. | NEW | Narrow or remove the suppressions where possible. |
| `cogs/spotify_handler.py` | 1-2 | INFO | Quality | Blanket pyright suppressions at file top. | G7 line 95 — STILL-PRESENT | Scope suppressions to specific lines. |
| `cogs/music/cog.py` | 1679 | INFO | Type | `process_spotify_url(ctx, query, queue)` passes a `deque` where the annotation says `list[dict]`; works at runtime (`.append`), but the annotation is wrong (masked by file-top suppression). | G7 line 64 — STILL-PRESENT | Annotate `MutableSequence`. |
| `cogs/ai_core/media_processor.py` | 32-33 | INFO | Hardening | FIXED-VERIFIED: `Image.MAX_IMAGE_PIXELS = 30_000_000` set and `warnings.filterwarnings("error", category=DecompressionBombWarning)` installed (correctly using `filterwarnings`, not `simplefilter`, preserving operator filters). Recorded as verification of prior H13. | MASTER H13 / G5 — FIXED-VERIFIED | None. |

---

## Prior-audit cross-check (explicit status)

| Prior ID / location | Status | Current evidence |
|---|---|---|
| MASTER **C2** / G7 line 94 — views `on_timeout` `hasattr` AttributeError | **FIXED-VERIFIED** | views.py 173 `if self.message:`, 176 except now includes `AttributeError`. |
| MASTER **H13** / G5 — `MAX_IMAGE_PIXELS` unset | **FIXED-VERIFIED** | media_processor.py 32-33. |
| MASTER **H19** / G7 line 61 — `!play` missing `@commands.guild_only()` | **FIXED-VERIFIED** | cog.py 1553 has `@commands.guild_only()`. |
| MASTER **H20** / G7 line 47 — play_lock leak on cancel | **STILL-PRESENT → escalated to C-1** | cog.py 829-837: only `TimeoutError` sets the release flag; `CancelledError` path leaks the acquired lock. |
| MASTER **H21** / G7 line 51/165 — yt-dlp download inside play_lock | **FIXED-VERIFIED (architecture changed)** | Download still inside lock BUT timeout raised 0.1s→2s (820-829) and retry loop refactored; the "drops legit play requests" symptom is addressed. NOTE: yt-dlp still runs under the lock (30s+), so true concurrency for back-to-back `!play` on one guild is still serialised — partial. Marked verified for the dropped-request symptom; the blocking-under-lock remains a design point (see C-1 area). |
| MASTER **H22** / G7 line 33 — periodic save ignores pending set | **FIXED-VERIFIED** | cog.py 192-205 saves only `_queue_save_pending` guilds; `_schedule_queue_save` (211-213) now consulted. |
| MASTER **H23** / G7 line 39 — load_queue deletes JSON before DB write | **FIXED-VERIFIED** | cog.py 456-488 saves to DB, reads back, only unlinks if `db_queue` non-empty. queue.py 340-356 same. |
| MASTER **H24** / G7 line 48/49/74 — FFmpeg leaks on 5 play paths | **FIXED-VERIFIED** | `player.cleanup()` present on all: loop replay (909-920), normal (1060-1090, 1175-1184 finally), fix (1443-1453), seek (2264-2275). |
| G1 line 165 — voice.py `music_cog.current_track` assumes dict | **REGRESSED → H-3** | Refactor to `MusicGuildState` removed `Music.current_track`; voice.py 174/181 now always `AttributeError` when playing. |
| G7 line 81/128 — QueueManager unused/duplicate | **STILL-PRESENT** | queue.py class orphaned; cog uses its own buggier persistence. |
| G7 cross-cut "Per-guild lock granularity" | **STILL-PRESENT** | stop/clear/shuffle/remove mutate queue without lock. |

(Other prior LOW/INFO items — join no-op message, queue pagination, nowplaying LIVE badge, volume rounding, substring char-match, format_duration negatives, Spotify single-artist/album paging, secret-in-trace — all STILL-PRESENT, itemised in the findings table above.)

## Notes / cross-cutting

- **Persistence correctness is the headline regression risk.** Two issues compound: the DB schema drops `type` (H-1) so Spotify queues are unplayable after restart, and `Music.save_queue` lacks the lock that the unused `QueueManager` already has (H-2). The cleanest fix for both is to delete the cog's bespoke persistence and route through `QueueManager` after adding a `track_type` column.
- **The play_lock state machine (C-1) is the only crash/deadlock-class bug.** It was reported and marked fixed in the prior cycle but the cancellation branch was never added; under normal operation it only bites on cog reload / shutdown / task cancellation, which is why it survives testing. Recommend replacing the shield+callback construction with `async with asyncio.timeout(...)` + `async with lock`.
- yt-dlp/PO-Token modernization (prior G7 cross-cut) is out of scope for these files (lives in `utils/media/ytdl_source.py`) but remains the highest-leverage external item; not re-verified here.
- Dependency currency (yt-dlp, spotipy, Pillow, discord.py) not independently web-verified in this pass — cutoff Jan 2026, today 2026-05-24; flagging as needs-verification for the next dependency sweep.

## Confirmation

Read every line of: cog.py (2604), queue.py (378), views.py (178), utils.py (94), __init__.py (23), spotify_handler.py (620), media_processor.py (717), voice.py (206). Cross-referenced `utils/database/database.py` music_queue methods (1888-1937), `utils/media/ytdl_source.py` (from_url/get_ffmpeg_options/search_source), and `cogs/music/url_safety.py` in full to validate H-1, the FFmpeg cleanup paths, and the SSRF gate.
