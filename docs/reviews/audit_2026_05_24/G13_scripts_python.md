# G13: Ops / Maintenance Scripts (Python) — Re-Audit

Audit date: 2026-05-24
Auditor: Claude (Opus 4.7, 1M) — read-only source audit
Scope: 25 assigned files under `C:\BOT Discord\scripts\` (every line read in full from the working tree, NOT git diff). Prior audit cross-checked against `docs/reviews/audit_2026_05/G11_scripts.md` + `MASTER_TABLE.md` (H40–H46).

Files read in full:
`scripts/bot_manager.py`, `scripts/dev_watcher.py`, `scripts/load_test.py`, `scripts/check_balance.py`, `scripts/verify_system.py`, `scripts/test_bot_manager.py`, `scripts/test_dashboard_chat.py`, `scripts/__init__.py`, `scripts/shared/__init__.py`, `scripts/shared/colors_fallback.py`, and `scripts/maintenance/{add_local_id, check_db, clean_cli_orphans, clean_history, convert_log015, count_cli_sessions, dump_cli_prompt, find_unused, inspect_dashboard_memories, inspect_db, migrate_to_db, reindex_db, rollback_migration, schema_smoke, view_db}.py`.

Cross-referenced supporting code (read-only, out of assigned scope): `utils/database/database.py:get_write_connection/get_connection` (commit/rollback semantics), `utils/reliability/self_healer.py:kill_everything` (authorization gate).

## Severity counts (this audit's NEW + carried findings)

| Severity | Count |
|---|---:|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 7 |
| LOW | 18 |
| INFO | 13 |

## Prior-finding verification (H40–H46)

| Prior ID | File:line (current) | Status | Notes |
|---|---|---|---|
| H40 (auto_stop kills prod w/o confirm) | `bot_manager.py:606-655` | FIXED-VERIFIED | `auto_stop_existing_bot()` now prompts `yes`/abort unless `assume_yes=True` or `BOT_MANAGER_ASSUME_YES` env. `start_bot()` (680-685) honors the abort. |
| H41 (STOP_FLAG CWD-relative + never cleaned) | `bot_manager.py:77-79, 728, 743-747` | FIXED-VERIFIED | `STOP_FLAG`/`PID_FILE`/`STATUS_FILE` now `PROJECT_ROOT / ...`. `stop_bot()` writes the flag then unlinks it (best-effort, `missing_ok`). |
| H42 (no Job Object → Windows orphan) | `dev_watcher.py:502-509` | STILL-PRESENT | Bot child still launched with `CREATE_NEW_PROCESS_GROUP` and no Job Object. If the watcher is force-killed, the bot is orphaned on Windows. See M-1. |
| H43 (add_local_id no transaction) | `add_local_id.py:44-90` | FIXED-VERIFIED | `isolation_level=None` + explicit `BEGIN IMMEDIATE` + try/except `rollback()` + `commit()`. Also now hard-refuses if `bot.pid` exists (115-126). |
| H44 (`--delete-json` no extra confirm/backup) | `migrate_to_db.py:206-227, 320-341` | FIXED-VERIFIED | `--delete-json` now aborts unless `--backup` OR `--yes-delete-json`. Deletion limited to files whose migration committed (`migrated_history_paths`). Per-file single transaction via `get_write_connection` (commit-on-success / rollback-on-error confirmed in `database.py:1155-1167`). |
| H45 (RENAME drops FKs / breaks cached ids) | `reindex_db.py:114-148, 310-324` | FIXED-VERIFIED | Pre-flight `PRAGMA foreign_key_list` refuses to run if any table FKs into `ai_history`; post-rename `PRAGMA foreign_key_check` rolls back on broken rows; indexes/triggers/views now re-created. External-id-breakage documented loudly (docstring 77-84). Residual semantic caveat noted as INFO. |
| H46 (verify_system imports modules → side effects) | `verify_system.py:1-92` | FIXED-VERIFIED | The `check_imports()` phase is gone entirely. Script is now compile-only (`compile(..., "exec")`); points users at `pytest --collect-only` for import resolution. No `importlib.import_module`. |

Net: 6 of 7 prior HIGHs FIXED-VERIFIED; **H42 (dev_watcher Job Object) STILL-PRESENT** and re-filed as M-1.

---

## Findings

| File | Line(s) | Severity | Category | Issue | Prior | Suggested fix |
|---|---|---|---|---|---|---|
| `dev_watcher.py` | 502-509 | MEDIUM | Windows orphan / process lifecycle | Bot child spawned with `CREATE_NEW_PROCESS_GROUP` (Win) / `start_new_session=True` (POSIX) but NOT assigned to a Job Object. If the watcher dies abnormally (Task Manager kill, OOM, power loss of the console), the bot child is orphaned — the only reaper is the explicit `terminate()` in the shutdown path, which never runs on hard-kill. | H42 | Create a Win32 Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` and `AssignProcessToJobObject` the child so the OS reaps the tree when the watcher handle closes. |
| `bot_manager.py` | 277-309, 312-323 | MEDIUM | Process detection correctness | `find_all_bot_processes()` matches `python` + exact basename `bot.py` and *excludes* cmdlines containing `dev_watcher`/`bot_manager`. But the bot child launched by `dev_watcher` (`[sys.executable, "bot.py"]`) has NO `dev_watcher` token in its own cmdline, so it is correctly found — yet `stop_bot()`/`auto_stop_existing_bot()` will then terminate that child WITHOUT touching the parent watcher, which immediately auto-restarts it (`check_for_crash`/restart loop). Result: "stop" appears to fail / the bot respawns. Stopping a dev-mode bot requires killing the watcher first. The code stops watchers before bots in `stop_bot()` (733-734) which mitigates, but only if the watcher PID was detected; a watcher started via `.bat`/`wt.exe` wrapper may not match `["python","dev_watcher"]`. | — | Detect the watcher via process *tree* (parent of the bot child) as well as cmdline, and stop the parent before the child; or write the watcher PID to `dev_watcher.pid` and consult it. |
| `bot_manager.py` | 658-677 | MEDIUM | Subprocess / observability | `_launch_script()` uses `os.startfile(path_str)` (Win non-hidden) and `subprocess.Popen([path_str], ...)` with no stdout/stderr capture. A bot that crashes on startup still yields a green "Bot start command sent!" (705/711/715) — operator gets no failure signal. `os.startfile` fully detaches the child (no handle, no exit code). | (prior G11 MED, unlisted in H-series) | Launch via `subprocess.Popen` capturing/forwarding output, poll once after a short delay to confirm the process is alive before printing success. |
| `bot_manager.py` | 669 | MEDIUM | Subprocess / PATH hijack | Hidden launcher calls `subprocess.Popen(["wscript", path_str], shell=False, cwd=cwd)` — `wscript` resolved via `PATH`. A `wscript.exe` planted earlier on `PATH` than `System32` would run instead. Operator-privilege context raises the impact. | (prior G11 MED) | Hardcode `os.path.join(os.environ["SystemRoot"], "System32", "wscript.exe")` or resolve with `shutil.which` and validate the location. |
| `migrate_to_db.py` | 144-148 | MEDIUM | Idempotency / data integrity | The `INSERT ... local_id = (SELECT COALESCE(MAX(local_id),0)+1 ...)` is correct within one run, but there is NO `UNIQUE(channel_id, message_id)` / `ON CONFLICT` guard. Re-running migration (e.g., after restoring a partial DB, or re-pointing at a non-empty DB) inserts DUPLICATE rows and continues numbering `local_id` from the existing max — silent duplication of user history. The docstring claims re-run safety but the code does not enforce it. | (prior G11 MED, partial) | Use `INSERT ... ON CONFLICT(channel_id, message_id) DO NOTHING` (requires the unique index) or check existence before insert; document that the source DB must be empty for that channel. |
| `migrate_to_db.py` | 185-195 | MEDIUM | Backup scope / disk | `create_backup()` does `shutil.copytree(Path("data"), data_backup_*)` with no exclude filter — duplicates the entire `data/` tree including `db_export/`, prior `data_backup_*`, large attachments, and the live DB's `-wal`/`-shm`. On a multi-GB data dir this can fill the disk and, because WAL/SHM are copied at an arbitrary instant, the copied DB may be inconsistent (mitigated only by "bot stopped"). | — | Exclude `db_export`, nested `data_backup_*`, and tmp dirs via `ignore=shutil.ignore_patterns(...)`; for the DB use `sqlite3 .backup` or copy WAL/SHM atomically. |
| `clean_history.py` | 17-99 | MEDIUM | Destructive / no confirm | `clean_history_files()` mutates every `data/ai_history_*.json` immediately on invocation — no `--apply`/`yes` gate. Atomic tmp+replace and a timestamped `.bak` make it recoverable, but an accidental run rewrites all history files with no abort window. Inconsistent with `clean_cli_orphans.py` (which gates behind `--apply`+`yes`). | (prior G11 MED) | Add a dry-run default + `--apply`/`--yes` confirmation, matching the `clean_cli_orphans.py` pattern. |
| `bot_manager.py` | 245-251 | LOW | Subprocess churn | `clear_screen()` spawns `cmd /c cls` every menu refresh inside the interactive loop. Functional (`shell=False`, fixed args) but wasteful process churn vs `os.system("cls")` or an ANSI clear. | (prior G11 LOW) | Use an ANSI clear sequence or `os.system`. |
| `bot_manager.py` | 549-603 | LOW | Cross-platform | Graceful SIGINT branch is POSIX-only (`if sys.platform != "win32"`); Windows jumps straight to `terminate()`. The comment "SIGINT doesn't work well" is misleading — `CTRL_C_EVENT`/`CTRL_BREAK_EVENT` can be delivered to a process group on Windows. No graceful attempt is made before terminate. | (prior G11 LOW) | Optionally send `CTRL_BREAK_EVENT` to the bot's process group (the child was started with `CREATE_NEW_PROCESS_GROUP`) before `terminate()`. |
| `bot_manager.py` | 756-774 | LOW | Bug / return value | `restart_bot()` calls `start_bot(mode)` (774) but does not `return` it — caller cannot tell if restart succeeded. (Prior also flagged "scheduled" downgrade; that is now FIXED at 769-770, and `start_bot` 693-698 maps `scheduled`→`hidden` with a warning.) | (prior G11 LOW ×2; scheduled part FIXED) | `return start_bot(mode)`. |
| `bot_manager.py` | 857-873 | LOW | Audit trail | `run_kill_all()` confirms with literal `yes` and forwards `authorized=True` to `kill_everything` (verified gate at `self_healer.py:710-727`), but logs no list of killed PIDs anywhere the operator can audit later. | (prior G11 MED → downgraded; confirm + auth now correct) | Log killed PIDs to `logs/self_healer.log`. |
| `bot_manager.py` | 876-897 | LOW | I/O edge case | `_tail_lines()` chunked tail loop `while pos > 0 and data.count(b"\n") <= n` works, but for a file whose last `n` lines exceed the 64 KB chunk and contain no newlines it may under-read; `splitlines()[-n:]` then returns fewer than `n` logical lines. Edge case only. | (prior G11 LOW) | Grow the chunk or loop until `>= n` newlines or buffer ≥ file size. |
| `bot_manager.py` | 924-963 | LOW | UX / cleanup | A `KeyboardInterrupt` during `start_bot()` breaks the main loop with no cleanup of a partially-launched child (which is detached via `os.startfile` anyway). | (prior G11 LOW) | Wrap `start_bot` launch in try/finally or document that Ctrl-C mid-launch may leave a detached process. |
| `dev_watcher.py` | 107-130 | LOW | Config validation | `load_from_file()` validates via `isinstance(value, expected_type)`. `bool` is a subclass of `int`, so a JSON `true` is accepted for an `int` field (e.g. `max_crash_retries: true` → stored as `True`), and an `int` is rejected for a `bool` field. | (prior G11 LOW) | Special-case `bool` vs `int` (reject `bool` where `int` expected, and vice versa). |
| `dev_watcher.py` | 166-178 | LOW | Logging growth | `logging.FileHandler(... "dev_watcher.log")` uses default append mode, no rotation — unbounded growth over long watcher sessions. | (prior G11 LOW) | Use `RotatingFileHandler` (e.g. 5 MB × 3). |
| `dev_watcher.py` | 187-196 | LOW | Cross-platform perf | `PollingObserver` on Windows is reliable but CPU-inefficient vs native `ReadDirectoryChangesW` (watchfiles). Perf opportunity, not a bug. | (prior G11 LOW) | Consider `watchfiles` for local NTFS. |
| `dev_watcher.py` | 316-334 | LOW | Exception handling / data flow | Fallback `check_and_stop_existing_bot()` catches bare `except Exception as e:` then `sys.exit(1)` — any failure to stop the old bot aborts the *watcher launch entirely* while the old bot keeps running. Operator's "start dev mode" silently fails. (Note: the Self-Healer path above usually handles this; the fallback is reached only when `self_healer` import fails.) | (prior G11 MED → downgraded; primary path is Self-Healer) | Narrow the except, and on failure continue into watcher start (let the new launch's own conflict detection handle it) rather than exiting. |
| `dev_watcher.py` | 539-596 | LOW | Restart-loop cap bypass | `consecutive_crashes` is reset to 0 only on a successful start (531). A bot that starts then fails the health check (`_perform_health_check` returns False, poll() non-None within `health_check_delay`) increments only `crash_count`, NOT `consecutive_crashes`, on the health-check path — so a fast-crashing bot detected via health-check (not via `check_for_crash`) can re-spawn without hitting `max_crash_retries`. The retry cap only fully protects the `check_for_crash` exit-code path. | (prior G11 MED → still present, downgraded as dev-only) | Increment `consecutive_crashes` (or bail) when the health check fails, mirroring the crash path. |
| `dev_watcher.py` | 465-478, 897-911 | LOW | Handle leak on kill failure | In `_start_bot_unlocked`, after `terminate()`→`wait(5)`→`kill()`→`wait(2)`, the inner `wait` is only guarded for `subprocess.TimeoutExpired`; if `kill()` raises `OSError` (already-gone/zombie) the old `self.process` is reassigned below, leaking the prior handle. Final shutdown (904-909) has the same shape. | (prior G11 MED → downgraded; dev tool) | Wrap `kill()`/`wait()` in `contextlib.suppress(OSError, subprocess.TimeoutExpired)` and null out `self.process` before reassign. |
| `dev_watcher.py` | 459-462, 580-581 | LOW | Observer stall | Debounce check and the `time.sleep(crash_retry_delay)` auto-retry both run while holding `self._lock`; a 5 s retry freezes the watchdog observer thread (any change event blocks on the lock for up to 5 s). Acceptable for a dev tool but undocumented in-code at the sleep site. | (prior G11 MED → downgraded) | Sleep outside the lock, or note the intentional freeze at line 580. |
| `load_test.py` | 104-134 | LOW | Mislabel / dead realism | `simulate_ai_request()` is purely synthetic (random sleep + 5% random raise); it never calls a real endpoint. The file is billed as "Load Testing" but exercises no bot code on the main path. | (prior G11 LOW) | Rename to a smoke/throughput harness, or wire it to the real AI entrypoint behind a flag. |
| `load_test.py` | 169-170 | LOW | Asyncio memory | `tasks = [rate_limited_request(i) for i in range(1, total+1)]; await asyncio.gather(*tasks)` materializes all coroutines at once; the semaphore caps concurrency but not creation. `--requests 1_000_000` allocates a million coroutines. | (prior G11 MED → downgraded; synthetic tool) | Use a bounded `asyncio.Queue`/worker fan-out, or chunk the gather. |
| `load_test.py` | 247-259 | LOW | Argparse bounds | `--requests`/`--concurrency`/`--delay` have no upper bound; `--concurrency 100000` allocates that many semaphore slots. | (prior G11 LOW) | Clamp via `type=lambda x: max(1, min(int(x), CAP))`. |
| `load_test.py` | 196, 225-238 | LOW | Shared-state pollution | `run_rate_limiter_test()` hardcodes `("gemini_api", user_id=12345)` and `run_circuit_breaker_test()` mutates the *global* `gemini_circuit` (`record_failure`/`reset`). In-process only, but if ever imported into the running bot it would trip the real breaker / pollute user 12345's bucket. | (prior G11 LOW ×2) | Use a dedicated test user id / a fresh circuit-breaker instance. |
| `check_balance.py` | 79-121 | LOW | Schema assumption | Hits `/v1/dashboard/billing/{subscription,usage}` — OpenAI-style endpoints, not native Anthropic. Works only against a proxy that emulates them; against real `api.anthropic.com` it 404s (now surfaced cleanly via `raise_for_status`). The host allowlist (38-44) hardening is solid; this is a documentation gap. | (prior G11 LOW) | Document the required proxy contract, or rename to "check proxy quota". |
| `dump_cli_prompt.py` | 25-27 | LOW | Import-time side effects | Imports `dashboard_chat_claude_cli`/`dashboard_common`/`dashboard_config` at module top; these may pull DB/config at import time. Mitigated by docstring + the comment. | (prior G11 LOW) | Defer imports into `main()` if any of those modules acquire resources on import. |
| `dump_cli_prompt.py` | 30-35, 56-61 | LOW | Event-loop blocking | Uses sync `sqlite3.connect` inside `async def main`, then `build_user_context` relies on the bot's `get_db()` singleton — a sync connect blocks the loop. Fine for a one-shot dump. (LIKE-escaping at 34 is a good fix.) | (prior G11 LOW) | Acceptable; optionally note one-shot intent. |
| `inspect_dashboard_memories.py` | 11-51 | LOW | Module-top execution | All queries run at import time (no `main()`/`__main__` guard). `import inspect_dashboard_memories` performs DB reads + prints. (DB path now anchored + `with` block — good.) | (prior G11 LOW) | Wrap body in `def main()` under `if __name__ == "__main__"`. |
| `inspect_db.py` | 9-17 | LOW | Module-top + broad except | Loop runs at module top (no guard); `except Exception as e:` prints a one-line "Skipping" without the error type. Path anchored now (good). | (prior G11 LOW ×2) | Guard under `__main__`; include `type(e).__name__`. |
| `find_unused.py` | 30-65 | LOW | Static-analysis blind spots | AST import extraction (a genuine improvement over the old regex, and relative-import resolution at 55-65 is correct) still cannot see dynamic loads: `bot.load_extension("cogs.x")`, `importlib.import_module(str)`, `__import__`. Discord cogs loaded by string will be reported "unused" with no whitelist. | (prior G11 MED → improved, downgraded) | Whitelist `cogs/**` extension modules, or parse `load_extension(...)` string args. |
| `find_unused.py` | 162-163 | LOW | Dead branch | Skips `rel_parts[0] == "tools"` but there is no `tools/` dir in the repo. Harmless dead code. | (prior G11 LOW) | Remove or document. |
| `schema_smoke.py` | 30-32, 59-62 | LOW | Magic count / migration assumption | `expected_version = len(glob("migrations/*.sql"))` assumes every `.sql` is a numbered migration (a stray helper/notes `.sql` inflates the count); `if len(tables) < 15` is a magic floor. Working-tree shows a new untracked `015_ai_history_summarized_at.sqlite.sql` — count is dynamic, so this stays correct only as long as the folder holds *only* migrations. | (prior G11 LOW ×2) | Track expected counts explicitly, or assert the filenames match `^\d{3}_`. |
| `schema_smoke.py` | 38, 55 | LOW | CWD restore on hard exit | `os.chdir(temp_dir)` / `os.chdir(original_cwd)` in `finally` — bypassed only by `os._exit`/signal-kill, which would leave the parent shell in a now-deleted temp dir. Limited risk. | (prior G11 MED → downgraded) | Acceptable; `finally` covers normal paths. |
| `view_db.py` | 46-54 | LOW | Memory | `data = [dict(row) for row in rows]` then `json.dumps(...)` for each table — a multi-GB table is fully materialized in RAM. (Whitelist via `KNOWN_TABLES` + bracket-quoting is good.) | (prior G11 LOW) | Stream rows with an incremental JSON writer for large tables. |
| `rollback_migration.py` | 54-58 | LOW | Backup discoverability | `list`/`restore` only see `bot_database_v*.db`; sibling backups created by other tools (`bot_before_reindex_*.db`, `bot_before_localid_*.db`, `pre_rollback_*.db`) are invisible, so the operator cannot restore a reindex/localid snapshot via this tool. | (prior G11 LOW) | Broaden the glob or add a `--all` listing mode. |
| `convert_log015.py` | 51-59 | LOW | Dead comment block | Lines 54-59 are a multi-line comment describing logic ("Logger already declared; skip re-insertion") that the code does NOT perform here — the real gate is the `if LOGGER_DECL not in original` check at 70. Stale/confusing. (Atomicity at 108 `tmp_path.replace(path)` and the `^|\r?\n`-anchored regex are correct now — prior MEDIUMs FIXED.) | (prior G11 MED ×2 → FIXED) | Delete the misleading comment block. |
| `test_bot_manager.py` | 13-103 | INFO | Dev/test helper | Imports `scripts.bot_manager` (read-only functions: `get_bot_status`, `find_*`, box helpers) and runs `SelfHealer.diagnose()` (read-only). No destructive calls; `sys.path` anchored to project root (good). Not under `tests/`. | — | None — informational, as instructed. |
| `test_dashboard_chat.py` | 14-122 | INFO | Dev/test helper — touches real DB | Calls `db.init_schema()` on the **real** `data/bot_database.db` (no temp dir), creates/deletes a `test-conv-123` conversation, and starts/stops the dashboard WebSocket server on `127.0.0.1:8765`. Cleanup deletes only its own test conv, but running this against a production DB applies schema migrations and binds a port. | — | Point it at a temp DB (like `schema_smoke.py`) and/or guard with an env flag; mark clearly as dev-only. INFO per instructions. |
| `verify_system.py` | 16-32 | INFO | Compile-only scope (good) | `_EXCLUDED_DIRS` correctly skips vendored/cache dirs; compile-only, no imports (H46 fixed). `RP` is excluded — confirm that's intended (roleplay assets, not source). | (was H46) | None; verify `RP` exclusion is desired. |
| `verify_system.py` | 76 | INFO | CWD-dependent root | `root_dir = Path.cwd()` — the compile sweep targets CWD, not `Path(__file__).parent.parent`. Invoked from a subfolder, it checks the wrong tree. Lower impact than prior (no imports now), but still CWD-coupled. | (prior G11 MED → downgraded, compile-only) | Anchor to `Path(__file__).resolve().parents[1]` (or accept a `--root` arg). |
| `add_local_id.py` | 27-32 | INFO | Backup consistency | `shutil.copy2(DB_PATH, ...)` backup does not copy `-wal`/`-shm`. The new hard `bot.pid` gate (115-126) means the bot must be stopped, so WAL should be checkpointed/empty — risk largely neutralized, unlike `reindex_db.py` which copies siblings defensively. | (prior G11 MED → downgraded) | For parity, copy WAL/SHM siblings if present. |
| `add_local_id.py` | 75-78 | INFO | Performance | Per-row `UPDATE` in a Python loop (N round-trips per channel). Correct under `BEGIN IMMEDIATE`, but O(rows) round-trips; slow for large histories. | (prior G11 MED → downgraded, correctness OK) | Single set-based `UPDATE ... = (SELECT COUNT(*) ...)` or a window function. |
| `clean_cli_orphans.py` | 18-25 vs `count_cli_sessions.py:19-37` | INFO | Encoder duplication | The `encode()`/`encode_bot_style()` path-encoder is duplicated across both scripts and must stay in sync with `dashboard_chat_claude_cli.py:_encode_claude_project_dirname`. A future change to the production encoder silently desyncs these (the `count_cli_sessions` MISMATCH warning is the only tripwire). | — | Import the production encoder instead of re-implementing it. |
| `reindex_db.py` | 77-84 (doc) | INFO | Residual semantic caveat | Even with the FK pre-check + `foreign_key_check`, re-numbering primary keys still silently invalidates any *external* cached `ai_history.id` (exported JSON dumps, audit links, cache keys) — there is no DB-level FK to catch those. The docstring documents it; flagged for awareness. | H45 (FIXED) | None — documented; operators must re-export downstream id references. |
| `count_cli_sessions.py` | 46-146 | INFO | Prior NameError FIXED | The prior `UnboundLocalError`/module-top execution (G11) is FIXED: work moved into `main()` under `__main__`; `data` is initialized to `{}` and `raw` handled defensively. Encoder now includes `_`. | (prior G11 LOW ×2 → FIXED) | None. |
| `clean_history.py` | 65-92 | INFO | Backup/atomicity FIXED | Prior `.bak`-overwrite-on-rerun and non-atomic-write concerns FIXED: timestamped `.bak` + tmp-file `os.replace`. Only the missing confirmation remains (see MEDIUM row). | (prior G11 part FIXED) | None for backup/atomicity. |
| `migrate_to_db.py` | 78-162 | INFO | Transaction FIXED | Prior per-row-commit concern FIXED: one `get_write_connection` block per file commits on success / rolls back on exception (`database.py:1155-1167`), and partial failures are excluded from `--delete-json`. The remaining gap is idempotency on re-run (see MEDIUM row). | (prior G11 MED → mostly FIXED) | None for atomicity. |
| `shared/colors_fallback.py` | 45-52 | INFO | Import-time side effect | `enable_windows_ansi()` runs at import time when `utils.media.colors` is importable; cannot be suppressed by a caller that doesn't want ANSI mutation. Minor, by design. | (prior G11 LOW) | Expose a no-op flag if suppression is ever needed. |
| `scripts/__init__.py`, `scripts/shared/__init__.py` | all | INFO | Clean | Package markers / re-exports only. No issues. | — | None. |

---

## Cross-cutting notes

### Out-of-scope but observed (not assigned)
- `scripts/dev/` exists in the working tree (untracked): `probe_ai_deep.py`, `probe_ai_fixes.py`, `probe_ai_flow.py` (+ compiled `.pyc`). These are NOT in the assigned file list and were not audited. Flagging only so the next audit pass can scope them — they are dev probes that likely import bot internals.
- `scripts/maintenance/migrations/*.sql` (incl. untracked `015_ai_history_summarized_at.sqlite.sql`) are SQL, not assigned Python; the migration *runner* lives in `utils/database/` (out of scope). `schema_smoke.py` depends on their count.

### Destructive-script posture (post-fix)
| Script | Destructive op | Confirm? | Backup? | Atomic? | Live-bot guard? |
|---|---|---|---|---|---|
| `bot_manager.py` (kill_all) | kill matching procs | `yes` prompt + `authorized=True` | no | n/a | n/a |
| `bot_manager.py` (auto_stop) | kill running bot on start | **yes/abort prompt (FIXED)** + env override | no | n/a | n/a |
| `dev_watcher.py` (startup stop) | kill bot at start | no (Self-Healer/fallback) | no | n/a | n/a |
| `clean_cli_orphans.py` | unlink + symlink-safe rmtree | `--apply`+`yes`/`--yes` | no | per-file | n/a |
| `clean_history.py` | rewrite JSON | **NO (gap)** | timestamped `.bak` | tmp+replace | n/a |
| `convert_log015.py` | rewrite .py | no (syntax-checked) | no | **tmp+replace (FIXED)** | n/a |
| `add_local_id.py` | schema + UPDATE | `yes`/`--force` | copy2 (no WAL) | `BEGIN IMMEDIATE` (FIXED) | **bot.pid hard-fail (FIXED)** |
| `migrate_to_db.py` | INSERT + optional delete | `--delete-json` needs `--backup`/`--yes-delete-json` (FIXED) | `--backup` (broad copy) | per-file tx (FIXED) | no explicit pid guard |
| `reindex_db.py` | DROP+RENAME | `yes` | copy2 + WAL/SHM | `BEGIN IMMEDIATE` + FK check (FIXED) | **bot.pid hard-fail (FIXED)** |
| `rollback_migration.py` (restore) | overwrite live DB | `YES`/`--yes` | `pre_rollback_*.db` | tmp + `os.replace` (FIXED) | WAL/SHM non-empty check |

The two biggest residual risks: **(1) dev_watcher Windows orphan (M-1, prior H42)** and **(2) clean_history runs with no confirmation**. `migrate_to_db` non-idempotent re-run is the notable new data-integrity finding.

### Confirmation
Every line of every assigned file was read in full from the working tree. Supporting modules (`database.py`, `self_healer.py`) were read read-only to verify commit/rollback and kill-authorization semantics. No source file was modified.
