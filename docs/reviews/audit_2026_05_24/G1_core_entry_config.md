# G1 Re-Audit — Core Entrypoint & Project Config (2026-05-24)

**Auditor:** senior code auditor (read-only on source).
**Scope:** Entrypoint + project configuration of a production Discord bot (Python 3.14, discord.py 2.7.1). Files read in full, working-tree content (not git diff). Dependency CVEs verified via web search (today 2026-05-24).

**Files reviewed (every line):**
`bot.py` (1085), `config.py` (202), `version.txt` (1), `cogs/__init__.py` (5), `cogs/ai_core/__init__.py` (121), `cogs/ai_core/imports.py` (295), `pyproject.toml` (155), `requirements.txt` (68), `Makefile` (122), `env.example` (296), `.pre-commit-config.yaml` (59), `.markdownlint.json` (11), `.gitignore` (137), `.cursorignore` (52), `.dockerignore` (103), `.devwatcher.example.json` (38), `.claude/settings.local.json` (16), `.github/dependabot.yml` (79), `.github/workflows/ci.yml` (519).

**Note on prior audit:** `docs/reviews/audit_2026_05/G1_core_entry.md` was written against an OLDER `bot.py` (922 lines) and `config.py` (194 lines). Both have since been substantially refactored (bot.py now 1085 lines with extensive defensive rewrites). Prior-finding line numbers do not map 1:1; I re-verified each relevant prior item against current content and marked status. The prior G1 group reported 0 CRIT / 0 HIGH / 9 MED. Most prior MED/LOW items are now FIXED-VERIFIED (see table). The dependency HIGH (H6 pypdf) lives in MASTER_TABLE.md and is **STILL-PRESENT**.

## Severity-count summary (this re-audit, G1 scope)

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 3 |
| MEDIUM | 7 |
| LOW | 19 |
| INFO | 12 |
| **Total** | **41** |

CRITICAL/HIGH inline:
- HIGH | requirements.txt:24 | `pypdf>=5.0.0` floor permits CVE-laden pypdf 5.x and 6.x<6.9.2 (incl. CVE-2026-33699 infinite-loop DoS, CVSS 8.2) -> pin `pypdf>=6.10.2` (or `>=6.9.2`)
- HIGH | requirements.txt (missing) | `defusedxml` is the sole DOCX XXE control (document_extractor.py:46-60 disables DOCX if absent) but is undeclared — only present transitively (installed 0.7.1) -> add `defusedxml>=0.7.1` as a direct dependency
- HIGH | requirements.txt:17 | `anthropic>=0.99.0,<1.0` does NOT match installed 0.98.1 (env unsatisfied); pin is also already behind latest 0.104.1 -> reconcile lockfile/pin (install `>=0.99.0` or lower floor to installed) and bump

---

## Findings table

| File | Line(s) | Severity | Category | Issue | Prior | Suggested fix |
|---|---|---|---|---|---|---|
| requirements.txt | 24 | HIGH | Dependency/CVE | `pypdf>=5.0.0` — open-ended floor allows the entire vulnerable 5.x line and 6.x below 6.9.2. Verified real CVEs: CVE-2026-33699 (infinite loop in `DictionaryObject.read_from_stream`, fixed 6.9.2, CVSS 8.2), CVE-2026-27628 (xref `/Prev` loop, fixed 6.7.2), CVE-2026-24688 (outline loop, fixed 6.6.2), CVE-2026-28351 (RunLengthDecode resource exhaustion, fixed 6.7.4). pypdf parses untrusted user-uploaded PDFs in `document_extractor.py`. Installed is 6.10.2 (safe) so `pip-audit --strict` in CI passes — the **floor**, not the installed pin, is the exposure: a fresh resolve on another machine can legally pull 5.x. | MASTER_TABLE H6 (`pypdf>=6.8.0`) — STILL-PRESENT (floor unchanged at `>=5.0.0`) | Pin `pypdf>=6.10.2` (matches installed) or at minimum `>=6.9.2`. |
| requirements.txt | (absent) | HIGH | Dependency/Security | `defusedxml` is NOT a declared dependency, yet it is the **only** XXE defense for DOCX extraction. `document_extractor.py:46-60` imports `defusedxml.lxml` and *disables DOCX entirely* (`DOCX_DISABLED=True`, logs `critical`) when it's missing. It is currently installed only transitively (0.7.1, likely via another package). If that transitive provider changes, DOCX silently breaks in prod with no requirements-level guarantee. | New (prompt hypothesis confirmed) | Add `defusedxml>=0.7.1` to requirements.txt under AI section. |
| requirements.txt | 17 | HIGH | Dependency/Drift | `anthropic>=0.99.0,<1.0` but the **installed** version is **0.98.1** — the environment does not satisfy the declared constraint (a clean `pip install -r` would upgrade/diverge). Latest is 0.104.1. The CVE floor (CVE-2026-34450/34452 fixed in 0.87.0) is satisfied by both, so this is a drift/reproducibility bug, not an open CVE. The upper cap `<1.0` will also silently exclude future majors. | New | Reconcile: either `pip install` to bring env to `>=0.99.0`, or lower floor to `>=0.98.1`; consider bumping toward 0.104.x and re-testing the SDK paths in `api/api_failover.py`. |
| requirements.txt | 23 | MEDIUM | Dependency | `imageio[ffmpeg]==2.37.3` pulls `imageio-ffmpeg` which downloads/bundles an FFmpeg binary — duplicates the bot's own FFmpeg resolution (`utils/media`, vendored `./ffmpeg/`). Two FFmpeg provisioning paths = version skew + larger image. Needs verification whether the imageio ffmpeg plugin is actually used or if system FFmpeg suffices. | New | Confirm usage; if system/vendored FFmpeg is used everywhere, drop the `[ffmpeg]` extra. needs verification. |
| requirements.txt | 7,11,12,58,61 | INFO | Dependency/CVE | CVE references in comments verified REAL and correctly mitigated by the pins: python-dotenv 1.2.2 ≥ fix for CVE-2026-28684 (symlink-follow in set_key); requests `>=2.33.0` ≥ fix for CVE-2026-25645; cryptography `>=48.0.0` ≥ fixes for CVE-2026-34073 & CVE-2026-39892; lxml `==6.1.0` is exactly the fix release for CVE-2026-41066 (XXE default); Pillow 12.2.0 is the fix for CVE-2026-40192. discord.py 2.7.1 / yt-dlp 2026.3.17 are current and non-vulnerable. | Prior note (line 249) asked to verify CVE numbers — VERIFIED-REAL | None — pins are correct. |
| requirements.txt | 3,28 | LOW | Quality | Header says "Python 3.14+" while comment line 28 and the file mix `==` exact pins with open `>=` floors inconsistently (e.g. discord.py/aiohttp/Pillow exact; pypdf/python-docx/cryptography/requests/PyNaCl floored). Floored security deps (cryptography, requests, pypdf) are the ones that can drift into either old-vulnerable or untested-new. | New | Prefer compiled lockfile (pip-tools / uv) or `>=x,<y` ceilings on security-sensitive floors. |
| bot.py | 27 | MEDIUM | Lifecycle | `load_dotenv()` at import time (line 27) runs before `setup_smart_logging()` (line 134) AND before the optional-import warnings (lines 54,68,101,111). Any warning emitted by those import blocks routes through the default root handler and may be lost/duplicated once smart logging reconfigures handlers. Same root issue flagged in prior audit. | Prior bot.py:32-34/96 — PARTIALLY-FIXED (logger declared early at 34, but warnings still precede `setup_smart_logging`) | Call `setup_smart_logging()` before the optional-import blocks, or buffer early records. |
| bot.py | 1037-1039 | MEDIUM | Lifecycle/Secrets | Sentry is initialized inside `__main__` (good — deferred), but `init_sentry(environment=...)` runs at line 1037 BEFORE `validate_required_secrets()` at 1041. If the DSN is set and the bot then `sys.exit(1)` on a missing secret, a Sentry client/transport thread is spun up only to be torn down by exit. Minor ordering; could leak a half-initialized transport. | New | Move `init_sentry` after secret validation, or guard teardown. needs verification of init_sentry side effects. |
| bot.py | 466,668,689 | MEDIUM | ErrorHandling | `sync_commands` (466) catches only `discord.HTTPException`; `health_check` is unguarded; app/prefix error handlers wrap `ctx.send` but the `sync` global tree sync can raise `discord.app_commands.CommandSyncFailure` (a subclass of HTTPException — OK) but other exceptions from `tree.sync()` are uncaught and would bubble to `on_command_error` then potentially recurse. | Prior bot.py:423-424 — STILL-PRESENT (same narrow catch) | Broaden to `except discord.DiscordException` or log+report generically. |
| bot.py | 457-461 | MEDIUM | Security/RateLimit | `!sync` runs `await self.tree.sync()` (global sync) with only a 60s per-user cooldown and `is_owner()`. Global command sync is heavily rate-limited by Discord; a 60s cooldown lets an owner trigger it every minute and hit API limits. Owner-only mitigates abuse, but the cooldown is too low for a global sync. | Prior bot.py:414-424 — STILL-PRESENT | Raise cooldown (e.g. once per 30 min) or sync to a guild during dev. |
| bot.py | 162-164 | MEDIUM | Concurrency | `_write_pid_file()` writes `tmp` then `tmp_path.replace(PID_FILE)`. Atomic-rename is correct, BUT the duplicate-detection read of `_old_pid` (lines 144-150) happens at import time while the write happens later in `__main__` (line 1067) — two near-simultaneous startups can both read the same stale `_old_pid`, both pass `basic_startup_check`, then race the replace. The replace prevents a torn file but not two live bots. | New (improvement over prior, residual race) | Use an OS-level exclusive lock (e.g. `O_EXCL` create, or `msvcrt.locking`/`fcntl.flock`) for true single-instance. |
| bot.py | 564,1038 | MEDIUM | Config | `PROMETHEUS_PORT` (564) and `SENTRY_ENVIRONMENT` (1038) read directly via `os.getenv` instead of through `config.settings`, bypassing the centralized config + its `_safe_int_env`/validation. PROMETHEUS_PORT does have a local try/except, but the pattern fragments configuration. | Prior bot.py:510 (hardcoded 9090) — PARTIALLY-FIXED (now env-driven, but not via settings) | Surface these in `BotSettings`. |
| bot.py | 1059 vs 774-789 | MEDIUM | Validation | `validate_token` now requires `len>=70` and rejects `"your_token_here"` (good upgrade). But the token-format logic is duplicated across `validate_token` (bot.py) and `validate_required_secrets` (config.py:127) with divergent rules (config checks `your_token_here`; bot.py checks 3-dot + length). Two sources of truth for "is this token valid". | Prior bot.py:696-708 (len>=50) — FIXED-VERIFIED (now >=70); prior config.py:42 dup — STILL-PRESENT | Centralize token validation in `BotSettings`. |
| bot.py | 143-150 | LOW | Concurrency | TOCTOU between `PID_FILE.exists()` (144) and `read_text()` (146). `except (ValueError, OSError)` now covers the `FileNotFoundError` race (OSError subclass). | Prior bot.py:136-142 — FIXED-VERIFIED | None (already guarded). |
| bot.py | 140 | LOW | Resource | PID file now anchored to `Path(__file__).resolve().parent` — fixes prior CWD-relative bug. | Prior bot.py:132 — FIXED-VERIFIED | None. |
| bot.py | 279-286 | LOW | Quality | `bootstrap()` still creates dirs by relative name (`"temp"`,`"data"`,`"logs"`) rather than reusing `settings.temp_dir/data_dir/logs_dir` (which are project-root-anchored). Dual source of truth for the same dirs; can diverge if CWD != project root. | Prior bot.py:254-285 & config note — STILL-PRESENT | Use `settings.*_dir`. |
| bot.py | 284-286 | LOW | ErrorHandling | `bootstrap()` dir creation catches only `PermissionError` then `raise`; other `OSError` (read-only FS, bad name, ENOSPC) propagate without the helpful log. | Prior bot.py:263-267 — STILL-PRESENT | Catch `OSError` broadly with context. |
| bot.py | 335,339 | LOW | Quality | Class `MusicBot` mutates `start_time`/`_health_task`/`_metrics_started`/`_shutdown_task`/`_default_executor` as instance state but they are declared as class attributes with defaults. Comment at 341-345 acknowledges this is intentional-for-readability; functionally OK (first assignment shadows on the instance) but bug-prone. | Prior bot.py:307/406-409 — STILL-PRESENT (documented) | Initialize in `__init__`. |
| bot.py | 533 | LOW | Performance | `await self.change_presence(...)` runs on every `on_ready`, which fires on each reconnect — redundant API call + presence reset each reconnect. | Prior bot.py:481 — STILL-PRESENT | Guard with a one-time flag. |
| bot.py | 540-545 | LOW | Quality | `import orjson` inside `on_ready` purely as an availability check, re-run each `on_ready`. | Prior bot.py:488-493 — STILL-PRESENT | Do the check once at module load. |
| bot.py | 481,485 | LOW | Quality | `health_check` shows `self.latency` (single number) for an `AutoShardedBot`; per-shard `latencies` would be accurate. Uptime string drops days (`{hours}h…` with no day rollover) — long uptimes show "100h". | Prior bot.py:435/438 — STILL-PRESENT | Show per-shard / add day field. |
| bot.py | 525 | LOW | ErrorHandling | `_on_health_task_done` logs `logger.error("...: %s", exc)` without traceback. | Prior bot.py:469-473 — STILL-PRESENT | Use `exc_info=exc`. |
| bot.py | 637-644 | LOW | Performance | Manual `traceback.format_exception(...)` + `"".join(...)` instead of `logger.error(..., exc_info=error)`. | Prior bot.py:575-578 — STILL-PRESENT | Use `exc_info`. |
| bot.py | 720-724 | LOW | Performance | `await self.get_context(message)` is called for every non-bot message solely to classify metrics (command vs other) — full parse+lookup per message. | Prior bot.py:644-649 — STILL-PRESENT | Pre-filter on prefix before building context. |
| bot.py | 917 | LOW | Resource | Executor `shutdown(wait=False, cancel_futures=True)` — returns immediately; pending blocking I/O in worker threads may still run. Acceptable for shutdown but no bounded wait. | Prior bot.py:785-791 — STILL-PRESENT | Consider `wait=True` with timeout. |
| bot.py | 966,1017-1020 | LOW | Lifecycle | Resume path recreates the bot via `create_bot()` and re-reads `.env` with `override=True`; reusing/replacing a `commands.Bot` after `bot.run()` returns is officially unsupported by discord.py. The recreation is the correct workaround but repeated resumes churn instances. | Prior bot.py:826/836-842/849-850 — PARTIALLY-FIXED (explicit `bot_instance=` plumbing added; recreation correct) | Document the limitation; consider one-shot run. |
| bot.py | 451,817,etc. | LOW | ErrorHandling | Multiple `except Exception:` + `logger.exception` in setup_hook/graceful_shutdown — broad but appropriate for best-effort startup/teardown; noted for completeness. | Prior INFO pattern | None (acceptable). |
| bot.py | 170-263 | INFO | Quality | `print()` (not logger) used throughout `smart_startup_check`/`basic_startup_check` — these run before/around logging setup; intentional for console UX but bypasses log files. | Prior bot.py:162-168 etc. — STILL-PRESENT | Optionally mirror to logger. |
| bot.py | 335 | INFO | Quality | Class named `MusicBot` but handles AI/music/dashboard — misleading name. | Prior bot.py:303 — STILL-PRESENT | Rename (cosmetic). |
| bot.py | 380-397 | INFO | Concurrency | Windows SIGTERM handler captures the per-`setup_hook` `loop`; on resume a stale loop is captured but guarded by `is_closed()` + `RuntimeError` catch. Best-effort, acceptable. | Prior bot.py:339-348 — FIXED-VERIFIED (now guards is_closed + catches RuntimeError) | None. |
| bot.py | 493-516 | INFO | Concurrency | `_schedule_shutdown` cancels prior shutdown task and adds a done-callback to surface errors — improved structured handling vs prior. | Prior bot.py:463 (no done-callback) — FIXED-VERIFIED | None. |
| bot.py | 552-554 | INFO | Concurrency | Health task: `create_task` then `add_done_callback` on next line; prior audit's "callback added after create" nit. Synchronous raise before callback attach is effectively impossible for this coroutine. | Prior bot.py:498-502 — ACKNOWLEDGED (low residual) | Optional reorder. |
| config.py | 17-26 | LOW | Quality | `_safe_int_env` wraps both `os.getenv` and `int()` in the try; `os.getenv` never raises, so the try scope is wider than needed. Also no auto-base (`int(value, 0)`) so a hex env value silently falls to default. | Prior config.py:16-25 — STILL-PRESENT | Tighten scope; optionally support base 0. |
| config.py | 49 | LOW | Security | `anthropic_base_url` is NOT `repr=False`. A proxy URL embedding credentials (`https://user:pass@proxy/v1`) would surface in any `repr()` that isn't the custom one (e.g. dataclasses' `asdict`, or if the custom `__repr__` is bypassed). | Prior config.py:46 — STILL-PRESENT | Add `repr=False`. |
| config.py | 68-87 | LOW | Bug | Guild/channel/creator IDs default to `0` sentinel. Code comparing `guild.id == settings.guild_id_main` silently no-matches when unset. Discord IDs are positive 64-bit, so `0` can't collide with a real ID (safe), but the silent-disable can mask misconfiguration. | Prior config.py:65-72/83-85 — STILL-PRESENT | Consider `int|None` + explicit "feature disabled" log. |
| config.py | 124-139 | MEDIUM | Validation | `validate_required_secrets` only hard-fails on missing/placeholder DISCORD_TOKEN and the proxy-key-format case. ANTHROPIC_API_KEY is merely a *warning* (validate_optional_secrets:144). With `CLAUDE_BACKEND=cli` (env.example default) that's correct (CLI uses subscription auth, no key). But there is NO validation that when `CLAUDE_BACKEND=api`, a key is actually present — an `api`-mode deploy with no key starts up and only fails at first AI request. config.py doesn't read `CLAUDE_BACKEND` at all. | Prior config.py:121-132 — STILL-PRESENT | Add backend-aware validation: if backend==api require a key. needs verification of where CLAUDE_BACKEND is consumed. |
| config.py | 50 | LOW | Bug | `claude_model` defaults to `"claude-opus-4-7"`; no validation against real model IDs — a typo fails only at request time. | Prior config.py:47-49 — STILL-PRESENT | Validate against known set or document. |
| config.py | 107-122 | LOW | Bug | `__post_init__` creates dirs only when `BOT_RUNNING` is set. Importing `config` (tests/scripts) does NOT create dirs, so any module writing to `settings.data_dir` without ensuring existence fails outside the bot process. | Prior config.py:104-119 — STILL-PRESENT | Ensure dirs in `bootstrap()` for all entrypoints that need them. |
| config.py | 160-166 | LOW | Quality | Custom `__repr__` redacts secrets (good) but omits guild/channel IDs useful for debugging; partial picture. | Prior config.py:153-159 — STILL-PRESENT | Include non-secret IDs. |
| config.py | 200-201 | INFO | Quality | Module-level singletons `settings`/`feature_flags` instantiate at import (reads env). Tests must patch env before import. | Prior config.py:193-194 — STILL-PRESENT | None (documented pattern). |
| cogs/ai_core/imports.py | 55-101 | MEDIUM | Security | Guardrails fail-open: when `processing.guardrails` import fails, `validate_response`/`validate_response_for_channel`/`validate_input_for_channel` all return `(True, ...)` (allow). A `logger.critical` fires and a `[guardrails_unavailable]` warning is appended to the violations list — but if a caller ignores the warnings list, harmful content passes unfiltered in prod. Deliberate anti-DoS tradeoff, but a real silent-bypass surface if the import path ever breaks. | Prior imports.py:73-99 — STILL-PRESENT (unchanged, well-documented) | Consider fail-closed for high-risk channels, or a startup hard-fail flag; ensure all callers inspect the warning list. |
| cogs/ai_core/imports.py | 76-101 | MEDIUM | Bug/Consistency | Fallback `validate_response` signature returns a 3-tuple; `validate_input_for_channel` fallback returns `(True, user_input, 0.0, [])` — an EMPTY violations list (no `_GUARDRAILS_WARNING`), unlike the other two fallbacks which include the warning. So input-side guardrail loss is completely silent even to warning-aware callers. | New (subtle divergence from sibling stubs) | Append `[_GUARDRAILS_WARNING]` to the input fallback too. |
| cogs/ai_core/imports.py | 99-100 | LOW | Bug | Fallback `is_silent_block(response, expected_min_length=50)` ignores `expected_min_length`, only flagging empty strings — behavior diverges from the real implementation (which presumably checks min length). | Prior imports.py:98-99 — STILL-PRESENT | Honor the param or document divergence. |
| cogs/ai_core/imports.py | 284-294 | LOW | Bug | Fallback `msg`/`msg_en` return the raw `key` (e.g. `"rate_limited"`) and ignore `**kwargs`, so user-facing strings show the key / literal `{placeholders}` if localization import fails. | Prior imports.py:287-291 — STILL-PRESENT | Return `key.format(**kwargs)` best-effort. |
| cogs/ai_core/imports.py | 18-19 | LOW | Quality | `if TYPE_CHECKING: pass` empty block. | Prior imports.py:12-19 — STILL-PRESENT | Remove. |
| cogs/ai_core/imports.py | 9-16 | LOW | Quality | `logger = logging.getLogger(__name__)` declared at line 11, but `logger.critical` at line 69 may run before `setup_smart_logging()` if this module imports early — same early-log concern as bot.py. | Prior imports.py:62-71 — STILL-PRESENT | Ensure logging configured before guardrail import, or buffer. |
| cogs/ai_core/imports.py | 40-49 | LOW | Quality | URL-fetcher fallbacks (`extract_urls`/`fetch_all_urls`/`format_url_content_for_context`) are silent no-ops — no one-time warning like `_record_token_usage` has. | Prior imports.py:39-49 — STILL-PRESENT | Add a one-time warning. |
| cogs/ai_core/__init__.py | 19-60 | LOW | Quality | Importing `cogs.ai_core` eagerly loads ai_cog + cache + analytics + rag + summarizer + prompt_manager etc. (heavy, incl. RAG/embeddings init) just to touch the package. | Prior __init__.py:25-46 — STILL-PRESENT | Lazy-load heavy submodules. |
| cogs/ai_core/__init__.py | 48-56 | LOW | ErrorHandling | Second parallel guardrails fallback: here `validate_response = None` (and `detect_intent = None`) on ImportError, whereas `imports.py:76` provides a *callable* fail-open stub. Callers importing from `__init__` get `None` (→ TypeError if called); callers importing from `imports` get a function. Two incompatible fallback semantics for the same dependency. | Prior __init__.py:48-56 — STILL-PRESENT | Re-export the `imports.py` stub instead of `None`. |
| ci.yml | 69-72 | MEDIUM | CI/Security | `pip-audit --strict` audits the **installed** dependency set, not requirements floors. With pypdf 6.10.2 installed it passes, so it will NOT catch the dangerous `pypdf>=5.0.0` floor (HIGH above). The audit gives false confidence that the declared range is safe. | New | Also run `pip-audit -r requirements.txt` (audits declared ranges) or compile a lockfile and audit it. |
| ci.yml | 24,27,42,51,77,201,etc. | INFO | CI/Security | All third-party actions SHA-pinned with version comments; `permissions: contents: read` at top; `concurrency` cancel-in-progress; trufflehog `--only-verified`; no use of untrusted `${{ github.event.* }}` in run-steps; CI dummy creds use the `test-...-not-real` prefix consistently. Workflow security posture is strong. | New | None — exemplary. |
| ci.yml | 84-112 | LOW | CI | `python-test` runs only on Python 3.14 (matrix has one entry). ruff `target-version=py313` is intentionally one minor below runtime (documented). No 3.13 runtime test — acceptable given `requires-python>=3.14`, but means 3.13-only regressions are untested. | New | Document or add 3.14-only note (already commented). |
| ci.yml | 143-161 | INFO | CI | mypy regression gate is robust: captures real exit code, distinguishes crash (exit≥2) from type errors, coerces non-numeric counts to a failing sentinel. Good. | New | None. |
| pyproject.toml | 8,42 | INFO | Config | `requires-python>=3.14` vs ruff `target-version=py313` mismatch is intentional and documented (pre-commit ruff pin lag). Consistent with ci.yml comment. | New | None. |
| pyproject.toml | 100 | INFO | Config | Comment "RUF006 removed — dangling tasks now properly tracked in bot.py" — corroborated by the done-callback patterns now present in bot.py (`_schedule_shutdown`, health task). Consistent. | Cross-ref | None. |
| pyproject.toml | 122-124 | LOW | Security | Bandit `skips=["B101"]` (assert) globally; with `exclude_dirs` already covering tests, skipping B101 repo-wide also silences asserts in production code (asserts are stripped under `-O`). Minor. | New | Scope B101 skip to tests only, or accept. |
| pyproject.toml | 170 (cov) | LOW | CI | `--cov-fail-under=55` (ci.yml:170) is a low coverage floor for a production bot of this size. | New | Raise gradually. |
| env.example | (all) | INFO | Secrets | No real secrets committed — all values are placeholders (`your_*_here`) or `0`/empty, and risky keys (ANTHROPIC, GEMINI, SPOTIFY) are commented out so a verbatim `cp env.example .env` won't attempt auth with placeholders. Security-host guidance (HEALTH_API/WS bind hardening, fail-closed WS token, pickle opt-in) is thorough. | New | None — clean. |
| env.example | 34-35 | INFO | Config | `CLAUDE_MODEL=claude-opus-4-7` matches config.py:50 default and the model ID in MEMORY context. Consistent. | New | None. |
| .gitignore | 24,83,117-137 | INFO | Security | `.env`, `bot.pid`, `cookies.txt`, sensitive AI data (`faust_data.py`, `roleplay_data.py`, `unrestricted_channels.json`), `assets/RP/`, and `.claude/settings.local.json` all ignored. Parity with `.dockerignore`/`.cursorignore` is maintained. | New | None. |
| .dockerignore | 34-37 | INFO | Security | `.env` + `.env.*` glob excludes all env variants from image (with `!env.example`); `*.db`, persona data, logs/data/temp all excluded. SQLite + persona never baked into image. Solid. | New | None. |
| .dockerignore | 15 | LOW | Build | `*.so` intentionally NOT excluded (Rust libs needed) — but combined with `__pycache__` exclusion this is fine; just confirm no stray dev `.so` leaks in. | New | None (documented). |
| .cursorignore | 1-52 | INFO | Security | Excludes `.env*`, credentials dirs (`.anthropic`/`.openai`/`.gemini`), persona data, and all DB/data/logs from IDE indexing — prevents persona/secret leakage into IDE prompts. | New | None. |
| .claude/settings.local.json | 4-14 | LOW | Security | Local permission allowlist contains absolute user paths (`/c/Users/ME/...`, `c:/Users/ME/BOT Discord/...`) and a broad `Bash(cargo check *)` / `Bash(.venv/Scripts/ruff.exe check *)`. This file IS gitignored (.gitignore:128), so not a repo leak; the wildcard bash perms are a local-trust convenience. Flagged for awareness only. | New | None (gitignored). Keep wildcards minimal. |
| .devwatcher.example.json | 1-38 | INFO | Quality | Example dev-watcher config; ignore patterns include `.env`, `data/`, `.db`. No secrets. Note `auto_retry_on_crash:true` + `max_crash_retries:3` is dev-only. | New | None. |
| .pre-commit-config.yaml | 9,19,46 | INFO | Security | Hooks SHA-pinned with version comments (ruff v0.15.12, pre-commit-hooks v4.6.0, bandit 1.7.8). `check-added-large-files maxkb=1000`, `debug-statements`, `check-ast` present. Local `pytest-fast` skipped in CI (documented). Good. | New | None. |
| .pre-commit-config.yaml | 9 vs ci.yml:42 | LOW | CI | Local pre-commit ruff is v0.15.12 but the `.claude/settings.local.json` references ruff-0.15.0 and Zed bundles ruff-0.15.0 — minor version skew across local tooling could yield different lint results than CI. | New | Align local ruff to the pinned hook version. |
| dependabot.yml | 16-23 | LOW | Dependency | The `python-minor` group globs `"*"` for minor+patch only. **Major** security bumps (e.g. a future pypdf 7 fixing a CVE, or anthropic 1.x) are NOT grouped and rely on individual PRs — and the open-floor pins (`pypdf>=5.0.0`) mean dependabot may not even propose an upgrade since the floor is already satisfied. This interacts with the pypdf HIGH: dependabot won't auto-raise a `>=` floor. | New | Pin exact versions (so dependabot proposes bumps) or add `versioning-strategy: increase`. |
| dependabot.yml | (all) | INFO | Dependency | Covers pip, github-actions, gomod, cargo, docker, npm — comprehensive weekly cadence. | New | None. |
| version.txt / pyproject.toml:6 | 1 / 6 | INFO | Quality | `version.txt` (3.3.15) matches `pyproject.toml` version (3.3.15). Consistent. (Note `cogs/ai_core/__init__.py` docstring mentions v3.3.7 reorg — historical, fine.) | New | None. |

## Cross-cutting observations

1. **The pypdf floor is the single most important fix** and is a regression-of-omission: prior MASTER_TABLE H6 already called it out (asking for `>=6.8.0`) and it was never tightened. `pip-audit --strict` masks it because the *installed* version is patched. Pin to installed (`>=6.10.2`).
2. **defusedxml must become a declared dependency** — it is load-bearing security (DOCX XXE) but only transitively present. This is the prompt's hypothesis, confirmed against `document_extractor.py:46-60`.
3. **anthropic pin (≥0.99) does not match the installed 0.98.1** — reproducibility/drift bug; reconcile the environment and the pin, and consider moving toward 0.104.x.
4. **Guardrails fail-open in two places with three different semantics** (`imports.py` callable stub WITH warning for response, callable stub WITHOUT warning for input, `__init__.py` `None`). Unify. The input-side missing warning (imports.py:97) is a new, fully-silent bypass surface.
5. **Most prior G1 bot.py/config.py findings are FIXED-VERIFIED** in the current working tree: PID path anchoring, token length floor (50→70), Windows SIGTERM loop-staleness guard, shutdown-task done-callbacks, atomic PID write, executor tracked for shutdown, FFmpeg flag no longer leaked via env. The refactor materially improved lifecycle robustness.
6. **CI/CD security posture is strong** (SHA-pinned actions, least-priv permissions, trufflehog verified-only, robust mypy gate, no event-injection). The one gap is `pip-audit` auditing installed set rather than declared floors.
7. **No real secrets in any committed config** (env.example, ignores all verified). Token-detection regex exists in guardrails (`guardrails.py:165`) as output-side defense.

## Confirmation
All 19 assigned files read in full (every line): yes. Dependency CVE claims verified via web search on 2026-05-24.
