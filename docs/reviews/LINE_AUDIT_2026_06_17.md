# Line-by-line Audit — 2026-06-17 (v3.4.11)

Full-repo line-by-line audit of every non-test source file across all four stacks (Python, TypeScript, Rust, Go) plus UI/config, using a fan-out reader + independent adversarial verifier per file group (49 groups, 98 agents). Findings below are the **confirmed** set (each independently re-traced); rejected false-positives are omitted.

## Summary

| Severity | Count |
| --- | --- |
| critical | 0 |
| high | 2 |
| medium | 18 |
| low | 140 |
| improvement | 40 |
| uncertain (needs runtime/external) | 15 |

| Category | Count |
| --- | --- |
| correctness | 64 |
| error-handling | 28 |
| maintainability | 24 |
| concurrency | 23 |
| resource-leak | 14 |
| performance | 14 |
| bug | 10 |
| security | 10 |
| dead-code | 4 |
| doc | 4 |
| improvement | 2 |
| style | 2 |
| api-misuse | 1 |

## Confirmed findings

| # | File | Line | Severity | Category | Detail |
| --- | --- | --- | --- | --- | --- |
| 1 | `scripts/maintenance/migrate_to_db.py` | 165-173 | high | bug | **Re-running migration duplicates every row whose message_id is NULL** — The INSERT uses ON CONFLICT(channel_id, message_id) WHERE message_id IS NOT NULL, a PARTIAL conflict target. Legacy JSON history rarely stored a message_id (item.get("message_id") returns None for essentially all model responses), so those NULL-message_id rows are not in the partial unique index and can never be detected as conflicts. Every re-run INSERTs a fresh duplicate with an incremented local_id. Re-running after a partial failure is an explicitly supported recovery path, so a second invocation silently doubles a... |
| 2 | `utils/web/url_fetcher_client.py` | 82-94, 21, 52-53, 117-130 | high | bug | **SSRF-safe resolver on the client session blocks the default 'localhost' Go-service host, permanently disabling the Go backend** — __aenter__ attaches _SSRFSafeResolver to the ClientSession used to talk to the TRUSTED internal Go service (lines 82-92). The service base URL host comes from URL_FETCHER_HOST, default 'localhost' (line 21), giving base_url 'http://localhost:8081'. aiohttp only short-circuits the resolver for LITERAL IPs (connector.py:1096 is_ip_address); 'localhost' is a hostname, so the health check GET http://localhost:8081/health (lines 117-119) is resolved through _SSRFSaf... |
| 3 | `cogs/ai_core/cache/ai_cache.py` | 458-478 | medium | concurrency | **Background cleanup loop calls cleanup_expired() (which acquires a threading.Lock) directly on the event loop** — start_cleanup_loop() awaits asyncio.sleep then at line 467 calls self.cleanup_expired() synchronously on the event-loop thread. cleanup_expired() (line 450-456) acquires self._cache_lock and does an O(n) scan of up to max_size (5000) entries plus deletes, all on the loop thread, and contends the same threading.Lock that get()/set() hold from executor/handler threads. This can briefly stall the loop. |
| 4 | `cogs/ai_core/cache/token_tracker.py` | 498-506 | medium | error-handling | **Failed persist batch is silently dropped, permanently losing those usage records from the DB** — _flush_persist_queue() removes the batch from the queue at lines 473-474 BEFORE the DB write, then on any exception (498) logs a warning and drops the batch. A transient/recoverable failure (lock timeout, WAL contention) discards real analytics rows the in-memory cache no longer references. Since quota enforcement was removed this is analytics-only. |
| 5 | `cogs/ai_core/memory/consolidator.py` | 693-717 | medium | correctness | **Relationship contradiction check uses substring membership, producing false negatives and false positives** — In detect_contradictions, the regex group captures one of the fixed Thai relation tokens (พี่/น้อง/แฟน/เพื่อน/ศัตรู/แม่/พ่อ) into mentioned_rel, and line 709 tests `if mentioned_rel not in relation` against the stored relationship string. This is substring containment, not token equality. Concretely: stored relation 'แฟนเก่า' (ex-partner) with text mentioning 'แฟน' (current partner) -> 'แฟน' in 'แฟนเก่า' is True -> the meaningful ex-vs-current contradiction is silently suppressed.... |
| 6 | `cogs/ai_core/memory/rag.py` | 695-712 | medium | error-handling | **_get_all_memories_cached only catches TimeoutError; other DB errors abort the whole search** — Lines 696-701 wrap db.get_all_rag_memories in asyncio.wait_for and catch ONLY TimeoutError. get_all_rag_memories (database.py:1548) runs a real SQLite SELECT via get_connection(); a transient sqlite3.OperationalError ('database is locked') or any aiosqlite error propagates out of _get_all_memories_cached. hybrid_search awaits it unguarded at line 1423 with no try/except, and search_memory (1575) and the rest of the call chain have none either, so the exception bubbles to the caller and aborts th... |
| 7 | `cogs/ai_core/storage.py` | 742-768 | medium | correctness | **Dedup key uses second-resolution timestamps, collapsing two distinct same-second same-text turns** — _normalize_history_timestamp truncates to whole seconds (isoformat(timespec='seconds'), line 229). The just-in-DB dedup (742-748) and adjacent-batch dedup (760-764) use (role, content-hash, second-resolution timestamp, message_id-or-None) as the duplicate key. Model rows have message_id=None at save time (back-filled later by update_message_id), so for models the key reduces to (role, content-hash, second). Two genuinely distinct model replies with identical short text (e.g. 'ok', an emoji... |
| 8 | `go_services/health_api/main.go` | 625-647 | medium | correctness | **Graceful shutdown races with process exit — Shutdown() drain is truncated** — I traced the exit path. On SIGINT/SIGTERM the shutdown goroutine (line 625) calls metricsCancel() then server.Shutdown(ctx) (line 636). server.Shutdown() closes the listener, which makes the blocking server.ListenAndServe() at line 645 return http.ErrServerClosed immediately. At line 645 the condition `err != nil && !errors.Is(err, http.ErrServerClosed)` evaluates to false for ErrServerClosed, so log.Fatalf is skipped and main() falls through past line 647 to line 648 and returns — terminating the process. Meanw... |
| 9 | `go_services/url_fetcher/main.go` | 372-392 | medium | concurrency | **Panic in a FetchBatch worker goroutine crashes the whole process (Recoverer does not cover it)** — middleware.Recoverer (line 525) only recovers panics on the goroutine running the handler. The /fetch/batch handler spawns child goroutines at line 374 (go func(idx,u)) that call f.Fetch -> extractHTMLContent -> goquery.NewDocumentFromReader / DOM walk on attacker-controlled response bodies. No recover() exists inside that goroutine, so any panic on that path propagates to the runtime and terminates the entire url_fetcher process. The single /fetch path (line 582) is fine because it runs dir... |
| 10 | `native_dashboard/src-ts/shared.ts` | 271-303 | medium | security | **SVG data-URI blocklist in isSafeAvatarUrl is bypassable with the comma form (data:image/svg,...)** — The explicit SVG rejection only matches three prefixes: 'data:image/svg+xml', 'data:image/svg ' (trailing space), and 'data:image/svg;'. A data URI written as 'data:image/svg,%3Csvg%20onload%3D...%3E' (MIME directly followed by a comma, no '+xml', no ';', no space) does not match any of those three checks, so execution falls through to line 299 ('data:image/'.startsWith) and isSafeAvatarUrl returns true. The function's own comment (lines 266-270) states the intent is to reject ALL SVG data... |
| 11 | `scripts/bot_manager.py` | 327 | medium | correctness | **find_all_dev_watcher_processes uses substring matching, not the hardened exact-basename check** — find_all_dev_watcher_processes returns _find_processes(["python", "dev_watcher"]) (line 327) with no exact_basenames argument, so it falls back to pure substring matching on the joined cmdline (line 292). This is the very substring trap that find_all_bot_processes was deliberately hardened against (lines 318-322 pass exact_basenames=["bot.py"]; the docstring at 312-317 explains why). Any python process whose cmdline merely contains the substring 'dev_watcher' anywhere — a log-path argument, a... |
| 12 | `scripts/dev_watcher.py` | 595-654 | medium | concurrency | **check_for_crash holds _lock across the multi-second crash-retry sleep, stalling the observer** — check_for_crash acquires self._lock at line 597 and, on the auto-retry branch, calls time.sleep(self.config.crash_retry_delay) (line 623, default 5s) and then _start_bot_unlocked — all while still holding _lock. The watchdog observer thread's _handle_change also acquires self._lock (line 768) for the hash read-modify-write, and start_bot acquires it (line 476). So for the full crash_retry_delay window the observer thread blocks on _lock and file-change processing is stalled. The start_bot path... |
| 13 | `scripts/install_all.ps1` | 19 | medium | bug | **Tls13 enum member throws on Windows PowerShell 5.1 / .NET Framework < 4.7, aborting the installer before any download** — Line 19 evaluates [Net.SecurityProtocolType]::Tls13 unconditionally. The script header (lines 2-3) documents launching via `powershell -ExecutionPolicy Bypass -File scripts\install_all.ps1` — i.e. Windows PowerShell 5.1, the in-box shell. On 5.1 backed by .NET Framework older than 4.7 (the default on un-patched Windows 10), the Tls13 enum member does not exist, so member resolution raises a terminating error. This statement runs at top level (line 19) before any try/ca... |
| 14 | `scripts/maintenance/reindex_db.py` | 201-204, 277-279 | medium | bug | **Index recreation can fail because the partial unique index on the OLD table still exists, or fail to copy partial-index WHERE clauses correctly is fine but auto-index names collide** — Indexes are collected from sqlite_master WHERE type='index' AND tbl_name='ai_history' AND sql IS NOT NULL (201-203), filtering out sql IS NULL which correctly skips auto-generated indexes (PRIMARY KEY / UNIQUE-constraint indexes have NULL sql). Those collected index CREATE statements reference the table name 'ai_history'. After DROP TABLE ai_history (266) and RENAME ai_history_new -> ai_history (267), recre... |
| 15 | `scripts/maintenance/reindex_db.py` | 218-222 | medium | correctness | **View-detection LIKE '%ai_history%' over-matches and can churn/abort on unrelated views** — Views tied to ai_history are gathered with sql LIKE '%ai_history%', a substring match over the whole CREATE VIEW text. It captures any view that mentions the string anywhere (e.g. ai_history_archive, ai_history_summary, an alias ai_history_count, or a comment). A false-positive view that does NOT depend on ai_history is dropped+recreated for no reason, and if its recreate fails (line 313) it sets index_recreation_failed forcing sys.exit(2) on an otherwise successful reindex. |
| 16 | `scripts/maintenance/reindex_db.py` | 383-400 | medium | error-handling | **Post-commit verification and VACUUM run unguarded after data is already durable** — After commit() succeeds (line 342) and _ignore_sigint() exits, SIGINT is restored. The verification SELECT (383-385) and VACUUM (394-400) run with no try/except. A Ctrl-C or aiosqlite error there raises out of reindex_ai_history(), so no [DONE] banner prints even though the rename/commit already durably succeeded. An operator seeing a traceback may wrongly re-run the destructive DROP/RENAME or restore from backup. VACUUM is purely optimization. |
| 17 | `utils/database/database.py` | 2831-2892, 2945-2990, 2992-3018 | medium | correctness | **Dashboard message edit/delete/pin/like/truncate paths never refresh the exported per-conversation JSON, leaving stale on-disk data** — save_dashboard_message() calls self._schedule_dashboard_export(conversation_id) at line 2703 so data/db_export/dashboard_chats/<id>.json stays current. But update_dashboard_message (2831), update_dashboard_message_pin (2871), update_dashboard_message_liked (2885), delete_dashboard_message (2945), delete_dashboard_messages_after (2980) and edit_and_truncate_dashboard_message (2992) all mutate dashboard_messages WITHOUT scheduling a dashboard export. I confi... |
| 18 | `utils/database/migrations.py` | 182-198 | medium | correctness | **Migration is not atomic: each await conn.execute on DDL auto-commits, so a mid-migration crash records no version row and leaves partial schema** — The code (lines 182-189) executes each parsed statement followed by the INSERT into schema_version, then a single commit at line 190. The block comment at 172-181 states this is deliberate (avoiding an explicit BEGIN so PRAGMA foreign_keys=OFF in migration 010 is honored and DDL autocommits). The consequence is acknowledged: a crash between the first DDL statement and the INSERT leaves applied schema changes with NO schema_version row, so the ... |
| 19 | `utils/web/url_fetcher.py` | 517-528, 542, 557 | medium | correctness | **Placeholder strings returned as content get injected into AI context as real page body** — Tracing is accurate: the non-text (line 518), Content-Length-too-large (528), streamed-overflow (542), and binary (557) branches all `return url, "[...]"` — a truthy placeholder as the content element. fetch_all_urls unpacks this into (url, title=url, content=placeholder) (lines 683-685), and format_url_content_for_context treats any truthy content as page body and emits it under `--- {title} ({url}) ---` (lines 708-720). So the bracketed marker is injected into the AI context. However these are exp... |
| 20 | `utils/web/url_fetcher_client.py` | 76-98 | medium | resource-leak | **__aenter__ failure after session creation leaks the ClientSession** — The pattern is real in principle: __aenter__ creates self._session (line 89) then awaits self._check_service() (line 93) before return self (line 94); if the code between creation and return raised, async-with would not invoke __aexit__ and the session would leak per fetch_url/fetch_urls call. However the only awaited statement, _check_service(), wraps its single network call in try/except Exception (lines 116-130) and cannot propagate a normal exception — it always returns a bool. So the leak is only reachable via a Ba... |
| 21 | `Makefile` | 30 | low | maintainability | **Makefile test/test-cov targets can hit the documented pytest -v pipe-freeze** — Confirmed: Makefile line 30 'test' target runs 'python -m pytest tests/ -v --tb=short' — explicit -v on top of pyproject addopts '-v' (pyproject.toml line 16). 'test-cov' (line 39) inherits addopts -v too. CLAUDE.md explicitly warns 'Never run raw pytest -v — it can hang' and the run_tests.ps1 wrapper exists to strip it. Only 'test-fast' (line 36) neutralizes it via --override-ini="addopts=". A contributor using the advertised 'make test' can trip the exact freeze. Severity is low because it is a contributor-t... |
| 22 | `bot.py` | 911-912 | low | error-handling | **Health-task await suppresses all exceptions including programming errors** — with contextlib.suppress(asyncio.CancelledError, Exception): await _health_task swallows every exception while awaiting the cancelled health task. The comment justifies suppressing a stored pre-cancel exception, but catching bare Exception also hides genuine bugs (e.g. a TypeError in the loop's finally/cleanup) with zero logging, unlike the rest of graceful_shutdown which logs via logger.exception. |
| 23 | `cogs/ai_core/ai_cog.py` | 535-541 | low | concurrency | **reset_ai skips locking when no processing lock pre-exists for the channel** — reset_ai fetches lock = processing_locks.get(channel_id) and only acquires when isinstance asyncio.Lock (535-537). When no lock pre-exists (first turn for the channel), the wipe runs with NO lock held. Between the .get() returning None and the awaited delete_history at 557, a queued on_message can run process_chat (logic.py:1262-1265 creates+acquires a fresh lock) and start generating; that turn's later save_history can re-insert rows AFTER delete_history removed them. Narrow window — only when the channel's ver... |
| 24 | `cogs/ai_core/ai_cog.py` | 914-918 | low | correctness | **Per-channel custom rate limit enforced only on webhook path, not on @mention/reply or DM chats** — The owner-set per-channel limit (get_custom_channel_limit + check_rate_limit(channel_config_name(...))) is consulted ONLY in the webhook command path (914-918). The guild @mention/reply path (1071-1074) and the DM path (988-995) check only gemini_api/gemini_global (and ai_user/ai_guild for DM) and never the custom bucket. rate_limiter.set_channel_limit's own docstring (rate_limiter.py:506-507) says 'Enforcement happens in ai_cog's rate-limit gauntlet via channel_config_name', but that wiring... |
| 25 | `cogs/ai_core/ai_cog.py` | 959-962 | low | correctness | **DM command detection misses leading-whitespace prefix (minor inconsistency with guild path)** — In _handle_dm_message line 961 tests message_content.startswith(prefix_tuple) on the RAW content, whereas the guild path (lines 1064-1068) also strips leading user mentions and tests content_without_mention. A DM beginning with whitespace before the prefix (e.g. ' !chat hi') will NOT be recognized as a command and is forwarded verbatim to process_chat as plain chat text. Owner-only gate + DMs-can't-mention-the-bot keep impact tiny; purely the leading-whitespace edge. |
| 26 | `cogs/ai_core/api/api_failover.py` | 584-595 | low | maintainability | **Probe-failover task is anchored in _pending_close_tasks, conflating two task kinds** — Confirmed by reading lines 583-595: the auto-failover task spawned from the health-probe failure path is added to self._pending_close_tasks purely as a GC anchor (the inline comment at 584-590 acknowledges it 'is not a close'). It is functionally correct today — add_done_callback(self._pending_close_tasks.discard) removes it on completion and nothing iterates _pending_close_tasks expecting only client-close tasks. It is a latent coupling risk: a future targeted close-drain that cancels/awaits entries of... |
| 27 | `cogs/ai_core/api/api_handler.py` | 455-525 | low | resource-leak | **detect_search_intent issues an un-timed Claude call that can hang the search-intent path** — client.messages.create() at line 502 is called without an explicit timeout= and without asyncio.wait_for, so the only deadline is the anthropic SDK's default. With max_tokens=10 the SDK's max_tokens-based duration estimate is tiny, so the ValueError('Streaming required') trap that the non-streaming path (1069-1086) guards against does NOT fire here, and the request is sent. There is therefore no application-controlled deadline on this classification call. |
| 28 | `cogs/ai_core/api/api_handler.py` | 968-974 | low | correctness | **Streaming retry placeholder reports the wrong (pre-increment) attempt number** — After a retryable timeout/transient failure, the placeholder is edited at line 971 with f'...retrying (attempt {stream_attempt})...' while stream_attempt still holds the value of the attempt that just FAILED; it is only incremented at line 974 afterward. So on the first failure the user sees 'attempt 1' for the second attempt about to run. The exhaustion log at line 978 uses stream_attempt - 1, so the two user/log messages disagree by one. |
| 29 | `cogs/ai_core/api/dashboard_chat.py` | 293-300 | low | bug | **DB-history loop uses bracket access on msg['role']/msg['id'] while the rest of the function uses .get()** — Line 294 uses msg['role'], line 299 uses msg['id'], and line 287 uses db_msgs[-1]['role'], whereas adjacent code (286, 295, 296, 298) defensively uses msg.get(...). If get_dashboard_messages returns a row missing one of those keys, the bracket access raises KeyError, which the broad except at line 302 catches and silently demotes the WHOLE turn to the untrusted, timestamp-less frontend-history fallback with only a warning. |
| 30 | `cogs/ai_core/api/dashboard_chat.py` | 638-654 | low | correctness | **Per-round token reset means a tool round that emits no usage_metadata zeroes input_tokens for the window display** — input_tokens/output_tokens are reset to 0 at the start of every tool round (lines 496-497) and only overwritten when a chunk carries a truthy usage value (lines 642-645). After the loop, total_input_tokens = input_tokens (line 653) takes ONLY the final round's value. If the final round's stream surfaces no usage_metadata (or prompt_token_count=0), total_input_tokens becomes 0 and the char/3 heuristic (844-852) kicks in even though earlier rounds had real counts — under-repo... |
| 31 | `cogs/ai_core/api/dashboard_chat_claude.py` | 1566-1620, 1685 | low | bug | **AI-edit retry replays the full response, may duplicate partial text in the client bubble mid-stream** — Confirmed at code level: handle_ai_edit_message_claude's _stream_once (1566-1620) pushes each text_delta to the client as a type:chunk frame and appends to full_response. On a retryable/timeout failure, _consume_claude_edit_stream (1651-1688) sends a retry-notice chunk (1660-1667/1676-1683) and then calls _reset_stream_state() (1685), which zeroes full_response. Unlike the chat handler (which keeps full_response accumulating and prefills a continuation, lines 1023/1033), the edit path r... |
| 32 | `cogs/ai_core/api/dashboard_chat_claude.py` | 996-1020, 1180, 1216 | low | correctness | **Retry-notice banner shown to the user is never part of the saved response (live vs persisted mismatch)** — Confirmed: on a retryable/timeout failure the chat handler builds retry_notice (996 / 1005-1007) and sends it as a type:chunk frame (1014-1020) which the frontend appends to the live streaming bubble. retry_notice is intentionally NOT added to full_response (the prefill reads partial=full_response at 1023 and never includes the notice). So the DB-persisted full_response (saved at 1177-1183) and the stream_end full_response (1216) omit the banner, while the live bubble showed it inter... |
| 33 | `cogs/ai_core/api/dashboard_chat_claude_cli.py` | 1056-1080 | low | correctness | **Document routing keys off (kind=='binary' OR binary-ext); a mislabeled .pdf/binary file is silently dropped** — _save_inline_documents enters the binary branch when `kind == 'binary' or is_binary_ext` (line 1056). A .pdf with kind='text' and a plain UTF-8 string body has is_binary_ext True, so it takes the binary branch, finds the body is not a `data:` URL, and `continue`s (line 1058-1059) — the attachment vanishes with only the generic per-document warning. Conversely a text file mislabeled kind='binary' with a plain string body also drops. Robustness, not a security hole (the extension ... |
| 34 | `cogs/ai_core/api/dashboard_chat_claude_cli.py` | 3428-3434, 3322-3343 | low | correctness | **AI-edit path puts persona in the prompt body instead of a cacheable system prefix (perf parity gap)** — handle_ai_edit_message_claude_cli builds argv with allow_read_for_images=False, no system_prompt_file, and no enable_web (line 3428-3434), placing the full persona in edit_prompt body (line 3323). This is internally consistent and intentional (one-shot fresh session), but it forgoes the cacheable --append-system-prompt-file prefix the chat path uses, so a long persona pays full body tokens on every /edit with no warm prompt-cache prefix. Not a bug — a parity/perf gap. |
| 35 | `cogs/ai_core/api/dashboard_chat_claude_cli.py` | 3567, 3513-3525 | low | concurrency | **AI-edit calls delete_session_file AFTER releasing the per-conversation lock** — The edit's chat-session wipe (delete_session_file at 3567) runs outside the try/finally that releases edit_lock at 3515. A chat turn queued on the same conversation lock can acquire it the instant the edit releases, read _CONVERSATION_SESSIONS[conversation_id] (still present until delete_session_file pops it at 3567), and spawn `--resume <old session>` against the pre-edit server-side context. It self-heals (resumed turn replays slightly stale content, or hits the _StaleSessionError fresh-rebuild path if the .... |
| 36 | `cogs/ai_core/api/dashboard_chat_claude_cli.py` | 673, 678, 891 | low | maintainability | **Lock-eviction safety relies on the private asyncio.Lock._waiters attribute** — _get_conversation_lock and reset_session gate eviction on `getattr(lock, "_waiters", None)`. On Python 3.14 (verified locally) asyncio.Lock._waiters is None when no waiters are queued and a deque when some are, so the current `not getattr(...)` truthiness test is correct TODAY. The valid forward-compat concern is fail-open: if a future asyncio rename/removes `_waiters`, getattr's None default makes the code treat a lock with pending waiters as evictable, popping it and handing the next caller a fresh Lock — rei... |
| 37 | `cogs/ai_core/api/dashboard_handlers.py` | 1457-1515 | low | correctness | **Non-string / non-dict `preferences` payload silently wipes stored preferences** — In handle_save_profile, raw_prefs = profile_data.get('preferences') and sanitized_prefs starts at None. Only the isinstance(raw_prefs, str) (line 1460) and isinstance(raw_prefs, dict) (line 1465) branches assign it. If a client sends preferences as a list/number/bool, neither branch runs, sanitized_prefs stays None, and save_dashboard_user_profile(preferences=None) persists None, clearing the user's existing value while still returning a profile_saved success with preferences_truncated=False. Unlike the dict... |
| 38 | `cogs/ai_core/api/dashboard_handlers.py` | 1790-1819 | low | correctness | **History response budget counts only `content` length, understating true frame size** — handle_load_ai_history caps the cumulative reply at MAX_HISTORY_RESPONSE_CHARS by subtracting only len(content) per row (lines 1792, 1795), ignoring per-row JSON overhead (role, id, local_id, message_id/user_id snowflake strings, timestamp, key names, and JSON escaping expansion of content). With up to 2000 rows the fixed per-row overhead alone is on the order of a few hundred KB, plus escaping expansion, so the serialized frame exceeds the intended ~20MB content budget. As the module comment (lines 31-... |
| 39 | `cogs/ai_core/api/discord_chat_claude_cli.py` | 529-558 | low | concurrency | **_send_overlimit_warning cooldown check/record straddle an await, allowing two stacked interactive views under a concurrent over-limit race** — The cooldown read `last = _OVERLIMIT_LAST_WARN.get(key, 0.0)` (line 535) and the record `_OVERLIMIT_LAST_WARN[key] = now` (line 558) genuinely straddle `await send_channel.send(...)` (line 544). _send_overlimit_warning is called at line 931, which is AFTER `async with lock` has exited (the block ends at 918; cleanup and the warning run unlocked). So if two over-limit turns for the same channel reach this function and interleave at the await, both r... |
| 40 | `cogs/ai_core/api/discord_chat_claude_cli.py` | 722-733 | low | error-handling | **_flatten_contents_to_prompt is called outside the per-attempt try, so a malformed contents item raises an uncaught exception that leaves the placeholder stuck on '💭 กำลังคิด...'** — _flatten_contents_to_prompt (called at 722, and 1018 in the non-stream path) runs inside `async with lock` but BEFORE the per-attempt `try` at 750. Inside it, `current.get('parts', [])` (348) and `item.get('role', ...)`/`item.get('parts', [])` (323/326) assume every contents element is a dict. If logic.py ever passes a non-dict tail item (e.g. a bare string), `.get` raises AttributeError, which is not caught b... |
| 41 | `cogs/ai_core/api/discord_chat_claude_cli.py` | 764-783 | low | concurrency | **Cancel watcher polls every 0.5s, so the channel lock can stay held up to ~0.5s after a user cancels** — The finding itself walks back its premise: it confirms behaviour is correct (session popped, lock released, no leak) and only objects to a sub-0.5s latency between on_text flipping `aborted` and `_cancel_watcher` cancelling the runner. on_text sets `aborted=True` and mutes deltas (line 640-644); the watcher (line 771-779) polls cancel_flags every 0.5s and calls `_runner.cancel()`, which the runner's finally turns into proc.kill. So at worst the subprocess lives ~0.5s longer than the use... |
| 42 | `cogs/ai_core/api/document_extractor.py` | 122-126 | low | correctness | **Binary non-PDF/DOCX data: URLs with kind_hint != 'text' return None with no log line** — In extract_from_payload, a file whose ext is not .pdf/.docx, whose data_field is a 'data:' URL, and whose kind_hint is not 'text' satisfies neither the PDF/DOCX branches nor `if kind_hint == 'text' or not data_field.startswith('data:')` (line 122) — both clauses false — so control falls through to `return None` (126) silently. e.g. an attached .rtf or an image-typed binary data: blob. The docstring (100-102) says the caller logs and moves on, and extract_and_persist does log a generic 'Extraction fail... |
| 43 | `cogs/ai_core/api/document_extractor.py` | 318-320 | low | security | **DOCX entry-name traversal guard misses Windows drive-absolute paths (C:\evil)** — The guard at 318-320 rejects entries starting with '/' or '\\' or containing a '..' path segment, but does not reject a Windows drive-absolute name like 'C:\\evil.xml' or 'C:/evil.xml' (no leading slash, no '..'). The finding itself correctly notes there is NO disk-write sink today — every entry is only streamed into memory via zf.open(...).read() for the byte-cap preflight (327-339) and python-docx reads from the BytesIO, never extracting to disk. So this is purely defense-in-depth hardening for a hypotheti... |
| 44 | `cogs/ai_core/api/document_extractor.py` | 402-403 | low | correctness | **Pre-normalise text slice at MAX_EXTRACTED_CHARS*4 can drop tail content without a truncation marker when normalise then shrinks below the cap** — _extract_text slices data_field[: MAX_EXTRACTED_CHARS*4] at 403 with no marker, then _normalise (which can only shrink) runs, then a marker is appended only if len(text) still exceeds MAX_EXTRACTED_CHARS (407-408). For an input between MAX_EXTRACTED_CHARS and MAX_EXTRACTED_CHARS*4 whose normalisation removes enough to drop under the cap, the user gets silently-truncated text with NO marker. The 4x headroom means normalise would have to remove >7... |
| 45 | `cogs/ai_core/api/ws_dashboard.py` | 1046-1055, 1079-1088 | low | dead-code | **Inner _client_inflight >= 2 concurrency gate is unreachable dead defense for message/ai_edit_message** — Both 'message' and 'ai_edit_message' are dispatched only through the read-loop gate at lines 929-967, which counts not-done tasks in _client_tasks for the client and rejects at current_inflight >= 2, so at most 2 handle_message tasks ever exist per client. Each task increments _client_inflight (lines 1055/1088) only after it starts running. The 2nd of at most 2 tasks can therefore read _client_inflight as at most 1, so the inner `if inflight >= 2` checks at lines 1047 and 1080 can neve... |
| 46 | `cogs/ai_core/api/ws_dashboard.py` | 357-359, 376-378 | low | performance | **Blocking socket.connect_ex with 1s timeout runs on the event loop in async _ensure_port_available** — _ensure_port_available is async but performs synchronous, blocking socket.connect_ex((WS_HOST, WS_PORT)) with sock.settimeout(1) directly on the event loop (357-359, and again at 376-378 inside the retry loop). If the port is filtered/dropping (not just refused), connect_ex can block for the full 1 second per probe, freezing the shared event loop. Impact is limited because it runs once at server start, hence low severity. |
| 47 | `cogs/ai_core/cache/ai_cache.py` | 316-318 | low | concurrency | **Miss counter incremented even when the entry expired in the window between lock releases** — In get(), a fuzzy match that exists at find_similar() time but expires by the time the lock is re-acquired at line 300-301 falls through to line 317 and is counted as a miss. This is a stats-accuracy nit, not a functional defect: the function still correctly returns None for an expired entry. |
| 48 | `cogs/ai_core/cache/ai_cache.py` | 798-821 | low | concurrency | **flush_l2_pending snapshots the pending set once; futures created after the snapshot are not awaited** — flush_l2_pending() takes pending = list(_l2_pending_futures) at line 806 then awaits only that snapshot via asyncio.wait. A _persist_to_l2 that submits a future after the snapshot but before loop close is not awaited. On shutdown this is a narrow window and the subsequent checkpoint() partially mitigates durability. |
| 49 | `cogs/ai_core/cache/token_tracker.py` | 313-316 | low | performance | **init_from_db inserts each row into up to three keyed lists then trims all keys after the full load** — During replay (291-317) each row appends to user:/channel:/guild: lists with no per-append cap; the MAX_RECORDS_PER_KEY trim runs once at 321-323 after the loop. A single hot channel/guild list can transiently hold up to max_rows (10,000) before being trimmed to 5000. Brief memory spike bounded by max_rows, not a leak. |
| 50 | `cogs/ai_core/cache/token_tracker.py` | 452-464 | low | concurrency | **Eager batch flush threshold is best-effort; queue can briefly exceed PERSIST_BATCH_SIZE** — _persist_usage appends under _persist_lock, reads queue_len, releases the lock (452-454), then flushes at 463 if queue_len >= PERSIST_BATCH_SIZE. Because the lock is released between the read and the flush, concurrent calls can each see the threshold and the queue can momentarily exceed it, or a competing flush empties it first. Documented as approximate and bounded by the periodic backstop. |
| 51 | `cogs/ai_core/commands/debug_commands.py` | 63 | low | error-handling | **ai_debug token estimate only catches ImportError, not a runtime failure of estimate_tokens** — Lines 62-67 wrap `history_manager.estimate_tokens(history)` in a try that only excepts ImportError. If estimate_tokens raises at runtime (e.g. TypeError/KeyError on a malformed history entry) it propagates and aborts the whole !ai_debug command, defeating the per-panel degrade hardening explicitly applied to the Cache (92-96) and RAG (117-122) panels which catch (AttributeError, KeyError, TypeError). The `len(history)*50` fallback only fires on ImportError. !ai_debug is owner-only and history is... |
| 52 | `cogs/ai_core/commands/memory_commands.py` | 146 | low | correctness | **!remember with no fact never reaches the localized usage hint (handled by global handler instead)** — remember_fact declares `*, fact: str` (keyword-only, required, no default), and forget_fact declares `*, query: str`. discord.py raises MissingRequiredArgument before the body runs on a bare `!remember`/`!forget`, so the `not fact`/`not query` branch of the guards at lines 152/209 is dead for the empty case and the tailored Thai hints (`!remember ผมชื่อ John`) are never shown. NOT a crash: bot.py's global on_command_error (line 658) catches MissingRequiredArgument and replies with a gener... |
| 53 | `cogs/ai_core/commands/memory_commands.py` | 302 | low | correctness | **view_memories renders stored fact content with active markdown while remember/forget escape backticks** — remember_fact (line 187) and forget_fact (line 226) neutralize backticks (`.replace('`', 'ʻ')`) before echoing self-authored content. view_memories interpolates `fact.content[:80]` raw into a non-code embed field value (line 302). _screen_injection blocks [system]/ignore-previous markers but does not strip backticks, markdown, or `[label](url)` link syntax, so a user can store a fact and have it rendered as active markdown when they later run !memories. This is purely a cosmetic/consi... |
| 54 | `cogs/ai_core/commands/server_commands.py` | 1209 | low | security | **cmd_read_channel skips the caller read-permission check when user is None** — The privacy gate (1231-1242) runs only `if user is not None`. When user is None the function reads and echoes the target channel's last N messages into origin_channel with no caller check, contradicting its docstring invariant ('only let the user read channels they can read'). In the live AI path tool_executor always passes user=user and is administrator-gated upstream, and the bot's own Forbidden is still caught, so there is no current leak. Confirmed at low as a fail-open default that diverges from cmd_list_ch... |
| 55 | `cogs/ai_core/commands/server_commands.py` | 807 | low | correctness | **cmd_set_channel_perm prefers a role over a member on name collision** — Line 807: `target = discord.utils.get(guild.roles, name=target_name) or find_member(guild, target_name)`. When a guild has a role and a member sharing the same name, the role always wins; the member can never be targeted by name. The overwrite then silently applies to the wrong entity type. Gated upstream by administrator (tool_executor:682) and the perm set is allowlisted to non-dangerous perms, so impact is limited to a confusing 'set it on the user but nothing changed' result. Confirmed at low. |
| 56 | `cogs/ai_core/commands/server_commands.py` | 996 | low | security | **cmd_list_roles lacks the per-handler caller gate present on sibling listing commands** — cmd_list_channels (978) fails closed unless the caller is a Member with view_channel; cmd_list_members (1023) and cmd_get_user_info (1077) require manage_guild. cmd_list_roles (996-1005) accepts `_user` and never inspects it — it dumps the full role roster with IDs to any caller reaching the handler. In the live AI-tool path this is gated upstream by the administrator check (tool_executor:682), so there is no current leak; the inconsistency is the missing defense-in-depth. Role names are also visible ... |
| 57 | `cogs/ai_core/core/message_queue.py` | 279-296 | low | maintainability | **pop_pending_messages leaves an empty list entry that cleanup_unused_locks then keeps the lock alive for** — pop_pending_messages sets self.pending_messages[channel_id] = [] (an empty list) rather than deleting the key. cleanup_unused_locks at line 486 skips lock removal only when self.pending_messages.get(channel_id) is truthy; an empty list is falsy, so this does NOT block lock cleanup — good. However the empty-list entry itself is never reclaimed by pop, so pending_messages accumulates one empty-list entry per channel ever seen, bounded only by MAX_CHANNELS eviction. This is a minor, bo... |
| 58 | `cogs/ai_core/logic.py` | 1239-1253 | low | maintainability | **Dedup key added before circuit-breaker / queue early-outs relies on every early return remembering to remove it** — check_and_add at line 1239 runs before the circuit-breaker check (1244), the lock.locked() queue path (1268), and the lock-acquire timeout/cancel paths. Each of those early returns must individually call remove_request, and they currently all do (1248, 1283, 1303, 1309); the main body relies on the inner finally (2176) plus the outer finally (2191). I traced every path between 1239 and the main try at 1319 and confirmed none currently leak the key. The first auditor explicit... |
| 59 | `cogs/ai_core/logic.py` | 1419 | low | improvement | **RAG memory search runs on placeholder text for empty/attachment-only messages** — Confirmed: display_message is set to the literal placeholder '[User sent image(s) without text]' (1375) or '[User wants to continue the conversation]' (1378) for empty/attachment-only messages, and rag_system.search_memory(display_message, ...) (1419) plus entity_memory.search_entities(display_message[:100], ...) (1444) then run a semantic/prefix search on that synthetic string. This retrieves memories matching the placeholder words rather than user intent — noise plus a wasted embedding round-trip per attac... |
| 60 | `cogs/ai_core/logic.py` | 1614-1642 | low | performance | **_compress_cache stored on chat_data risks persistence coupling and grows the session dict** — Confirmed: chat_data['_compress_cache'] = {'src_len':..., 'history': history} (1633-1636) stores a second full copy of the compressed history slice inside the live per-channel session dict, invalidated only when len(history) changes. I checked storage.save_history — it reads/writes only chat_data['history'] (storage.py 876, and the grep showed no _compress_cache reference), so there is NO persistence/serialization leak of the cache (the auditor's 'serialization coupling' worry is unfounded — conf... |
| 61 | `cogs/ai_core/logic.py` | 1852-1866 | low | correctness | **Cancelled streaming turn persists user message but never the partial reply, desyncing memory from what Discord shows** — In the was_cancelled branch the user_item is always appended/saved, but the model turn is saved only when `not use_streaming` (line 1852). A cancelled streaming turn streamed a partial reply into Discord that is intentionally NOT written to history, so its Discord message_id is never recorded and that visible message is invisible to edit/delete mirroring and to future prompts. The DESYNC and lost-message_id sub-claims are real. HOWEVER the first auditor's central justif... |
| 62 | `cogs/ai_core/media_processor.py` | 443-444 | low | correctness | **load_character_image uses naive substring match on character names** — `if char_name.lower() in message_lower` does an unanchored substring match, so a short character name ('rin','al','mai') matches inside unrelated words ('drinking','always','email'), loading the wrong reference image into the Claude payload. character_tags.replace_character_names deliberately anchors with `^[ \t]*(...)[ \t]*$` for exactly this reason; this loader has no word-boundary guard. The match is non-deterministic in dict order and the first false hit wins. This is corroborated by the repo's own prior audit (G8_... |
| 63 | `cogs/ai_core/memory/entity_memory.py` | 249-346 | low | dead-code | **add_entity joined-transaction branch (_own_tx=False) is unreachable** — in_tx = getattr(conn, 'in_transaction', False) is always False on entry, so _own_tx is always True and the False branches (skip-commit at 312/337, skip-rollback at 343) never execute. Confirmed by the connection manager: utils/database/database.py:1258-1260 performs conn.rollback() on every checkout, then runs only PRAGMA foreign_keys=ON (a PRAGMA, not DML) before yielding. Under sqlite3's legacy isolation, a PRAGMA/SELECT does not open an implicit transaction, so conn.in_transaction is False. Additionally get_write_c... |
| 64 | `cogs/ai_core/memory/long_term_memory.py` | 262-281 | low | correctness | **extract_facts can emit duplicate facts; the 'extractor de-dupes its own output' claim is false** — extract_facts appends a Fact for every regex match across all patterns with no de-dup pass. Overlapping identity patterns (lines 142 and 152 both capture a Thai name) or preference+generic captures can yield two Fact objects with identical content from one message. The FactExtractor docstring and the process_message comment (line 425, 'the extractor additionally de-dupes its own output first') assert de-duplication the extractor never performs. Storage is saved only because process_message's... |
| 65 | `cogs/ai_core/memory/long_term_memory.py` | 596-597 | low | resource-leak | **Cursor from conn.execute is never explicitly closed in get_user_facts** — `cursor = await conn.execute(query, params)` then `rows = await cursor.fetchall()` leaves the aiosqlite cursor unclosed when the `async with db.get_connection()` block exits and the connection is returned to the pool. Because connections are pooled and reused, the cursor lingers on a recirculated connection rather than being promptly released. |
| 66 | `cogs/ai_core/memory/memory_consolidator.py` | 507-511 | low | correctness | **_generate_summary extracts topics from both roles' text while key sentences use only user turns** — all_text (line 507) joins content from all messages (user + AI) and feeds _extract_topics, while key_sentences (517-524) draw only from user turns. On a Thai-first roleplay bot where AI replies are typically far longer than user prompts, high-frequency assistant prose can dominate key_topics over the user's actual subject matter, skewing the extracted topics toward AI verbiage. |
| 67 | `cogs/ai_core/memory/memory_consolidator.py` | 507-527 | low | performance | **_extract_topics runs synchronous (pythainlp) tokenization on the event loop over the full untruncated message join** — _generate_summary builds all_text by joining the full DB `content` of up to MAX_MESSAGES_PER_CONSOLIDATION (500) messages with no per-message length cap (unlike consolidator._history_to_text which truncates each part to 500 chars). all_text is then passed to _extract_topics, which on line 602 calls pythainlp word_tokenize synchronously. For 500 long roleplay turns this can be hundreds of KB of Thai text segmented on the asyncio event loop in one shot, blocking all other c... |
| 68 | `cogs/ai_core/memory/rag.py` | 1017-1023 | low | bug | **MAX_RAG_REBUILD truncation permanently strands rows beyond the cap from the semantic (FAISS) index** — On a full rebuild, all_memories is truncated to MAX_RAG_REBUILD (100k) at line 1023 before FAISS build. get_all_rag_memories has no ORDER BY (database.py:1565-1567) so which rows are dropped is nondeterministic. After _index_built is set, the disk-load reconcile (_reconcile_sync, 941-970) only adds rows MISSING from FAISS, never the dropped ones, so for a >100k-row table the truncated rows are invisible to semantic search until/unless a future full rebuild includes them. Mitigating: the ... |
| 69 | `cogs/ai_core/memory/rag.py` | 1531-1542 | low | correctness | **age_days uses .days (truncated int) while _calculate_time_decay uses fractional days** — At line 1542 age_days = (now - created).days is integer-truncated, so a memory younger than 24h reports age_days=0, disagreeing with the fractional age (total_seconds()/86400) used by _calculate_time_decay at 1253 and by MemoryMetadata.calculate_importance. MemoryResult.age_days is annotated float (139) but receives an int here and 0 at 1529, so the reported age is coarser than the decay actually applied to the score. The created_at.replace at 1535 assuming str is guarded by the surrounding except at ... |
| 70 | `cogs/ai_core/memory/rag.py` | 1628-1641 | low | error-handling | **Legacy _linear_search has the same narrow TimeoutError-only DB catch as the hot path** — The legacy fallback _linear_search wraps db.get_all_rag_memories in asyncio.wait_for at 1636-1638 and catches only TimeoutError (1639). Any sqlite3/aiosqlite error (e.g. 'database is locked') propagates out instead of degrading to []. This is the same defect class as finding #1 but on the legacy path; the first auditor mentioned it in passing inside finding #1 but did not list it as its own item. Lower severity than #1 because _linear_search is the legacy method, not the per-message hot path, but it i... |
| 71 | `cogs/ai_core/memory/rag.py` | 938-972 | low | concurrency | **Reconcile snapshots existing_ids/all_memories outside the index lock; concurrent add_memory can duplicate** — existing_ids = set(faiss_index.id_map) is read at 939 without the FAISS lock, and _reconcile_sync iterates the all_memories captured at load. A concurrent add_memory between disk-load and the to_thread reconcile (the documented race window) can add a vector to FAISS that reconcile then also adds from its stale view, producing one duplicate FAISS entry. Impact is genuinely low: add_single does not dedupe by design, RRF + the result cap (limit) absorb the dup, and it self-heals on t... |
| 72 | `cogs/ai_core/memory/state_tracker.py` | 111-118 | low | doc | **RLock comment overstates the concurrency it guards against** — The comment claims the RLock serialises 'Discord events that may execute concurrently on the asyncio loop.' In production all access is on a single event-loop thread and no locked method awaits inside its critical section, so coroutines cannot interleave through the synchronous bodies — the lock provides no protection there. cleanup_old_states is also called synchronously, not via to_thread (ai_cog.py:409). The lock IS load-bearing for the multi-threaded dev probe (scripts/dev/probe_ai_fixes.py:210) and is harmless/defensive o... |
| 73 | `cogs/ai_core/processing/intent_detector.py` | 168-173,179 | low | correctness | **Intra-intent and inter-intent ties resolved silently by pattern/dict insertion order** — Within an intent, best_sub_cat is replaced only on strict `score > max_score` (line 168), so equal-scored patterns keep the first-listed sub_category. Across intents, `max(intent_scores, key=lambda x: intent_scores[x][0])` (line 179) returns the first key on a tie, which is INTENT_PATTERNS dict insertion order (GREETING, QUESTION, COMMAND, ROLEPLAY, EMOTIONAL). A message equally matching a 0.9 GREETING and a 0.9 ROLEPLAY action_marker always classifies GREETING. Deterministic but order-dependent and u... |
| 74 | `cogs/ai_core/response/webhook_cache.py` | 205-218 | low | maintainability | **Package response/__init__ does not re-export the lifecycle/healthcheck helpers webhook_cache exports** — webhook_cache.__all__ exports get_cached_webhook, invalidate_webhook_cache, set_cached_webhook, invalidate_webhook_cache_on_channel_delete, start/stop_webhook_cache_cleanup and webhook_cache_healthcheck, but response/__init__.py only re-exports get_cached_webhook, invalidate_webhook_cache and set_cached_webhook. The package surface is therefore inconsistent with the submodule's stated public API. Note this is mitigated in practice: ai_cog.py imports start/stop_webhook_cache_cleanup and... |
| 75 | `cogs/ai_core/session_mixin.py` | 189-211 | low | maintainability | **Stale 'Guardrails not available' comment + per-call import (no functional bug)** — The try/except wraps `from .imports import is_unrestricted` and only catches ImportError. Per imports.py:86-107, is_unrestricted ALWAYS resolves (to a real impl or a stub returning False), so the import never actually fails and is_unrestricted never raises here. The except comment 'Guardrails not available' is stale (guardrails were removed; this is the unrestricted module). The import also runs on every get_chat_session call. None of this is a functional defect — behavior is correct — it is a readability/e... |
| 76 | `cogs/ai_core/storage.py` | 1065-1067 | low | correctness | **JSON history loader drops rows with roles other than user/model, diverging from the DB loader** — _load_history_json skips any item whose role is not in ('user','model') via `continue` (1066-1067). The DB loader path in load_history (979-989) faithfully returns whatever role string is stored, applying no such filter. The same logical history therefore differs between the JSON fallback and the DB backend: a row with role 'system'/'assistant'/legacy role is present from the DB but vanishes from JSON. Since the JSON path is the only persistence when the DB is unavailable, this asymmetry can ... |
| 77 | `cogs/ai_core/storage.py` | 24-30 | low | correctness | **json_dumps silently discards kwargs when routing to orjson** — json_dumps delegates to stdlib json only when kwargs has a truthy 'indent' or ensure_ascii is False (28); otherwise it calls orjson.dumps(obj) and DROPS all kwargs (30). A future caller passing sort_keys=True, default=..., or separators=... would have those silently ignored, producing output that differs from json.dumps semantics. Current in-file callers (888, 910) pass only ensure_ascii=False, indent=2 and are safe, so this is a latent footgun rather than an active bug. |
| 78 | `cogs/ai_core/storage.py` | 312-321 | low | concurrency | **invalidate_all_cache() drops generation bumps for channels not currently in _history_cache** — invalidate_all_cache() bumps _cache_generations only for keys present in _history_cache (the `for cid in _history_cache` loop, 317-318). load_history() snapshots `generation` under the lock (958) before its awaited DB read, then re-checks `_cache_generations.get(channel_id, 0) == generation` before caching (994-995). For a channel NOT currently in _history_cache (never cached, or evicted by _enforce_cache_size_limit which also pops its generation at 171), invalidate_all_cache() does not bump its... |
| 79 | `cogs/ai_core/tools/tool_executor.py` | 399-424 | low | correctness | **read_channel resolves the target channel differently than cmd_read_channel (ID-first vs name-first)** — The executor resolves a numeric channel_name by ID first (guild.get_channel) then falls back to name. But cmd_read_channel (server_commands.py:1223-1225) resolves by NAME first (discord.utils.get(text_channels, name=...)) and only falls back to ID. If a guild has a text channel literally named e.g. "123456" AND a channel whose id is 123456, the executor's permission check would validate the id-channel while cmd_read_channel would actually read the name-channel. cmd_read_channel performs... |
| 80 | `cogs/ai_core/voice.py` | 180-183 | low | api-misuse | **get_voice_status calls Music cog private _gs without guarding AttributeError** — music_cog._gs(guild_id) (lines 180,188) reaches into another cog's private API. bot.get_cog('Music') only confirms the cog is loaded. _gs exists today (cogs/music/cog.py:214) but if a future Music-cog refactor renames it or _gs raises (e.g. for an unknown guild), the AttributeError/exception is unguarded and propagates out of get_voice_status, failing the entire status string instead of degrading. getattr is correctly used for current_track but not for the _gs call itself. Low severity: cross-cog coupling wit... |
| 81 | `cogs/music/cog.py` | 1087-1102 | low | resource-leak | **Loop-replay play() failure disables loop and falls through to queue logic without deleting the orphaned current_track temp file** — In the loop branch, if voice_client.play(player) raises, the except at 1061 calls player.cleanup() (kills the ffmpeg subprocess) and re-raises; the re-raise is caught at 1087/1090 which sets loop=False and falls through to the normal queue logic at 1103. The previous current_track['filename'] is never safe_delete()d on this path: loop was just disabled so the after-callback that would delete it never runs (play failed), and at line 1174 current_track is overw... |
| 82 | `cogs/music/cog.py` | 1558-1602, 1688-1706 | low | error-handling | **fix command disconnects before validating user-in-voice / file-exists, leaving stale current_track when reconnect path bails** — fix() stops+disconnects at 1559-1560 BEFORE checking ctx.author.voice (1576) or file existence (1595). On the file-gone early return (1595-1602) the bot has already reconnected (channel.connect at 1578 succeeded) so the finally-block's deferred-cleanup guard (vc.is_connected() True at 1704) does NOT run cleanup, leaving current_track populated while the bot sits connected+idle. On the author-left return (1579-1584), the bot is left disconnected, but the prior di... |
| 83 | `cogs/music/cog.py` | 2752-2756 | low | error-handling | **_resolve_in_use lacks the per-item Path.resolve() guard that every other temp-sweep path has** — _resolve_in_use does `{str(Path(n).resolve()) for n in names}` in a set comprehension with no per-item try/except. If any current_track['filename'] is a path that Path.resolve() rejects (OSError/ValueError on OS-illegal chars / reserved names), the whole comprehension raises and the exception propagates out of asyncio.to_thread back to cleanup_cache's callers: the clearcache command (2824, surfaces an unhandled error to the user) and on_ready startup (2881, unhandled at startup). Both _periodi... |
| 84 | `cogs/music/cog.py` | 895-902 | low | correctness | **play_next retry budget allows 11 _play_next_once invocations, not 10** — Loop: retries=0; call _play_next_once; if not retry_next return; if retries>=max_retries(10) return; retries+=1. _play_next_once is invoked at retries=0..10 before the guard trips at retries==10, i.e. 1 initial attempt + 10 retries = 11 invocations. Each invocation pops at most one bad track, so the practical worst case is 11 dropped tracks instead of an intended 10. Harmless in practice. |
| 85 | `cogs/music/queue.py` | 263-311 | low | concurrency | **load_queue docstring overstates lock protection vs save_queue's unlocked DB write** — load_queue's docstring claims the lock prevents 'a save scheduled mid-load' from racing the deque assignment, but save_queue (lines 179-205) deliberately releases the same lock before db.save_music_queue/clear_music_queue. The lock only serializes the in-memory snapshot, so a save that already snapshotted+released can issue its DB write concurrently with load_queue's DB read; the docstring's promise does not hold at the DB layer. |
| 86 | `cogs/music/queue.py` | 353-368 | low | concurrency | **JSON-migration DB write awaited while holding per-guild lock (inverse of save_queue convention)** — load_queue's JSON-fallback branch awaits db.save_music_queue (line 359) and asyncio.to_thread(filepath.unlink) (line 368) while still inside `async with self._get_lock(guild_id)` (opened at 269). This contradicts save_queue's documented convention (174-178) of releasing the lock before slow I/O. A concurrent add_to_queue/save_queue for that guild would block on disk/network during migration. |
| 87 | `cogs/music/views.py` | 120-136 | low | error-handling | **stop_button acknowledges interaction AFTER stop(); send_message can also fail leaving callback to raise** — In stop_button the only interaction acknowledgement is send_message at line 136, placed AFTER voice_client.stop() (134). Beyond the stop() concern: there is no try/except around the send_message itself. If the interaction token already expired or was acknowledged elsewhere, send_message raises discord.HTTPException/InteractionResponded, which propagates as an unhandled error AND means the subsequent message.edit (143-145) to disable buttons still runs but the user sees a failed inte... |
| 88 | `cogs/music/views.py` | 132-134 | low | error-handling | **stop_button calls voice_client.stop() unguarded after clearing queue** — stop_button clears queue/state (127-130) then calls voice_client.stop() (134) unguarded, BEFORE acknowledging the interaction via send_message (136). pause_resume_button wraps pause()/resume() in try/except discord.ClientException and skip_button guards stop() behind is_playing()/is_paused(). If stop() raises (e.g. client mid-disconnect), the exception propagates out of the callback after state was already mutated and before the interaction is acknowledged, surfacing as an unhandled interaction error. |
| 89 | `go_services/health_api/main.go` | 242-266 | low | security | **No rate limiting / lockout on bearer-token authentication failures; length short-circuit leaks token length via timing** — requireBearerToken correctly fails closed on empty token (lines 245-248) and uses subtle.ConstantTimeCompare (line 259) to avoid byte-level timing leaks. However there is no per-IP throttle or failed-auth backoff in front of the auth-protected Group (line 384). Default bind is 127.0.0.1 (line 298), which bounds exposure, but HEALTH_API_HOST/GO_HEALTH_API_HOST is an explicitly supported config (lines 293-296); if set to a routable interface, an on-network attacker can ... |
| 90 | `native_dashboard/package.json` | 3 | low | maintainability | **package.json version (2.0.0) diverges from the rest of the dashboard (3.4.11)** — Confirmed: native_dashboard/package.json line 3 is '"version": "2.0.0"' whereas the sibling Cargo.toml and tauri.conf.json (same app, same dir) are both 3.4.11 and the repo-wide pyproject.toml is 3.4.11. The npm manifest is not consumed for the runtime artifact (Tauri uses tauri.conf.json/Cargo.toml), so no functional break, but it is misleading for provenance/release auditing. |
| 91 | `native_dashboard/scripts/create_desktop_shortcut.py` | 44-47 | low | error-handling | **Removing an existing shortcut is unguarded and can abort with a traceback** — At lines 45-46 `if shortcut_path.exists(): shortcut_path.unlink()` runs before the COM try/finally (which begins at line 53/54). If the existing .lnk is locked (open in Explorer/AV) or the user lacks delete permission, unlink raises PermissionError/OSError that escapes create_shortcut_via_pythoncom uncaught and surfaces as a raw traceback — inconsistent with the function's pattern of returning False with a friendly message on every other failure (ImportError 12-14, missing exe 36-38, COM failure 69-71). |
| 92 | `native_dashboard/src-ts/app.ts` | 1170-1182 | low | correctness | **Log level classification and filtering use whole-line substring match, conflating message text with severity** — Lines 1172-1174 set level via line.includes('ERROR')/'WARNING'/'DEBUG', and line 1176 filters via line.includes(filter). A record whose message text incidentally contains 'ERROR' (e.g. an INFO line 'no ERROR found') is colored as error and also passes a filter==='ERROR' selection despite being INFO. Conversely the level cascade is order-dependent: a line containing both 'WARNING' and 'ERROR' is classed error. Produces misclassified colors and incorrect filtered views. Cosmetic,... |
| 93 | `native_dashboard/src-ts/app.ts` | 156-162 | low | correctness | **Ctrl+1..6 navigation keyed off e.key digit range fails on layouts where top-row keys are symbols** — Line 156 checks e.ctrlKey && e.key >= '1' && e.key <= '6' and parseInt(e.key). On AZERTY and similar layouts the unmodified top-row keys emit symbols (&é"'(-), so e.key is not a digit and Ctrl+digit navigation silently no-ops. Minor UX papercut, not a crash; US/most layouts unaffected. |
| 94 | `native_dashboard/src-ts/app.ts` | 1672-1697, 1887-1908 | low | maintainability | **setupCropEventListeners runs twice per open; closeCropModal leaves boundStartDrag/boundStartDragTouch non-null and cropListenersAttached=true** — I traced every path. crop-area is obtained via document.getElementById('crop-area') and is a persistent DOM node never cloned or recreated, so the asymmetry the finding describes is real but produces no leak and no double-binding: on each open the detach block at 1672-1681 removes whatever bound handlers are currently live (boundStartDrag/Touch are still set after a close, the three document handlers were already removed in closeCropModal so the... |
| 95 | `native_dashboard/src-ts/app.ts` | 838-883 | low | error-handling | **updateStatus uses Promise.all so one endpoint's rejection skips updating both the status panel and charts** — Line 838-841 awaits Promise.all([cachedStatus ?? invoke('get_status'), cachedDbStats ?? invoke('get_db_stats')]). If get_status resolves but get_db_stats rejects (transient IPC/Mutex contention on one endpoint), Promise.all rejects, the catch at 881 only logs, and NEITHER the status badge/buttons NOR the charts update for that tick even though fresh status was available. On a busy backend a single endpoint hiccup stalls the whole panel for one interval. Self-heals next tick; low i... |
| 96 | `native_dashboard/src-ts/chat-manager.ts` | 829-857 | low | maintainability | **Lexical const declarations directly inside the unbraced `error` case clause** — `const errorMsg` (829) and `const stuckErrorMsg` (856) are declared at the top level of the `case 'error':` clause, which is NOT wrapped in `{ }` (unlike neighbors provider_updated, profile, message_edited which all use braces). Lexical declarations in a case clause share the switch block scope, putting these identifiers in the TDZ for every other case. No runtime bug today (each case breaks first), but it is a footgun and what ESLint no-case-declarations flags. |
| 97 | `native_dashboard/src-ts/chat-manager.ts` | 927-935 | low | error-handling | **document_saved handler can throw on a malformed frame (char_count undefined)** — The docs array is cast from the WS frame via `as Array<{...; char_count: number}>` (line 927) with no runtime validation. If a document entry omits char_count, line 931 `d.char_count.toLocaleString()` throws TypeError (undefined has no toLocaleString) for the single-doc path, and line 935 `s + d.char_count` yields NaN (rendered as the literal 'NaN chars') for the multi-doc path. Other handlers (chunk, stream_start, error) explicitly guard such fields with `typeof ... === 'string'`, and lines 2339/2346 already... |
| 98 | `native_dashboard/src-ts/chat/conversation-list.ts` | 141-187 | low | resource-leak | **renderTags rebinds tag listeners each call but relies on innerHTML GC; in-progress tag input is lost on re-render** — renderTags() replaces host.innerHTML wholesale and then attaches fresh click/keydown listeners to the newly created chips and #chat-tag-add input. Because innerHTML replacement detaches the old subtree, the old listeners are garbage-collected, so there is no listener leak. However, any re-render triggered while the user is mid-typing in the #chat-tag-add input destroys the input element, discarding the partially typed tag and keyboard focus. |
| 99 | `native_dashboard/src-ts/chat/formatter.ts` | 206-240 | low | bug | **Table placeholder consumes the leading newline without restoring it, gluing a table to the preceding paragraph line** — The table regex matches (?:^\|\n) OUTSIDE the capture group, consuming the single newline that separates a paragraph line from a table directly below it, and emits the placeholder with NO surrounding newline (unlike lists at 249/257 which emit \n...\n). The \n->\<br> pass at 261-262 therefore never sees a boundary, and after restore at line 266 the preceding text and the table are glued. |
| 100 | `native_dashboard/src-ts/chat/formatter.ts` | 206-240 | low | bug | **Two adjacent tables separated by a single newline are merged into one malformed table** — When an AI emits two markdown tables with only a single newline between them (no blank line), the body-row quantifier (?:\\|.+\\|(?:\n\|$))+ greedily consumes the SECOND table's header and separator rows as data rows of the FIRST table. rows[1] (the first table's separator) drives alignment for everything, and the second table's '\| B \|' header and '\|---\|' separator render as ordinary <td> cells inside the first table instead of as a second <table>. Reproduced: regex returns a single match spannin... |
| 101 | `native_dashboard/src-ts/chat/image-attach.ts` | 146-159 | low | concurrency | **Selecting more files than the cap emits one near-identical toast per rejected file** — On 'change', files are iterated into attach()/documents.attach() which check this.images.length + this.pendingCount against the cap and showToast() once PER rejected file. Selecting 10 images at once spams up to N 'Maximum 5 images allowed' toasts. The fileInput.value='' reset is synchronous but harmless because FileReader closures retain the File. |
| 102 | `native_dashboard/src-ts/chat/search.ts` | 112-122 | low | correctness | **focus() count display recomputed twice in perform(); step() can show a stale count if matches were cleared by a re-render** — perform() calls focus(0) which writes '1 / N', then perform() writes the same string at lines 102-104 (harmless redundancy). More substantively, when renderMessages() replaces container.innerHTML the <mark> nodes are detached but this.matches[] still holds them; a subsequent step()/focus() calls scrollIntoView on a node no longer in the document (silent no-op) and shows a misleading count. |
| 103 | `native_dashboard/src-ts/chat/ws-client.ts` | 133-142 | low | resource-leak | **withTimeout leaves the underlying invoke() promise pending when the 8s timeout wins** — When the timeout branch resolves first, the original invoke() promise p is never cancelled (Tauri invoke has no abort) and remains pending. The .catch(() => '') prevents unhandled rejection and .finally clears the timer when p settles, so there is no crash — only a lingering microtask under sustained backend deadlock. |
| 104 | `native_dashboard/src-ts/chat/ws-client.ts` | 258-261 | low | correctness | **Oversized-frame guard only applies to string frames; non-string (Blob/ArrayBuffer) frames bypass the size check** — The MAX_MESSAGE_LENGTH guard is gated on typeof event.data === 'string'. A binary frame (Blob/ArrayBuffer) would skip the size check and flow into JSON.parse(event.data). On the text-only protocol this never triggers, hence low severity. |
| 105 | `native_dashboard/src-ts/shared.ts` | 257 | low | security | **asset://tauri:// host check is skipped when the parsed hostname is empty (triple-slash form)** — For asset:// / tauri:// URLs the localhost restriction is guarded by `if (parsed.hostname && parsed.hostname.toLowerCase() !== 'localhost') return false;`. A URL like 'asset:///avatars/x.png' (three slashes) parses to an empty hostname, so the truthiness guard is false and the localhost check is skipped entirely. The path-based checks (drive-letter, '..', and the required 'avatars/' prefix) still run and confine the result, so this is not exploitable into arbitrary file read, but the host allo... |
| 106 | `native_dashboard/src-ts/shared.ts` | 764 | low | correctness | **animateNumber auto-decimals miscounts for values in exponential string form** — decimals is auto-derived via `to.toString().split('.')[1]?.length`. For small non-integer targets that JS stringifies in exponential notation (e.g. 0.0000001 -> '1e-7'), toString() contains no '.', so split('.')[1] is undefined and decimals resolves to 0. The animation and final render then show '0' instead of the intended fractional value. The dashboard's displayed metrics (memory MB, counts) are unlikely to reach sub-1e-6 magnitudes, so impact is minimal, but the derivation is not robust for such inputs. |
| 107 | `native_dashboard/src/bot_manager.rs` | 299-303 | low | correctness | **exe_under_base uses a raw string prefix match that can match a sibling directory** — process_belongs_to_us accepts a process as 'ours' if its canonicalized exe path string starts_with(base_canon). Because this is a plain string prefix test (not a path-component boundary check), a base_path of e.g. `c:\users\me\bot` would also prefix-match an unrelated interpreter living under `c:\users\me\bot-other\.venv\...`. Combined with kill_orphan_bot_processes, a python+bot.py process whose interpreter happens to live under such a sibling directory could be classified as belonging to this install an... |
| 108 | `native_dashboard/src/bot_manager.rs` | 799-807 | low | resource-leak | **start_dev stores the dev-watcher Child before confirming the PID file write, but a failed write only logs** — self.dev_watcher_child is set, then fs::write of the PID file is attempted; on write failure it only eprintln's and proceeds. After a dashboard restart there is no PID file and no in-memory handle, so the watcher cannot be reaped. |
| 109 | `native_dashboard/src/main.rs` | 412-419 | low | correctness | **Frontend error rate limiter is a fixed window, not sliding — allows ~40 writes across a boundary** — Lines 412-419: if `now.duration_since(rate.0).as_secs() < 1` and `rate.1 >= 20`, drop; else if window expired, reset to `(now, 1)`. This is a fixed window keyed off the first error's Instant. 20 errors at t=0.99s plus 20 at t=1.01s both pass (the second batch resets the window), allowing ~40 writes in a short span around the boundary. The struct comment (line 28) and the function comment (lines 401-405) call it a 'Rolling rate-limit window', which overstates the smoothing. The DoS-mitigati... |
| 110 | `native_dashboard/src/main.rs` | 440 | low | error-handling | **create_dir_all result discarded before opening the error log** — Line 440: `let _ = std::fs::create_dir_all(&log_dir);` discards the result. If directory creation fails (a file exists at that path, or permissions), the subsequent OpenOptions::open at line 498 fails and DOES return a mapped error ('Failed to open error log'), so there is no silent data loss — but the more specific directory-creation failure is hidden and surfaces 60 lines later as a less precise message. |
| 111 | `native_dashboard/src/main.rs` | 475-505 | low | concurrency | **Log rotation guard is dropped before the append, so a write can interleave with a rotation** — The error_log_rotation guard (`_rot_guard`, line 478) is scoped to the rotation if-block and dropped at line 495; the OpenOptions append at lines 498-505 runs without it. Thread A can rotate (rename live log -> .old, line 488) after thread B has already evaluated the size check but before B opens its append handle, so B may either write into the just-renamed .old file or recreate the live log. Result is at most a single error line landing in dashboard_errors.log.old — benign, no corruption (appe... |
| 112 | `native_dashboard/src/main.rs` | 714 | low | correctness | **dotenv value quote-stripping uses trim_matches and can over-strip / mismatch quotes** — Line 714: `let val = val.trim().trim_matches('"').trim_matches('\'');`. trim_matches removes ALL leading/trailing occurrences of the given char, and the two calls are independent (double then single), so (1) a value with repeated boundary quotes is over-stripped and (2) a mismatched pair like KEY="value' has BOTH the leading double-quote and trailing single-quote removed, unlike a real dotenv parser which only strips a matched surrounding pair. The unit test at lines 1138-1148 only exercises single mat... |
| 113 | `native_dashboard/src/main.rs` | 803-804 | low | security | **Saved-path TOCTOU: bot.py existence and allowed-root check use separate resolutions, and raw (non-canonical) path is returned** — resolve_production_base_path reads bot_path.txt into saved_path (line 802), checks `saved_path.join("bot.py").exists()` against the raw string (line 803), then is_under_allowed_root(&saved_path) independently canonicalizes saved_path (line 778), and on success returns the RAW saved_path (line 805) — not the canonical form. So the existence check, the root-confinement check, and the later `python bot.py` spawn in BotManager each resolve the same string independe... |
| 114 | `native_dashboard/ui/index.html` | 204-493 | low | bug | **Unclosed <div class="chat-layout"> — modals nested inside chat layout, relying on parser auto-close at </section>** — Line 204 opens `<div class="chat-layout">` (indent 16). Its children are the `<aside class="chat-sidebar">` (206-226) and `<div class="chat-main">` (229-347). Line 347's `</div>` (indent 20) closes `chat-main`, returning the parser to inside `chat-layout`. The four modals at lines 349-492 (indent 16) are then emitted, and line 493 `</section>` closes the section WITHOUT a `</div>` ever closing `chat-layout`. A stack-trace of div tags confirms the document is off-by-one (21... |
| 115 | `native_dashboard/ui/index.html` | 770 | low | doc | **Dashboard About box shows stale version 3.4.4 (repo is 3.4.11)** — Confirmed: index.html line 770 reads '<p>Version 3.4.4 - Enhanced Edition</p>' while pyproject.toml (v3.4.11), native_dashboard/Cargo.toml (3.4.11) and tauri.conf.json (3.4.11) all declare 3.4.11. I also read scripts/maintenance/sync_doc_stats.py: its DOC_PATTERNS sync list (README.md, CLAUDE.md, native_dashboard/README.md, docs/DEVELOPER_GUIDE.md, docs/INSTALL.md) has NO entry for native_dashboard/ui/index.html, so this literal is never reconciled and drifts every release. |
| 116 | `native_dashboard/ui/styles.css` | 2227 | low | maintainability | **Undefined --accent-color custom property on loading spinner** — `.loading-spinner` sets `border-top-color: var(--accent-color, #7c3aed)`. --accent-color is never defined, so the spinner's leading edge always uses the hardcoded fallback #7c3aed. After the v2 uplift the dark-theme --accent-purple is #ab78ff (line 5368), so the spinner edge is visibly off-palette and does not track theme switches. |
| 117 | `native_dashboard/ui/styles.css` | 2255 | low | maintainability | **Undefined --bg-hover custom property on load-more button hover** — `.load-more-btn:hover` uses `background: var(--bg-hover, #333350)`. --bg-hover is not defined, so the hover always falls back to the hardcoded dark-slate #333350, which does not track the light theme (shows a dark slate hover even in light mode). Cosmetic only. |
| 118 | `native_dashboard/ui/styles.css` | 300 | low | maintainability | **Undefined --accent-primary custom property (survives only via fallback)** — The skip-link background reads `var(--accent-primary, #5865f2)`. No `--accent-primary` token is defined anywhere (the :root blocks at lines 11, 4209, 5350, 5690 define --accent-purple/--accent-pink/--accent-violet/etc but never --accent-primary). So the skip link always renders the literal fallback Discord-blurple #5865f2, off-brand for the purple/pink theme and the only place blurple appears. Functional via fallback; the intended brand colour never applies. |
| 119 | `rust_extensions/rag_engine/src/errors.rs` | 5-18 | low | dead-code | **RagError enum is entirely dead code — never constructed or returned** — errors.rs defines a full thiserror RagError enum (DimensionMismatch, CapacityExceeded, Io, Serialization) and lib.rs re-exports it via `pub use errors::RagError;`, but nothing in the crate ever constructs, returns, or matches on it. All fallible methods in lib.rs return PyResult and build PyValueError directly (e.g. add(), search(), save(), load()). The CapacityExceeded and Serialization variants in particular have no producer anywhere. |
| 120 | `rust_extensions/rag_engine/src/lib.rs` | 296-302 | low | correctness | **compute_similarity static method skips the finite-value validation enforced everywhere else** — add(), add_batch(), search(), and load() all reject non-finite (NaN/Inf) floats before they reach cosine_similarity, and the module comments (lib.rs:139-146, 199-208) explicitly justify this as a correctness guarantee. compute_similarity() is the one public entry point that omits it: it checks only `a.len() != b.len()` then calls cosine_similarity directly. A caller passing an Inf-containing vector reaches the scalar path where norm_a/norm_b become Inf, denom=Inf, and dot/denom yields either 0.... |
| 121 | `rust_extensions/rag_engine/src/lib.rs` | 467-505 | low | correctness | **load() accepts NaN/Inf timestamp from file while importance and embeddings are finite-validated** — On load(), embedding elements are filtered for finiteness (lib.rs:479) and importance is rejected if non-finite (lib.rs:490-491), but the timestamp from `item["timestamp"].as_f64()` (line 471) is stored unchecked into MemoryEntry.timestamp (line 499). A crafted/corrupt dump with timestamp = Infinity or NaN is therefore accepted. At query time, search() computes `(current_time - entry.timestamp)/3600.0` (line 237); with an Inf/NaN timestamp this yields a non-finite age and final_score, but t... |
| 122 | `scripts/bot_manager.py` | 330-340 | low | correctness | **_get_launcher_info dev-watcher detection uses raw substring scan, not exact arg-basename** — _get_launcher_info classifies a process as Dev Mode when 'dev_watcher' in cmdline_str or 'start_dev' in cmdline_str (line 339), then labels script via a substring ternary (line 340). This is the same substring trap that find_all_bot_processes was deliberately hardened against with exact_basenames (lines 297-300, 318-322): any cmdline that merely contains the substring 'dev_watcher' or 'start_dev' anywhere (e.g. a log-path argument) would be classified as Dev Mode and propagate the wrong launcher t... |
| 123 | `scripts/bot_manager.py` | 461 | low | performance | **cpu_percent(interval=0.05) per process serializes status collection and yields noisy readings** — get_bot_status loops over all bot PIDs calling pr.cpu_percent(interval=0.05) (line 461). Each call blocks 50ms, so N instances stall the status panel N*50ms, and a single 50ms sample is a poor CPU estimate (often near-zero or spiky). The status screen is reprinted on every menu loop iteration (main() line 1017 / print_status at 1017), so the cost is repeated. Confirmed as a measurable UX/perf cost, not a correctness bug. |
| 124 | `scripts/build_all.ps1` | 21-22 | low | error-handling | **build_all.ps1 success-path $LASTEXITCODE check is unreliable; child-script `exit 1` also short-circuits the parent** — build_all.ps1 invokes the child scripts with the call operator (`& "$ScriptsDir\build_rust.ps1"`, line 21) and then tests `if ($LASTEXITCODE -ne 0)` (line 22). Two problems. (1) On the FAILURE path, build_rust.ps1 / build_go.ps1 abort with `exit 1` (e.g. build_rust.ps1 lines 18, 40); a script-scope `exit` invoked via `&` terminates the entire host process, so build_all.ps1 never actually reaches line 22 — the explicit error message at lines 23-24 is dead code (the real fa... |
| 125 | `scripts/dev/probe_ai_deep.py` | 146-188 | low | correctness | **long_term_memory cache-fallback probe can silently assert nothing** — probe_long_term_memory_cycle stores a Fact only if _store_fact or store_fact exists (lines 171-174, no else/assert), and asserts the fact is retrievable only inside `if facts:` (line 182). If neither store method exists (silent skip) or get_user_facts returns an empty list in cache mode, the meaningful 'stored fact retrievable from cache' check at 184-188 is skipped and the probe passes having verified only isinstance(facts, list). A regression where cache-fallback stops persisting facts would not produce a FAIL. |
| 126 | `scripts/dev/probe_ai_deep.py` | 287-313 | low | error-handling | **tool_executor permission probe passes vacuously if execute_tool_call raises** — probe_tool_executor_permission has no try/except around the await, unlike sibling probes probe_tool_executor_none_args and probe_tool_executor_dm. If the production path raises (e.g. AttributeError against the minimal _NonAdmin/_DummyChannel stubs) before returning the denial string, the exception propagates out of main_async into asyncio.run(main_async()) in main(), aborting the entire probe run with a traceback and a non-zero exit, instead of recording a FAIL via check(). This skips probe_tool_executor_dm an... |
| 127 | `scripts/dev/probe_ai_flow.py` | 242-260 | low | correctness | **webhook_cache TTL probe forces expiry via timestamp=0, not the real TTL window** — probe_webhook_cache_ttl sets _webhook_cache_time[99] = 0 to force expiry. This proves get returns None for a 1970 timestamp but does not pin the actual TTL boundary, so a wrong-sign or wrong-unit TTL comparison could still pass. |
| 128 | `scripts/dev/validate_runtime.py` | 124-130 | low | error-handling | **CLI smoke check reports PASS even when the token is absent due to timeout** — check_cli computes ok = 'SMOKE_OK_4242' in reply and prints timed_out but does not use it in the verdict. A timeout (environmental) is therefore reported identically to a wrong-output regression (both FAIL), making triage of the opt-in validator harder. |
| 129 | `scripts/dev_watcher.py` | 261, 270-287, 516, 1001 | low | correctness | **dev_watcher PID_FILE is relative while bot_manager anchors it to PROJECT_ROOT** — PID_FILE = "bot.pid" (line 261) is a bare relative name resolved as Path(PID_FILE) at lines 272 (is_bot_running), 516 (_start_bot_unlocked cleanup) and 1001 (shutdown cleanup). At runtime main() does os.chdir(PROJECT_ROOT) at line 866 before any of these run, so the entry-point path is correct. bot_manager.py deliberately anchors PID_FILE to PROJECT_ROOT (line 77) to avoid CWD drift, and the module header (lines 40-43) explicitly states it wants to be importable by tests/other tools without chdir side effect... |
| 130 | `scripts/dev_watcher.py` | 744-747 | low | correctness | **Watched-extension check uses single suffix while _should_ignore uses full suffixes** — _handle_change computes file_ext = Path(event.src_path).suffix.lower() (line 745) and skips unless it is in watch_extensions (default [".py"]). Path.suffix returns only the last suffix and '' for dotless names. This is correct for the .py-only default, but it is asymmetric with _should_ignore (line 692) which uses p.suffixes (the full set). If a user ever adds a multi-part watch target the single-suffix compare misbehaves. A latent inconsistency rather than an active bug given the shipped default. |
| 131 | `scripts/dev_watcher.py` | 985-996 | low | error-handling | **Orphaned bot child if final shutdown wait() raises a non-TimeoutExpired error** — In main()'s shutdown block, event_handler.process.terminate() then wait(timeout=5); except subprocess.TimeoutExpired escalates to kill() (lines 989-994), but the broad except Exception at line 995 only logs at debug and does NOT attempt kill(). If wait() raises something other than TimeoutExpired (e.g. an OSError from the OS process API), the force-kill safety net is skipped and a hung child can survive the watcher exit. _start_bot_unlocked's stop block (lines 510-513) uses the more defensive pattern of forc... |
| 132 | `scripts/install_all.ps1` | 318 | low | maintainability | **Node.js direct-download fallback pins v22 while CLAUDE.md and the winget path target Node v24** — Line 318 sets $nodeUrl to node-v22.12.0-x64.msi for the non-winget fallback, while CLAUDE.md documents the active toolchain as Node v24 and the dashboard package.json targets a Node-24-era stack (TypeScript ^6.0.3, Vitest ^4.1.9). The winget path (line 315, OpenJS.NodeJS.LTS) currently resolves to Node 24. A machine provisioned without winget therefore gets a different major Node version than the documented and tested environment. |
| 133 | `scripts/install_all.ps1` | 379 | low | maintainability | **Dev tools installed unpinned, drifting from CLAUDE.md's pinned ruff 0.15.17** — Line 379 runs `pip install ruff bandit[toml] pip-audit mypy` with no version constraints. A freshly provisioned environment can pull a ruff/mypy newer than the version the codebase's ruff/mypy config was authored against (CLAUDE.md pins ruff 0.15.17), surfacing as new lint/type failures on an otherwise unchanged tree. |
| 134 | `scripts/maintenance/add_local_id.py` | 92-102 | low | correctness | **Verification SELECT min/max format spec raises TypeError if a channel_id is NULL** — SELECT DISTINCT channel_id at line 59 yields a NULL row if any ai_history row has channel_id IS NULL. The UPDATE loop at lines 64-78 uses `WHERE channel_id = ?` bound to None; SQL `= NULL` is never true, so those NULL-channel rows never receive a local_id. The verification query then GROUP BY channel_id produces a NULL group whose MIN/MAX(local_id) are NULL, and the format spec `{row['min_lid']:3}` at line 102 raises `TypeError: unsupported format string passed to NoneType.__format__`, masking an otherwis... |
| 135 | `scripts/maintenance/clean_cli_orphans.py` | 99-128 | low | resource-leak | **Dry-run 'freed MB' undercounts directory-tree bytes** — In the per-directory orphan branch, file sizes are added to deleted_bytes only inside the `if apply:` os.walk block (lines 99-105). On the default dry run, deleted_dirs is incremented (line 128) but no bytes from those trees are summed, so the 'freed ... MB' figure at line 152 omits directory-tree bytes. Top-level orphan .jsonl files (line 74) and stray files (line 141) DO sum bytes in dry-run, making the dir omission an inconsistent undercount that misleads an operator estimating reclaimable space. |
| 136 | `scripts/maintenance/dump_cli_prompt.py` | 37, 133 | low | maintainability | **No-arg invocation defaults to a placeholder substring that matches nothing useful** — When run with no positional arg, `arg` defaults to the literal placeholder at line 133, and main() runs LIKE '%placeholder%', which prints 'No conversation matching ...' and returns for virtually any DB. A no-arg run silently does nothing useful instead of dumping a recent conversation or printing usage. This is a developer-leftover default, not a correctness bug. (Note: the default value is Thai text, which is fine per project convention; the issue is the behavior, not the language.) |
| 137 | `scripts/maintenance/find_unused.py` | 86-122 | low | correctness | **Suffix fallback can mis-resolve sibling modules with same leaf name; dict mixes absolute and relative keys** — The slow fallback (119-121) returns the FIRST path_key that endswith '/'+var. When two packages contain the same leaf module name (cogs/ai_core/cache/store.py vs utils/cache/store.py), a suffix-resolved import picks whichever iterates first. The lookup dict mixes backslash-normalized absolute path keys (100) AND relative-to-root keys (107), so an absolute-path key can surface first, attributing the import to the wrong file and skewing the advisory report. |
| 138 | `scripts/maintenance/migrate_to_db.py` | 385-394 | low | error-handling | **History-file deletion swallows OSError silently while summary implies cleanup is complete** — In the cleanup loop, history files use try/except OSError: pass (386-390) and only increment deleted on success; metadata uses contextlib.suppress(OSError) (392-394). A file that fails to unlink (permission, Windows file lock) is silently dropped with no per-file diagnostic. The summary 'Deleted N / Skipped M' counts only migration-failures in M (396-398), not unlink failures, so leftover source JSON is left on disk while cleanup looks complete; a subsequent re-run re-migrates it (compounding the... |
| 139 | `scripts/maintenance/sync_doc_stats.py` | 310-316 | low | correctness | **--check exits 0 (in sync) when a stat is uncomputable even though the doc value is stale** — In apply(), when a stat's value is None (tool not on PATH), the rule is skipped with continue (310-311) and never compared. Under --check a doc number that genuinely drifted is reported as in-sync purely because the tool could not run. A CI docs-check job in a container without the JS toolchain passes green while vitest/playwright counts silently rot. A skipped-stats warning prints (284-285) but the exit code is still 0. |
| 140 | `scripts/maintenance/sync_doc_stats.py` | 317-325 | low | performance | **Drift-reporting block recomputes regex matches against the ORIGINAL text** — After building new_text, the reporting loop (318-324) re-runs re.finditer(pat, text) over the ORIGINAL text to print '{old} -> {new}' lines, independent of the right-to-left rewrite that produced new_text. It is a wasted second scan of every rule and the reported old->new pairs are derived from the original text rather than the actual applied diff. |
| 141 | `scripts/startup/_common.psm1` | 234 | low | correctness | **Stop-ExistingBot can force-kill an unrelated python process on PID reuse** — Stop-ExistingBot reads the PID from bot.pid, validates `^\d+$` (line 233), gets the process (line 234, the numeric string coerces to int for -Id so this works), and only checks `$Process.ProcessName -match 'python'` (line 235) before Stop-Process -Force (line 237). If the recorded PID was recycled by the OS to an unrelated python.exe (e.g. another bot, a Jupyter kernel, the dashboard CLI backend), the name guard still matches and the wrong process is force-killed. |
| 142 | `utils/database/database.py` | 1328-1357 | low | resource-leak | **close_pool cancels the WAL-checkpoint task but never awaits it before tearing down the pool** — close_pool() (async) cancels self._checkpoint_task at 1336-1337 then immediately drains/closes pooled connections and nulls _conn_pool/_pool_semaphore/_connection_count (1355-1357) WITHOUT awaiting the cancelled task. _periodic_wal_checkpoint (1079-1091) does its work inside 'async with self.get_connection()'; if the cancel lands while it is mid-checkpoint, the coroutine still owns a semaphore slot + pooled connection at the moment close_pool resets those fields. Because the task is never await... |
| 143 | `utils/database/database.py` | 1685-1700 | low | correctness | **save_ai_messages_batch counts ON CONFLICT updates as inserts, overstating the returned count** — executemany at 1685 uses ON CONFLICT(channel_id, message_id) ... DO UPDATE SET content, then inserted += len(rows_to_insert) at 1693 for every row regardless of whether the row was newly inserted or updated in place. When a batch re-saves messages whose (channel_id, message_id) already exist (a restore/replay of overlapping history), those rows update but are still counted, so the returned total can exceed the number of new rows added. The method's stated intent is to 'return the real number s... |
| 144 | `utils/database/database.py` | 190-262 | low | concurrency | **_schedule_export can drop a coalesced export when a save lands after the snapshot read but before the pending key is discarded** — _export_pending_keys coalesces: while a key is present, additional _schedule_export calls return early (195-196). The key is discarded only in do_export's finally AFTER export_channel_to_json completes (244). export_channel_to_json reads its row snapshot (the SELECT at 3532-3539) and then writes via to_thread. If a save commits AFTER that SELECT but BEFORE the finally discards the key, that save's _schedule_export returns early (key still present) and no follo... |
| 145 | `utils/database/database.py` | 264-292 | low | resource-leak | **_schedule_dashboard_export has no concurrent-task cap and shares the 20-task budget that the AI-history path relies on** — _schedule_export guards task creation with 'if len(self._export_tasks) >= 20: skip' (249-254). _schedule_dashboard_export adds its task to the SAME shared self._export_tasks set (287) but performs NO length check before create_task (286). Under a burst of saves across many distinct conversation_ids (each conversation_id is its own dedupe key in _dashboard_export_pending, so coalescing does not bound the count), dashboard export tasks proliferate unbounded AND push the... |
| 146 | `utils/localization.py` | 231-237 | low | correctness | **__getattr__ returns '[Missing message: key]' string instead of raising AttributeError for unknown keys** — LocalizedMessages.__getattr__ logs a warning for unknown keys but then calls self.get(key), which returns the literal '[Missing message: <key>]' string rather than raising AttributeError. This breaks attribute semantics: hasattr(messages,'typo') returns True, getattr(messages,x,default) never falls back to its default, and copy/pickle/inspection machinery that probes attributes silently gets placeholder strings. |
| 147 | `utils/media/ytdl_source.py` | 249-300 | low | security | **SSRF check is TOCTOU-separate from yt-dlp's own DNS resolution (DNS rebinding window)** — _is_private_url() resolves the hostname once, then yt-dlp independently re-resolves the same hostname when it calls extract_info(). An attacker controlling DNS can answer the first lookup with a public IP (passing the guard) and the second lookup (inside yt-dlp/ffmpeg) with 127.0.0.1 or 169.254.169.254. The guard reduces but does not eliminate SSRF for a DNS-rebinding adversary because the resolved IP is not pinned for the actual fetch. |
| 148 | `utils/media/ytdl_source.py` | 284-300 | low | error-handling | **Fallback swallows the original HQ-failure cause when re-raising** — When both HQ and fallback attempts fail, the code does raise e2 (the fallback exception) without chaining the original HQ exception e. The HQ failure reason — often more informative (auth/cookies/format) — is lost from the traceback. |
| 149 | `utils/media/ytdl_source.py` | 330-336 | low | security | **Stream size cap trusts advertised filesize metadata which the upstream controls** — In stream mode the disk-fill/DoS bound is enforced only against data.get('filesize')/filesize_approx. These values come from the remote server (or a malicious extractor) and are not authoritative — an endless or oversized live stream can omit filesize or advertise a small value, fully bypassing the cap, after which the raw upstream URL is handed to ffmpeg with no byte limit. |
| 150 | `utils/media/ytdl_source.py` | 357-369 | low | correctness | **temp/ confinement uses CWD-relative Path('temp') which can diverge from outtmpl base** — The download-path confinement resolves expected_root via Path('temp').resolve(), relative to CWD. outtmpl is also relative 'temp/...', so they normally agree, but the security check is tied to CWD rather than the project root. Works today but is fragile; ffmpeg_path by contrast anchors to Path(__file__).parents[2]. |
| 151 | `utils/monitoring/health_api.py` | 755-761 | low | concurrency | **/health/ready reads latency_ms and guild_count off-lock after is_healthy()** — is_healthy() (lines 335-354) takes _data_lock only for its own snapshot of is_ready/last_heartbeat/latency_ms and releases it. The /health/ready 200-response build at lines 759-760 then reads health_data.latency_ms and health_data.guild_count directly with no lock, while update_from_bot() (lines 224-233) writes those same fields under _data_lock on the event-loop thread. The inline comment at 753-754 only justifies is_healthy()'s lock for the readiness DECISION, not these two attribute reads. Impact is a slight... |
| 152 | `utils/monitoring/health_api.py` | 799-800, 938-940 | low | concurrency | **/stats, /stats/json and /health/deep read guild_count/latency_ms (and is_ready) off-lock — same class as the /health/ready finding** — The first auditor scoped the off-lock-read concern to /health/ready only, but the identical pattern recurs: /stats (lines 799-800) and /stats/json (817-818) read health_data.guild_count and health_data.latency_ms outside _data_lock (only the counters at 790-793/807-810 are snapshotted under _counter_lock), and _perform_deep_health_check reads health_data.is_ready (937, 941), health_data.latency_ms and health_data.guild_count (938-939) off-lock while update... |
| 153 | `utils/monitoring/health_client.py` | 195-204 | low | error-handling | **get_health()/is_ready()/push gate on _service_available which is None until connect() runs** — Verified: _service_available starts None (line 67) and is only set by _check_service() (174,185,189). get_health (197), is_ready (208), set_service_status (220) and _flush_buffer_locked (283) all gate on it. If a HealthAPIClient is constructed directly and used WITHOUT connect(), _service_available stays None (falsy) so get_health returns {'status':'unknown'} and metrics buffer forever without ever probing. However every module-level helper (push_request_metric, push_ai_response_time, get_health... |
| 154 | `utils/monitoring/performance_tracker.py` | 224-228 | low | concurrency | **get_stats reads _stats without _container_lock, unlike every sibling accessor** — Verified by reading the class. get_all_stats (235-237), get_summary (326-327), get_hourly_trend (245-248) and cleanup_old_stats (278-287) all snapshot the container dicts under self._container_lock, and record() mutates _stats under the same lock (187-190). get_stats is the lone reader that does `if operation not in self._stats` then `self._stats[operation].to_dict()` with no lock. _stats is a defaultdict(PerformanceStats); the `in` test does NOT auto-vivify and the early-return guards the subsequent subscri... |
| 155 | `utils/monitoring/structured_logger.py` | 434-478 | low | concurrency | **PerformanceTimer mutates shared _timings dict without a lock; used as a process-global singleton** — global_timer = PerformanceTimer() (line 698) is a module-level shared instance. measure() (440-450) does an unlocked check-then-insert (`if step_name not in self._timings: self._timings[step_name] = deque(...)`) then appends, and get_all_timings() (462-474) iterates self._timings.items() with no lock; clear() (476-478) mutates it. If global_timer is exercised from two real OS threads for a new step_name concurrently, the check-then-insert can race (one deque lost), or get_all_timings() ite... |
| 156 | `utils/monitoring/token_tracker.py` | 322-335 | low | concurrency | **export_stats() produces a torn snapshot across three separate lock acquisitions** — export_stats() calls get_global_stats(), get_daily_summary(30), and get_top_users(50) in sequence. Each helper acquires and releases self._lock independently (lines 222, 236, 209), so a concurrent record() can interleave between them. The exported 'global', 'daily_summary', and 'top_users' sections can therefore reflect different instants and disagree (e.g. global total_requests not equaling the summed daily/top-user numbers). This is a reporting consistency issue, not corruption, and the module is explici... |
| 157 | `utils/reliability/error_recovery.py` | 447-457 | low | correctness | **consecutive_failures is incremented on the terminal (non-retried) attempt, conflating 'backoff failures' with 'all failures'** — The except block increments state.consecutive_failures (453) on every failed attempt including the last one (attempt == max_retries-1) where no retry follows. The success path resets it but the exhausted-then-fallback path does not, so terminal failures accumulate and feed the >100 'abandoned' eviction special-case in _cleanup_old_backoff_states (136-139) and the 'total failures' log at 498. |
| 158 | `utils/reliability/error_recovery.py` | 459-460 | low | error-handling | **on_retry callback is invoked unguarded; a raising callback aborts the retry loop and bypasses fallback** — on_retry(attempt + 1, e) at line 460 sits directly inside the 'except config.recoverable_errors as e' block with no try/except around it. If the user callback raises, that exception propagates out of retry_async (through the finally that unpins the key), skipping remaining retries AND the fallback path at 502-504. |
| 159 | `utils/reliability/memory_manager.py` | 680-697 | low | resource-leak | **cached_with_ttl async path has no single-flight; concurrent cold-key callers all execute func, and the docstring overclaims stampede protection** — async_wrapper checks the cache then awaits func; N concurrent callers on a cold key all miss and each invoke func before any result is stored. The inline comment at line 695 ('including None to prevent stampede') implies concurrent-stampede protection that does not exist — None-caching only suppresses the SECOND-onward call after the first completes. |
| 160 | `utils/reliability/rate_limiter.py` | 569 and 618 | low | correctness | **Custom per-channel buckets skipped by reload_limits/update_all_adaptive_limits via key-split mismatch** — Custom channel config names contain a colon (channel_custom:CID). reload_limits (569) and update_all_adaptive_limits (618) get config name via key.split(':')[0], yielding channel_custom not channel_custom:CID, so the configs lookup is None and the bucket is skipped. After an owner sets a channel limit, reload never reconciles that bucket max_tokens/window. Built-in configs (no colon) are fine. |
| 161 | `cogs/ai_core/ai_cog.py` | 1431-1459 | improvement | performance | **Multi-chunk plain resend sends without pacing, may trigger Discord rate limiting** — _send_chunked (1431-1434) loops output_channel.send back-to-back with no inter-message delay, while the webhook {{Name}} path sleeps 0.5s between chunks (1456). A long stored message split into many 2000-char chunks fires sends in a tight burst, which can hit Discord's per-channel send rate limit and cause 429s / discord.py auto-throttling. Not a correctness bug (discord.py retries on 429), just suboptimal pacing on large resends. |
| 162 | `cogs/ai_core/api/api_failover.py` | 212 | improvement | correctness | **Proxy label host-extraction picks the wrong segment for non-standard base URLs** — FILE/LINE CORRECTION: the first-pass filed this against dashboard_config.py:212 (which is actually the GENERAL_UNRESTRICTED_FRAMING string). The real code is api_failover.py:212. There the label is built as proxy_base.split('//')[1] if '//' in proxy_base else proxy_base. For 'https://host/v1' this yields 'host/v1' (path included, not just authority); for a value with more than one '//' or a trailing-slash form it can grab an odd/empty segment. The value is only used for EndpointConfig.label -> display_name,... |
| 163 | `cogs/ai_core/api/api_failover.py` | 298-326 | improvement | correctness | **total_requests counts only successes and failures, omitting non-failover errors recorded elsewhere** — Verified the asymmetry: record_success (298-305) and record_failure (307-326) bump total_requests/total_failures, but the health_check probe paths (546-551 success, 569-574 failure) mutate consecutive_failures/last_success_time/last_failure_time WITHOUT touching total_requests or total_failures. Therefore failure_rate (total_failures/total_requests, surfaced in get_status at 625) reflects only real user-traffic requests and silently excludes health-probe outcomes. This is arguably intend... |
| 164 | `cogs/ai_core/api/dashboard_chat.py` | 841-855 | improvement | correctness | **Cap-notice text excluded from reported output token count when the model reported tokens** — When the tool-round cap is hit, _cap_notice is appended to full_response at line 823. The fallback output-token estimate at lines 853-855 only runs if total_output_tokens is falsy; if the model reported tokens, the injected notice's characters are never counted toward token_usage.output_tokens. This is cosmetic — Gemini did not generate the notice text, so excluding it from a billing-style count is arguably correct — but it means the displayed output-token figure slightly under-counts the characte... |
| 165 | `cogs/ai_core/api/dashboard_chat_claude.py` | 1235 vs 1278 | improvement | error-handling | **SDK client-side timeout (anthropic.APITimeoutError) on retry-exhaustion routes to the generic handler, not the dedicated timeout branch** — Verified at runtime that anthropic.APITimeoutError is NOT a subclass of builtin TimeoutError (MRO: APITimeoutError -> APIConnectionError -> APIError -> AnthropicError -> Exception). APITimeoutError IS in _RETRYABLE_ERRORS (line 62), so a stream timeout is caught at line 987, retried, and on exhaustion re-raised as last_error (1092). The outer `except TimeoutError` at 1235 does NOT catch it; it falls to `except Exception` at 1278 (record_failure + fail... |
| 166 | `cogs/ai_core/api/dashboard_chat_claude.py` | 1668-1698, 1770, 1784 | improvement | error-handling | **Same APITimeoutError mis-routing exists in the AI-edit handler (timeout message never shown for SDK timeouts)** — Parallel instance of finding 4 in handle_ai_edit_message_claude that the first auditor did not flag. _consume_claude_edit_stream catches anthropic.APITimeoutError via _RETRYABLE_ERRORS (1651) and on exhaustion re-raises last_error (1697). The outer `except TimeoutError` at 1770 (which would send 'Edit timed out. Please try again.') will NOT catch a re-raised anthropic.APITimeoutError because it is not a subclass of builtin TimeoutError; it falls through to `except Exception` a... |
| 167 | `cogs/ai_core/api/dashboard_chat_claude.py` | 756-757 | improvement | doc | **Stale line-number reference in comment (says line 683) for the unrestricted-mode badge gate** — The comment at lines 756-757 states the badge is gated on the SAME composite condition as the actual injection at 'line 683'. The actual injection condition `if unrestricted_mode and allow_unrestricted:` is at line 698, not 683. Line 683 is in the middle of the build_claude_message("user", ...) call. The logic is correct (badge gate at 758 matches the injection gate at 698); only the hard-coded line number is stale and will mislead a future reader. |
| 168 | `cogs/ai_core/api/dashboard_chat_claude_cli.py` | 2398-2422, 3539-3547 | improvement | maintainability | **Nested chat try/except recovery ordering is correct (verified) — no defect** — Independent verification of the chat handler's exception flow: there are two nested trys — innermost at line 2827 with a single `except RuntimeError as err` at line 2838 (indent 12) that handles ONLY the stale-session retry and re-raises everything else via `else: raise` (2913); and the enclosing try at line 2823 (indent 8) whose handlers `except TimeoutError` (2914), `except _OverloadedError` (2931, before `except RuntimeError` 2957), and `except Exception` (2973) correctly catch the re-raised/retry-raised exc... |
| 169 | `cogs/ai_core/api/dashboard_handlers.py` | 826-833 | improvement | correctness | **`delete_pair` with pair_message_id == 0/"0" issues a guaranteed-miss delete** — In handle_delete_message the guard at line 826 `if delete_pair and pair_message_id:` treats integer 0 and string '0' inconsistently: 0 is falsy and is skipped, but '0' is truthy, so int('0')=0 sets pair_message_id_int=0 (line 828) and then `if pair_message_id_int is not None:` (line 853) issues db.delete_dashboard_message(0, ...). Since dashboard message ids are AUTOINCREMENT starting at 1, id 0 never matches, so no row is deleted and no error is raised. Harmless today (a wasted DB round-trip and a minor asymm... |
| 170 | `cogs/ai_core/api/document_extractor.py` | 196-207 | improvement | maintainability | **MAX_DOC_PAGES, the 60s PDF timeout, and the three DOCX zip-guard constants are function-local rather than module-level** — MAX_DOC_PAGES=2000 (195), the 60s wall-clock guard (216), and _MAX_DOCX_TOTAL_BYTES/_MAX_DOCX_ENTRIES/_CHUNK (293-295) are defined inside their functions while MAX_EXTRACTED_CHARS, _MAX_DECODED_DOC_BYTES, MAX_TOTAL_CHARS, MAX_ROWS sit at module scope. The DOCX constants are re-created on every call. No behavioural bug; purely a single-place-to-audit-the-DoS-envelope organisation suggestion. |
| 171 | `cogs/ai_core/api/ws_dashboard.py` | 959 | improvement | correctness | **Rate-limit slot rollback relies on float-equality of loop time** — The concurrency-rejection rollback at line 959 (`if _times and _times[-1] == now: _times.pop()`) removes the timestamp appended at line 907 only if the deque tail exactly equals the `now` float for this frame. This works today because no await occurs between times.append(now) (907) and this check, so the appended value is still at the tail and bit-identical. It is fragile: a future refactor adding an await or another append between those points would silently turn the rollback into a no-op, re-introducing the self-lock-out... |
| 172 | `cogs/ai_core/api/ws_dashboard.py` | 976-993 | improvement | performance | **_on_task_done closure is recreated on every message/ai_edit_message frame** — A fresh _on_task_done function object is defined inside the read loop on every inbound 'message'/'ai_edit_message' frame (976-991) and attached via add_done_callback. The closure captures nothing message-specific (only references self via the bound sets), so allocating a new function per frame is avoidable churn on the hot path. Behavior is correct. |
| 173 | `cogs/ai_core/data/__init__.py` | 48-54 | improvement | maintainability | **_faust_attr treats an intentionally empty persona string as 'undefined'** — _faust_attr skips any attribute whose value is None OR == "" (line 53) and falls through to the next candidate name / default. An operator who deliberately sets e.g. FAUST_DM_INSTRUCTION = "" to suppress DM framing cannot do so via the new name — it falls back to the legacy FAUST_DM_MODE, then the empty default. Currently benign (final default is also ""), but the rule conflates 'unset' with 'set-to-empty-on-purpose' and would matter if a non-empty legacy alias existed. |
| 174 | `cogs/ai_core/data/roleplay_data.py` | 269 | improvement | style | **Module-level `import os` placed mid-file instead of at top** — `import os` appears at line 269 deep in the module body rather than with the header. Works fine but is an E402-style smell that hides the env dependency (GUILD_ID_RP). Purely cosmetic. |
| 175 | `cogs/ai_core/media_processor.py` | 371-403 | improvement | resource-leak | **Shared 2-worker GIF encode pool can be starved by abandoned timed-out encodes** — On a 60s timeout the worker thread is intentionally left running (documented at 384-396) and the pool has max_workers=2. Two consecutive pathological/slow GIFs that each time out occupy both workers until ffmpeg finishes, queuing subsequent legitimate encodes behind them. This is a deliberate trade-off versus the prior per-call thread leak, explicitly documented in the code. It is a known design tension, not a defect on a normal path. |
| 176 | `cogs/ai_core/memory/rag.py` | 1452-1476 | improvement | performance | **Small channel that misses the global FAISS top-k always falls back to full linear scan** — For channel_id is not None, FAISS returns the global top k=limit*3 (1458), filtered to _chan_by_id (1467-1470). For a small/new channel competing against a large global index, the global top-k can contain none of this channel's ids, emptying semantic_results and triggering _linear_search_raw over all_memories (1476) every query. This is intentional for recall (commented 1460-1466) and all_memories is already channel-scoped, so it is correct — but the FAISS optimization provides no speedup for such c... |
| 177 | `cogs/music/cog.py` | 416-431 | improvement | resource-leak | **queue_settings sidecar is never unlinked, accumulating one small file per guild that ever played** — _save_queue_settings_sync always writes queue_settings_{guild_id}.json on every save (volume/loop/mode_247) and never removes it, even when a guild reverts to all-default settings (volume 0.5, loop False, mode_247 False). Unlike _save_queue_json_sync which unlinks the queue file when the queue is empty (493-498), there is no symmetric unlink for the settings sidecar. On a large fleet this leaves one stale small file per guild after it stops using the bot. Not unbounded per-guild growth, ju... |
| 178 | `cogs/music/queue.py` | 99-121 | improvement | concurrency | **shuffle_queue/remove_track mutate shared deque lock-free without documenting the single-writer contract** — shuffle_queue does list->clear->extend and remove_track does index/del on the shared deque without _get_lock. toggle_loop documents the single-writer assumption (127-134) but shuffle_queue/remove_track don't. Under the current single-coroutine-per-guild dispatch this is safe; if violated, a concurrent get_next popleft interleaved with shuffle's clear/extend could drop/duplicate tracks. |
| 179 | `cogs/spotify_handler.py` | 433-435 | improvement | style | **import time re-evaluated inside pagination loops** — `import time` is inside the while-loops of get_all_playlist_tracks (433) and get_all_album_tracks (575). Harmless (sys.modules caches it) and time.sleep is correctly inside the executor thread, not the loop. Clarity nit. |
| 180 | `go_services/health_api/main.go` | 565-566 | improvement | improvement | **Batch endpoint lumps unknown metric types / name-mismatches into generic 'skipped' count** — In /metrics/batch, an entry with a Type outside {counter,histogram,gauge} matches no switch case (no default at line 565), and entries with a valid Type but a name not handled inside the inner switch (e.g. type=counter name=active_connections) also fall through without processed++. All such entries are reported only via skipped = len(payloads) - processed (lines 573-576). Unlike /metrics/push, which returns 400 'unknown metric type' / 'metric name/type mismatch', the batch caller gets 200 with no ... |
| 181 | `native_dashboard/src-ts/app.ts` | 2030-2044 | improvement | maintainability | **API-failover readiness poll keyed off a never-reset window flag** — initApiFailoverUI sets window._apiFailoverRequested=true on first run and skips the readiness poll thereafter. The function is invoked exactly once (DOMContentLoaded:117), so harmless today; the window-property guard is indirection that would also prevent re-requesting endpoints if init ever re-ran after a WebView2 navigation. Pure cleanliness. |
| 182 | `native_dashboard/src-ts/app.ts` | 398-399 | improvement | maintainability | **drawChart gradient derivation assumes rgba() input and no-ops on hex/hsl** — color.replace(/,\s*[\d.]+\)$/, ', 0.3)') only rewrites a trailing alpha before ')'. The two call sites (450-451) pass well-formed rgba() strings so it works today; a future hex or hsl() color would make the replace a no-op and addColorStop would receive an opaque color, yielding a solid fill. Latent fragility tied to an undocumented input contract, not a current bug. |
| 183 | `native_dashboard/src-ts/chat/context-window.ts` | 110-115 | improvement | performance | **save() runs on every update() even when nothing material changed** — update() calls save() for any non-null conversationId, doing Array.from(entries)+JSON.stringify of up to 200 entries on every token-usage frame. |
| 184 | `native_dashboard/src-ts/chat/formatter.ts` | 265-274 | improvement | maintainability | **Placeholder restore silently yields '' on any index mismatch** — Each restore pass uses blocks[parseInt(idx)] \|\| '', so an out-of-range index (future regex change emitting an untracked placeholder, or a control byte slipping past the line-55 strip) drops content to '' with no log. |
| 185 | `native_dashboard/src-ts/chat/prism.ts` | 94-98 | improvement | performance | **Dependency loading awaits sequentially; intentional for extend-ordered chains** — loadPrismLanguage awaits each dependency in series. This is required because Prism components call Prism.languages.extend(<base>) at eval time and load order matters. No action needed. |
| 186 | `native_dashboard/src/database.rs` | 104-143 | improvement | performance | **Connection cache degrades to single-flight under concurrent Tauri commands** — get_connection takes the one cached Connection out of the Mutex; concurrent callers open fresh connections and on drop only the last guard's connection is retained, so the cache gives no reuse benefit under concurrency. |
| 187 | `native_dashboard/tauri.conf.json` | 25 | improvement | maintainability | **CSP duplicated in tauri.conf.json and index.html can silently drift** — Confirmed: the script-src sha256 hash 'sha256-Y24CGBiNpvAHum+ZoXXPIUxmExgs7YP4BGdo45z8wdI=' appears identically in tauri.conf.json line 25 and the index.html <meta> CSP line 12. They agree today, but the hash is hand-maintained in two files; regenerating it in only one after editing the inline importmap would break the WebView (meta blocks the script) or drop Tauri-level enforcement. The tauri.conf version carries 'frame-ancestors none' which the meta deliberately omits (index.html lines 6-11 document the omission rat... |
| 188 | `rust_extensions/Cargo.toml` | 1-24 | improvement | correctness | **No [profile.release] overflow-checks; release builds silently wrap on integer overflow** — The workspace Cargo.toml defines no [profile.release] section, so release builds use the default overflow-checks=false and integer arithmetic wraps silently. Current size/index math is guarded (load() uses saturating_add for read_cap and min() for with_capacity; scalar_cosine indexing is bounded by a.len()/4), so there is no live overflow bug today, but any future arithmetic on a Python-supplied length would wrap to a small value rather than failing loudly. |
| 189 | `rust_extensions/media_processor/src/gif.rs` | 109-204 | improvement | dead-code | **get_gif_frame_count is dead code duplicating the parser** — get_gif_frame_count is re-exported in lib.rs but never invoked by any pymethods or pyfunction, and carries an explicit allow(dead_code). It duplicates roughly 90 lines of the GIF byte-walking logic in is_animated_gif. Maintaining two near-identical hand-rolled parsers means a future bounds-check fix to one can silently miss the other. Not a correctness bug today, but a maintenance hazard. |
| 190 | `rust_extensions/media_processor/src/resize.rs` | 53 and 58 | improvement | maintainability | **Inconsistent overflow handling vs check_bomb_dimensions** — resize_image computes the pixel-count guard as a plain widened multiply on lines 53 and 58, whereas lib.rs check_bomb_dimensions deliberately uses checked_mul for the same product. Both operands are u32 widened to u64 so the product cannot overflow u64; this is not a live bug, but the two guards diverge in style for identical math. |
| 191 | `rust_extensions/media_processor/src/resize.rs` | 71 | improvement | resource-leak | **Full decode after only a header-dimension cap** — The 100MP cap is enforced on header dimensions, then the image is fully decoded with load_from_memory. The pixel-count cap is a good proxy but does not strictly bound peak decode memory for formats whose decoders allocate auxiliary buffers proportional to entropy. This is an inherent limitation of header-based bomb detection, not a code error. |
| 192 | `rust_extensions/rag_engine/src/lib.rs` | 210-214 | improvement | performance | **search() deep-clones the entire embedding store on every query** — Before releasing the GIL, search() materializes `entries_snapshot: Vec<_> = entries.values().cloned().collect()`, cloning every MemoryEntry — including its full Vec<f32> embedding (dimension default 384) and id/text Strings — on each call. For an N-entry store this is O(N*dimension) float copies plus N String allocations per query, which can dominate the cosine cost. The snapshot is a deliberate choice to drop the read lock and release the GIL during the Rayon scan, but copying the whole embedding and id/text is heavier th... |
| 193 | `scripts/build_all.ps1` | 43 | improvement | doc | **'Next steps' text uses bash `&` background/chain syntax that is invalid in PowerShell** — Line 43 prints `.\bin\url_fetcher.exe & .\bin\health_api.exe`. In PowerShell `&` is the call operator, not bash's background-and-chain. A user copy-pasting this into a PowerShell prompt gets a parse error rather than two running services. It is only a Write-Host string (not executed here), so this is misleading guidance, not a runtime bug. |
| 194 | `scripts/maintenance/convert_log015.py` | 146-151 | improvement | error-handling | **stdin paths are not stripped of surrounding quotes** — main() builds paths via Path(line.strip()); a quoted path (shell-quoted for spaces) keeps the quote chars and path.exists() returns False → [MISS], silently skipping files the user expected to process. Ergonomic edge case, not a correctness bug on clean unquoted input. |
| 195 | `scripts/maintenance/watch_history.py` | 293-305 | improvement | correctness | **tail viewer shows rows as first written, not later in-place content edits** — The tail loop fetches WHERE id > last_id ORDER BY id ASC and advances last_id = max(last_id, row['id']) (293, 305). This is correct and resilient. But ai_history rows can be UPDATEd in place (production upsert DO UPDATE SET content on back-filled message_id; migration summarization can rewrite content). An edit to an already-printed row never re-appears, so the viewer only ever shows content as first inserted. Acceptable for tail -f but worth a one-line note. |
| 196 | `utils/media/colors.py` | 78-84 | improvement | error-handling | **enable_windows_ansi() colorama fallback can still raise and isn't caught** — If the kernel32 path raises (AttributeError/OSError/ValueError) and colorama is present, colorama.init() is called inside the except handler but not itself guarded. A failure in colorama.init() would propagate out of enable_windows_ansi() rather than returning False as the function's bool contract implies. |
| 197 | `utils/media/media_rust.py` | 280-295 | improvement | performance | **Standalone helpers construct a fresh MediaProcessorWrapper (and Rust processor) per call** — resize_image/is_animated_gif/image_to_base64 each instantiate a new MediaProcessorWrapper, which in the Rust path constructs a new RustMediaProcessor(max_dimension, jpeg_quality) every call. On a hot path this repeatedly allocates the native processor object instead of reusing a module-level singleton. |
| 198 | `utils/monitoring/performance_tracker.py` | 54 | improvement | correctness | **p50 uses nearest-rank upper element for even samples (no averaging)** — Verified: p50 returns sorted_times[len//2] (line 54), and to_dict() does the same (line 103). For an even-length sample this returns the higher of the two central values rather than averaging, and p95/p99 use int(n*0.95)/int(n*0.99) with min-clamp (lines 63-64, 73-74, 104-105) i.e. nearest-rank with no interpolation. This is a slight high-bias, acceptable for a coarse latency dashboard but not an exact statistical median/percentile despite the property being named/documented 'median'. No incorrect runtime behavior, ju... |
| 199 | `utils/monitoring/sentry_integration.py` | 95-116 | improvement | security | **_scrub_breadcrumb redacts only top-level string values, not nested dict/list values in crumb['data']** — _scrub_breadcrumb redacts crumb['message'] (102-103) and iterates crumb['data'] redacting only direct string values (104-108). It does not recurse into nested dicts/lists, so a breadcrumb with data={'request': {'headers': {'Authorization': 'Bearer ...'}}} passes through unredacted. The same shallow-only pattern appears in _scrub_event's extra walk (149-151) and in capture_exception/capture_message context redaction (261-264, 300-303). This is defense-in-depth: the WARNING breadcrumb ca... |
| 200 | `utils/monitoring/structured_logger.py` | 183-187 | improvement | error-handling | **Formatter assumes record.exc_info is a 3-tuple; a non-tuple truthy exc_info would crash formatting** — format() indexes record.exc_info[0].__name__ and record.exc_info[1] (184-185). The stdlib logging pipeline normalizes exc_info into a (type, value, tb) tuple before any formatter runs, so on every normal path this is safe. Only if code constructs a LogRecord manually and sets exc_info to a bare exception or True would indexing raise; that exception is outside the try/except (which wraps only the redact step at 194-199), so the record would be dropped via handleError. This is a hardening ... |

## Uncertain (flagged, not auto-fixed — need runtime/external confirmation)

| # | File | Line | Category | Detail |
| --- | --- | --- | --- | --- |
| 1 | `cogs/ai_core/api/dashboard_chat.py` | 500-507 | resource-leak | **generate_content_stream creation wrapped in wait_for may orphan the underlying stream on timeout** — asyncio.wait_for(gemini_client.aio.models.generate_content_stream(...), timeout=60.0) wraps stream CREATION. On timeout, wait_for cancels the coroutine; if the SDK had already opened an HTTP/stream resource there is no async-context-manager (async with) or aclose() around it — the stream is consumed via a plain 'async for' (line 525) and never explicitly closed even on the success path. For ... |
| 2 | `cogs/ai_core/memory/consolidator.py` | 707-717 | correctness | **Relationship contradiction can report an empty 'mentioned' value and self-match the related name** — The cached relationship pattern requires the captured relation token to appear AFTER both `name` and `related_name` in the text (rf'{name}.{{0,200}}?{related_name}.{0,80}?(token)'). When the stored relationship value itself is one of the fixed tokens (e.g. relation == 'แฟน') and the text mentions the same token, line 709 `'แฟน' not in 'แฟน'` is False so nothing fires (correct). But when rela... |
| 3 | `cogs/music/url_safety.py` | 55-79 | security | **Alt-form IPv4 canonicalization via inet_aton is platform-dependent (Windows skew)** — _parse_alt_ipv4 uses socket.inet_aton to canonicalize legacy IPv4 forms; the docstring notes it mirrors the OS rules 'on Linux'. On the Windows host, inet_aton's accepted syntaxes may differ from the eventual getaddrinfo resolver, so the cheap literal layer could under-block a form Windows' resolver would still target at a private IP. Not a hole on the full async path (is_url_query_safe_async runs a real g... |
| 4 | `utils/reliability/self_healer.py` | 719, 733-734 | correctness | **auto_heal aggressive KILL_DUPLICATE_BOTS reports success false when caller is itself a bot.py process** — kill_all_bots never kills self.my_pid (646-648), but the aggressive success check requires find_all_bot_processes to be empty (734). If the caller is a bot.py process it remains, so success is always false even after killing all others. CLI caller (cli) is unaffected. |
| 5 | `utils/reliability/shutdown_manager.py` | 442-464 | concurrency | **shutdown_sync may create a second event loop whose asyncio.Lock/Event are bound to the wrong loop when the bot loop runs on another thread** — If the bot loop runs on a non-main thread, the main-thread signal handler sees no running loop, calls asyncio.run() (fresh loop), and _get_lock()/_get_shutdown_event() may reuse primitives already bound to the real loop, raising 'attached to a different loop' or never signalling real waiters. |
| 6 | `scripts/maintenance/reindex_db.py` | 395-400 | correctness | **isolation_level toggle for VACUUM may leave an implicit txn open and re-trigger the failure it claims to fix** — The code sets conn.isolation_level = None for VACUUM, but the preceding SELECT at line 383 runs under the previously-restored isolation level and, under sqlite3 legacy isolation, a SELECT does not start a write transaction; however any prior DML in the same connection state could. More to the point, whether setting isolation_level mid-connection on aiosqlite actually flips the un... |
| 7 | `scripts/maintenance/sync_doc_stats.py` | 108-124 | correctness | **pytest count regex 'tests collected' misses pytest's 'collected N items' wording -> count silently left as None** — _pytest_total runs pytest --collect-only -q and parses r"(\d+)\s+tests?\s+collected". Modern pytest's -q collect-only summary prints 'N tests collected' only in some versions; the canonical phrasing is 'collected N items' (non-quiet) and with -q the tail is just 'N tests collected in Xs' on newer pytest but historically '====== N passed ======' is absent for collect-only. If t... |
| 8 | `scripts/dev/validate_runtime.py` | 185-190 | error-handling | **SDK no-credit detection only catches BadRequestError and string-matches 'credit'** — check_sdk treats a no-credit account as SKIP only if anthropic.BadRequestError is raised AND str(e) contains 'credit' (lines 185-186). Anthropic surfaces low/insufficient credit as a 400 with 'Your credit balance is too low...', which is BadRequestError, so the common case is handled. However other billing/quota states (402/403/429 -> PermissionDeniedError, RateLimitError, or generic APIStatusError) are not... |
| 9 | `scripts/dev/probe_ai_deep.py` | 265-268 | correctness | **Summarizer probe's AsyncMock create() may never be awaited, weakening the 'API not called' guarantee** — probe_summarizer_short sets s.client.messages.create = AsyncMock(side_effect=AssertionError('API was called')) to assert that short history short-circuits before the API. An AsyncMock with side_effect=AssertionError raises synchronously at call time (when create(...) is invoked), so if the production code DOES call create it will raise the AssertionError as designed — that part is fine. ... |
| 10 | `native_dashboard/src-ts/app.ts` | 1599-1650, 1795-1811 | bug | **Reopening the cropper for the identical data URL may not re-fire cropImage.onload, blocking the crop with a confusing toast** — openAvatarCropModal sets cropImage.onload then cropImage.src=imageUrl (1650). The only caller is handleAvatarUpload via FileReader, which produces a fresh data URL string each read — but re-selecting the SAME file yields a byte-identical data URL, and cropImage.src may already hold that exact value from a prior open. On some Chromium/WebView2 builds assigning src t... |
| 11 | `native_dashboard/src-ts/history-manager.ts` | 1101-1112, 1155-1174 | resource-leak | **Delegated click handlers are never removed on page teardown / manager disposal** — renderChannelList (1101-1112) and renderMessages (1155-1174) store one delegated click listener per container and correctly remove-then-re-add it each render, so handlers never stack on a single instance. init() (245-261) binds the three static buttons once, guarded by dataset.historyBound. The reported concern is only realizable if a NEW HistoryManager is instantiated against the SAME, still-attached DOM nod... |
| 12 | `native_dashboard/src-ts/history-manager.ts` | 342-346 | concurrency | **PERMANENT-error re-sync via openChannel() can start a second load while one is already pending** — onError() only reaches the re-sync at 345 when editInFlight was true (guard at 325) AND a PERMANENT code arrived (339). A mutation can only be started while pendingChannelLoadId is null (startEdit 701, requestDelete 817, undo 903 all gate on it), but openChannel has NO gate blocking it while editInFlight is true, so a user clicking a different channel row mid-ack sets pendingChannelLoadId. If ... |
| 13 | `native_dashboard/src-ts/chat/document-attach.ts` | 127-143 | correctness | **No aggregate-size cap on attached documents (5 x 32 MB = 160 MB, ~213 MB as base64)** — attach() enforces MAX_DOC_SIZE per file and MAX_ATTACHED_DOCS by count but never bounds cumulative payload size; five 32 MB files held as base64 data URLs and shipped in one WS send can spike WebView2 memory and may exceed backend/WS frame limits with a hard-to-diagnose send failure. Same for images (5 x 20 MB). |
| 14 | `native_dashboard/src-ts/chat/conversation-list.ts` | 153-186 | correctness | **Tag add/remove handlers capture a stale `conversation` closure; clicking after a switch mutates the wrong conversation** — renderTags(conversation) binds the remove-button click handlers (lines 153-164) and the add-input keydown handler (lines 169-185) over the `conversation` parameter captured at render time. Both handlers send WS frames keyed on conversation.id (lines 159, 176). If renderTags is NOT re-invoked when the active conversation changes (the active-conversation switch path repai... |
| 15 | `native_dashboard/src/bot_manager.rs` | 393-408 | correctness | **Orphan-kill 'is_test' entry-script detection can be defeated by interpreter flags taking a value** — is_test takes the first argv token after index 0 that does not start with '-' as the entry script; a separated-value interpreter option (e.g. `python -W default bot.py`) makes the value token 'default' be treated as the entry, mis-evaluating the test-skip heuristic. |
