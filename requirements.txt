# Discord Bot - Production Dependencies
# Updated: 2026-06-16 (version upgrade pass — latest stable across the board, Python 3.14.6)
# Python 3.14+

# ==================== Core ====================
discord.py==2.7.1             # Discord API wrapper; voice crypto is pinned separately below
python-dotenv==1.2.2          # Environment variable management (CVE-2026-28684)
aiohttp==3.14.1               # Async HTTP client (security fixes)
psutil==7.2.2                 # Process and system utilities
certifi>=2026.6.17,<2027      # SSL certificates (security updates)
cryptography>=49.0.0,<50      # Transitive dep — pinned for CVE-2026-34073, CVE-2026-39892
requests>=2.34.2,<3           # Transitive dep — pinned for CVE-2026-25645
tzdata>=2026.1,<2027          # IANA timezone data (required on Windows)

# ==================== AI ====================
google-genai==2.10.0          # Google Gemini API (latest — unified GenAI SDK v2)
anthropic>=0.109.2,<1.0       # Anthropic Claude API (CVE-2026-34450, CVE-2026-34452)
Pillow==12.3.0                # Image processing (CVE-2026-40192/42308/42309/42310/42311)
beautifulsoup4==4.15.0        # HTML parsing for URL content (latest)
lxml==6.1.1                   # Fast HTML parser (CVE-2026-41066)
numpy==2.5.1                  # Numerical operations (for RAG embeddings, latest)
faiss-cpu==1.14.3             # Vector similarity search (latest)
imageio[ffmpeg]==2.37.3       # GIF to video conversion for Gemini (latest)
pypdf>=6.14.2,<7              # PDF text extraction; floor must stay >=6.9.2 (CVE-2026-33699 infinite-loop DoS, +27628/24688/28351 on untrusted PDFs)
python-docx>=1.2.0,<2         # DOCX text extraction for dashboard document memory
defusedxml>=0.7.1,<1          # No longer imported by our code (python-docx self-protects via resolve_entities=False); kept as a harmless pin, safe to drop

# ==================== Music ====================
yt-dlp==2026.7.4              # YouTube downloader — IMPORTANT: old versions freeze on changed APIs
spotipy==2.26.0               # Spotify API wrapper (latest)
PyNaCl>=1.6.2,<2              # Voice transport encryption for discord.py voice
davey>=0.1.5,<1              # DAVE protocol (E2EE) — REQUIRED by discord.py 2.7+ for voice alongside PyNaCl (voice_client raises without it)

# ==================== Database ====================
aiosqlite==0.22.1             # Async SQLite (latest)

# ==================== Development ====================
watchdog==6.0.0               # File watcher for hot reload (latest)
colorama==0.4.6               # Windows ANSI color support (latest)
pyyaml>=6.0.3,<7             # YAML parsing for prompt templates
types-PyYAML>=6.0.12.20260518,<7         # Type stubs for mypy
types-requests>=2.33.0.20260518,<3  # Type stubs for requests/spotipy integration

# ==================== Monitoring (Optional) ====================
sentry-sdk==2.64.0            # Error tracking (optional)
prometheus-client==0.25.0     # Metrics & monitoring (latest)

# ==================== Performance ====================
orjson==3.11.9               # 10x faster JSON parsing (drop-in replacement)


# ==================== Windows Utilities ====================
winshell==0.6; sys_platform == 'win32'   # Windows shell integration
pywin32==312; sys_platform == 'win32'    # Windows COM automation

# ==================== Token Counting ====================
tiktoken==0.13.0              # Accurate token counting for AI (latest)

# ==================== Testing ====================
pytest==9.1.1                 # Test framework (CVE-2025-71176)
pytest-asyncio==1.4.0         # Async test support (latest)
pytest-timeout==2.4.0         # Safety net: kills hung tests after 30s
pygments>=2.20.0,<3          # Syntax highlighter — pinned for CVE-2026-4539

# ==================== Implicit Dependencies ====================
# These are auto-installed with the packages above:
# - aiosignal, frozenlist (aiohttp)
# - google-auth, googleapis-common-protos, protobuf (google-genai)
# - requests, urllib3, certifi (spotipy)
